# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/

# Build
# Anchored to repo root (not `build/`/`dist/` unanchored) so these don't
# accidentally also match source directories that happen to be named
# "build" or "dist" elsewhere in the tree -- e.g. this repo's own
# scripts/build/ (Plan §3 layout), which is committed tooling, not output.
/build/
/dist/

# Logs
*.log

# Secrets
.env
.env.*
*.pem
*.key

# Temporary
tmp/
temp/
.cache/

# Playwright (Story #10) -- generated test output, never committed
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# CDN deploy staging (scripts/deploy_gadgets_media.sh) -- transient, never committed
.deploy/

# --- Binary media -----------------------------------------------------------
# widgets/ and gadgets/ are shared UI assets consumed by nebulacast.com,
# stellar-attractor, and other products. Rendered/binary media lives on the
# CDN, not in git. Vector/text formats (svg, and text-based data like csv/
# json/yml) are NOT listed here — they're diffable and fine to keep in git.

# Images (raster)
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.tiff
*.tif
*.webp
*.heic
*.heif
*.ico
*.psd
*.xcf

# Video
*.mp4
*.mov
*.avi
*.mkv
*.webm
*.flv
*.wmv
*.m4v
*.mpg
*.mpeg

# Audio
*.mp3
*.wav
*.flac
*.aac
*.ogg
*.wma
*.m4a
*.aiff
*.aif

# Animation / design project files
*.blend
*.blend1
*.aep
*.prproj
*.c4d

# 3D models
*.fbx
*.obj
*.glb
*.gltf
*.stl
*.dae

# Raw camera formats
*.raw
*.cr2
*.cr3
*.nef
*.arw
*.dng