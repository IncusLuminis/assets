# scripts/maintenance/

Reserved for `inventory.ts` (re-run the Story #2-style survey against the
current `library/`), `verify-cdn.ts` (confirm every published version's
assets are actually reachable on the CDN), and `detect-orphans.ts` (find
`dist/` or CDN content with no matching source) — Architecture §48.

Deliberately empty in Story #5. No maintenance tooling exists yet to
maintain.
