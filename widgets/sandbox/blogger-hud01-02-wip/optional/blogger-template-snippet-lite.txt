<!--
  NebulaCast HUD Panels — Blogger Template Snippet LITE (HUD-01 / HUD-02 / HUD-03)
  =================================================================================
  Paste this entire block into your Blogger theme's HTML,
  just before the closing </head> tag.

  Blogger → Theme → Edit HTML → find </head> → paste above it → Save.

  This only needs to be done ONCE per blog.
  After this, paste individual post HTML blocks from examples/ into Blogger posts.

  Contents:
  1. Google Fonts (Share Tech Mono)
  2. Shared CSS (keyframes, media containers, paper rows, row layout)
  3. CSS for HUD-01 (nc-ol-*), HUD-02 (nc-or-*), HUD-03 (nc-hp-*)
  4. Shared JS: NcHudMini, HudPapers
  5. Panel JS: NcHud01 factory only

  Note: HUD-02 and HUD-03 JS factories are NOT included in the lite snippet.
  Use the full snippet if you need NcHud02.init() or NcHud03.init().
-->

<!-- NebulaCast HUD: Google Fonts -->
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="anonymous"/>
<link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&amp;display=swap" rel="stylesheet"/>

<!-- NebulaCast HUD: Shared CSS (keyframes, media, paper rows, row layout) -->
<style>
/*<![CDATA[*/
/* ================================================================
   NebulaCast HUD — Shared Styles
   Version : 1.0  (2026-06-09)
   Covers  : Styles common to HUD-01 (nc-ol-*) and HUD-02 (nc-or-*).
             Must be loaded BEFORE the individual panel CSS files.

   WHERE TO PUT THIS:
   Blogger → Theme → Edit HTML
   Add a <style> block containing this file's content just before
   the panel-specific <style> blocks (HUD-01 and/or HUD-02).
   In staging.html a single <link> tag near the top handles this.
   ================================================================ */

/* ================================================================
   SHARED KEYFRAMES
   ================================================================ */

@keyframes hudBreath {
  0%, 100% {
    box-shadow:
      0 0 18px rgba(0,220,255,.18),
      0 0 42px rgba(0,140,255,.10),
      inset 0 0 28px rgba(0,255,255,.08);
  }
  50% {
    box-shadow:
      0 0 28px rgba(0,240,255,.28),
      0 0 64px rgba(0,180,255,.16),
      inset 0 0 42px rgba(0,255,255,.12);
  }
}

@keyframes hudPanelSweep {
  0%   { transform: rotate(8deg) translateX(-320%); }
  100% { transform: rotate(8deg) translateX(900%); }
}

@keyframes hudProgressScan {
  0%   { transform: translateX(-100%); }
  100% { transform: translateX(100%);  }
}

@keyframes hudRotate {
  from { transform: rotate(0deg); }
  to   { transform: rotate(360deg); }
}

@keyframes hudPulseOuter {
  0%, 100% { transform: scale(.94); opacity: .35; }
  50%      { transform: scale(1.02); opacity: 1; }
}

@keyframes hudPulseInner {
  0%, 100% { transform: scale(.92); opacity: .28; }
  50%      { transform: scale(1.05); opacity: .78; }
}

@keyframes hudBlinkSoft {
  0%, 100% { opacity: .32; }
  50%      { opacity: 1; }
}

@keyframes hudLockBoxPulse {
  0%, 100% { transform: scale(.78); opacity: .25; }
  50%      { transform: scale(1.12); opacity: .95; }
}

/* ── .nc-panel-sweep ─────────────────────────────────────────── */

.nc-panel-sweep {
  position: absolute;
  top: -38%;
  left: -32%;
  width: 58%;
  height: 180%;
  pointer-events: none;
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(80,220,255,0) 8%,
    rgba(80,220,255,.06) 25%,
    rgba(180,255,255,.22) 50%,
    rgba(80,220,255,.08) 75%,
    transparent 100%
  );
  transform: rotate(8deg) translateX(-320%);
  mix-blend-mode: screen;
  animation: hudPanelSweep 7s linear infinite;
}

/* ================================================================
   SHARED PAPER ROWS  (HudPapers.render() output)
   ================================================================ */

.nc-hud-panel-header {
  font-family: "Share Tech Mono", monospace;
  font-size: 15px;
  letter-spacing: .09em;
  text-transform: uppercase;
  color: rgba(80, 220, 255, 0.82);
  text-align: center;
  padding: 7px 8px 8px;
  border-bottom: 1px solid rgba(80, 220, 255, 0.12);
  margin-bottom: 2px;
}

.nc-hud-paper-row {
  display: flex;
  align-items: baseline;
  gap: 6px;
  padding: 5px 8px;
  border-bottom: 1px solid rgba(80, 220, 255, 0.06);
  transition: background .15s;
}
.nc-hud-paper-row:hover { background: rgba(80, 220, 255, 0.04); }
.nc-hud-paper-row:last-child { border-bottom: none; }

.nc-hud-paper-year {
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .06em;
  color: rgba(80, 220, 255, 0.50);
  flex-shrink: 0;
  white-space: nowrap;
}

.nc-hud-paper-title {
  font-family: "Share Tech Mono", monospace;
  font-size: 12px;
  line-height: 1.45;
  letter-spacing: .02em;
  color: rgba(80, 220, 255, 0.88);
  flex: 1;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  text-decoration: none;
  cursor: pointer;
}
a.nc-hud-paper-title:hover {
  color: rgba(80, 220, 255, 1);
  text-shadow: 0 0 10px rgba(80, 220, 255, 0.45);
  text-decoration: underline;
  text-underline-offset: 2px;
}

.nc-hud-paper-journal {
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .05em;
  color: rgba(80, 220, 255, 0.32);
  flex-shrink: 0;
  white-space: nowrap;
}

/* ================================================================
   SHARED MEDIA CONTAINERS  (nc-hud-media, YouTube / TikTok facades)
   ================================================================ */

.nc-hud-media {
  position: relative;
  width: 100%;
  overflow: hidden;
  background: #000;
  margin-bottom: 14px;
}

.nc-hud-media .nc-reconnect {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .18em;
  color: rgba(80,220,255,.55);
  pointer-events: none;
  z-index: 0;
}

.nc-hud-media iframe {
  position: relative;
  z-index: 1;
  display: block;
  width: 100%;
  height: 100%;
  border: none;
}

.nc-hud-media-vertical   { aspect-ratio: 9/16; position: relative; overflow: hidden; }
.nc-hud-media-horizontal { aspect-ratio: 16/9; position: relative; overflow: hidden; }

/* ── Micro-mode poster overlay (static frame + play icon) ────────
   For media types without a click-to-load facade (HeyGen currently —
   YouTube/TikTok already replace themselves with the real iframe on
   click, see .nc-yt-facade/.nc-tt-facade below). The real <iframe>
   stays mounted the whole time; this div just visually covers it
   while the widget is collapsed to micro, so the embed's own native
   player chrome isn't visible until the panel expands. No extra click
   handler needed — a click on this div bubbles straight through to
   the widget's existing mini→maxi expand handler (NcHudMini.init in
   hud-mini.js already expands on any click while .is-mini). */
.nc-hud-media-poster {
  display: none;
  position: absolute;
  inset: 0;
  z-index: 2;
  cursor: pointer;
  background-color: #000;
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-media-poster,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-media-poster {
  display: block;
}
.nc-hud-media-play {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 26px;
  height: 26px;
  border-radius: 50%;
  background: rgba(0, 10, 20, .55);
  border: 1.5px solid rgba(80, 220, 255, .9);
  box-shadow: 0 0 10px rgba(80, 220, 255, .6);
  pointer-events: none;
}
.nc-hud-media-play::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 56%;
  transform: translate(-50%, -50%);
  border: 0 solid transparent;
  border-top: 6px solid transparent;
  border-bottom: 6px solid transparent;
  border-left: 10px solid rgba(150, 235, 255, .95);
}

/* Raw YouTube embed (пастят напрямую из Share → Embed): responsive 16:9 */
.nc-hud-01-html-slot iframe[src*="youtube.com/embed"],
.nc-hud-01-html-slot iframe[src*="youtube-nocookie.com/embed"],
.nc-hud-02-html-slot iframe[src*="youtube.com/embed"],
.nc-hud-02-html-slot iframe[src*="youtube-nocookie.com/embed"] {
  width: 100%;
  height: auto;
  aspect-ratio: 16 / 9;
  border: none;
  display: block;
}

/* ── YouTube facade (thumbnail preview → iframe on click) ───── */
.nc-yt-facade {
  position: absolute;
  inset: 0;
  cursor: pointer;
  background-color: #000;
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  transition: filter .2s ease;
}
.nc-yt-facade:hover { filter: brightness(.85); }
.nc-yt-play {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 68px;
  height: 48px;
  background: rgba(200, 0, 0, .88);
  border-radius: 10px;
  pointer-events: none;
  transition: background .15s ease, transform .15s ease;
}
.nc-yt-play::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 54%;
  transform: translate(-50%, -50%);
  border: 0 solid transparent;
  border-top:  11px solid transparent;
  border-bottom: 11px solid transparent;
  border-left: 20px solid #fff;
}
.nc-yt-facade:hover .nc-yt-play {
  background: rgba(220, 0, 0, 1);
  transform: translate(-50%, -50%) scale(1.08);
}

/* ── TikTok facade — thumbnail + play button, iframe вставляется при клике ──
   position: relative (не absolute!) чтобы не уходить за bounds контейнера в maxi */
.nc-tt-facade {
  position: relative;
  display: block;
  width: 100%;
  height: 100%;
  cursor: pointer;
  background-color: #111;
  background-size: cover;
  background-position: center top;
  background-repeat: no-repeat;
  transition: filter .2s ease;
}
.nc-tt-facade:hover { filter: brightness(.85); }
.nc-tt-play {
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  width: 48px; height: 48px;
  background: rgba(254, 44, 85, .88);
  border-radius: 50%;
  pointer-events: none;
  transition: background .15s ease, transform .15s ease;
}
.nc-tt-play::after {
  content: '';
  position: absolute; top: 50%; left: 56%;
  transform: translate(-50%, -50%);
  border: 0 solid transparent;
  border-top: 9px solid transparent;
  border-bottom: 9px solid transparent;
  border-left: 17px solid #fff;
}
.nc-tt-facade:hover .nc-tt-play {
  background: rgba(254, 44, 85, 1);
  transform: translate(-50%, -50%) scale(1.08);
}

/* ================================================================
   ROW CONTAINER  (.nc-hud-row)
   ================================================================ */

.nc-hud-row {
  display: flex;
  flex-wrap: wrap;
  gap: 0;
  align-items: flex-start;
  background: #000;
}

/* HUD-01 (nc-ol-widget) mini sizing */
.nc-hud-row .nc-ol-widget.is-mini {
  flex: 0 0 317px;
  width: 317px;
  max-width: 317px;
}
.nc-hud-row .nc-ol-widget[data-collapse="micro"].is-mini {
  flex: 0 0 var(--nc-micro-w, 87px);
  width: var(--nc-micro-w, 87px);
  max-width: var(--nc-micro-w, 87px);
}
.nc-hud-row .nc-ol-widget:not(.is-mini) {
  flex: 0 0 100%;
  order: 99;
}

/* HUD-02 (nc-or-widget) mini sizing */
.nc-hud-row .nc-or-widget.is-mini {
  flex: 0 0 auto;
  width: 317px;
}
.nc-hud-row .nc-or-widget[data-collapse="micro"].is-mini {
  width: var(--nc-micro-w, 87px);
}
.nc-hud-row .nc-or-widget:not(.is-mini) {
  flex: 0 0 100%;
  order: 99;
}
/*]]>*/
</style>

<!-- NebulaCast HUD: Panel CSS — HUD-01 (nc-ol-*) -->
<style>
/*<![CDATA[*/
/* ================================================================
   NebulaCast HUD-01 — Blogger Template CSS
   Version : 1.1  (2026-06-09)
   Covers  : HUD-01 (object lock panel, nc-ol-*)
   Scope   : All nc-ol-* and nc-hud-01-* classes — safe for Blogger,
             no :root overrides, no global resets.

   REQUIRES: blogger-hud-shared.css must be loaded first.
             (keyframes, .nc-panel-sweep, paper rows,
              media base, .nc-hud-row)

   WHERE TO PUT THIS:
   Blogger → Theme → Edit HTML
   Paste the <style> block from optional/blogger-template-snippet.txt
   just before the closing </head> tag.
   ================================================================ */

/* ── Box-sizing (scoped — does not touch Blogger theme) ────────── */

.nc-ol-widget,
.nc-ol-widget * {
  box-sizing: border-box;
}

/* Keyframes, .nc-panel-sweep → blogger-hud-shared.css */

/* ================================================================
   WIDGET ROOT
   ================================================================ */

.nc-ol-widget {
  position: relative;
  width: 100%;
  margin: 0 auto;
  padding: 0;
  overflow: hidden;
  background: #000;
  font-family: "Share Tech Mono", monospace;
  max-height: 600px;
  transition: max-height 1000ms cubic-bezier(.2, .9, .2, 1),
              opacity    150ms ease;
}

/* ── Main panel ──────────────────────────────────────────────── */

.nc-ol-panel {
  position: relative;
  width: 100%;        /* fills blog width in expanded state */
  height: 420px;
  margin: 0 auto;

  display: grid;
  grid-template-columns: 520px 1fr;

  overflow: visible;  /* allows button to sit outside frame */

  background:
    linear-gradient(135deg, rgba(3,28,45,.96), rgba(0,9,20,.98)),
    rgba(0,8,16,.95);

  opacity: .65;

  animation:
    ncOlOpenHud 1.2s cubic-bezier(.2,.9,.2,1) forwards,
    hudBreath 5s ease-in-out infinite;

  transform-origin: top left;  /* mini thumbnail anchors from left edge */
  transition: transform 1000ms cubic-bezier(.2, .9, .2, 1);
}

/* ── SVG frame ───────────────────────────────────────────────── */

.nc-ol-frame-svg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 30;
  pointer-events: none;
  overflow: visible;
}

.nc-ol-frame-line {
  fill: none;
  stroke: rgba(80,220,255,.88);
  stroke-width: 2;
  vector-effect: non-scaling-stroke;
  filter: drop-shadow(0 0 5px rgba(80,220,255,.85));
}

.nc-ol-frame-inner {
  stroke: rgba(150,245,255,.62);
  stroke-width: 1.2;
}

/* At mini/micro scale the panel is CSS-shrunk (transform:scale or a forced
   small box) while vector-effect:non-scaling-stroke keeps this stroke at
   its literal 1.2px width — thin enough that it anti-aliases into the
   background and reads as "missing" next to the much bolder outer line.
   Widening it only in the collapsed state restores the double-frame look
   without touching the crisp maxi rendering. */
.nc-ol-widget.is-mini .nc-ol-frame-inner {
  stroke-width: 2.2;
}

.nc-ol-frame-photo-top,
.nc-ol-frame-photo-bottom {
  stroke: rgba(100,235,255,.72);
  stroke-width: 1.4;
}

.nc-ol-frame-tab {
  fill: rgba(70,200,255,.90);
  filter: drop-shadow(0 0 10px rgba(80,220,255,.9));
}

.nc-ol-frame-tab-top {
  animation: ncOlTopSvgSlider 5s ease-in-out infinite;
}

/* ── Sweep ───────────────────────────────────────────────────── */

.nc-ol-sweep {
  z-index: 3;
  opacity: .5;
}

/* ── Left image column ───────────────────────────────────────── */

.nc-ol-left {
  position: relative;
  min-width: 520px;
  width: 520px;
  height: 420px;
  overflow: hidden;
  background: #000;
  z-index: 1;
}

.nc-ol-image {
  position: absolute;
  left: 42px;
  top: 34px;
  width: 445px;
  height: 352px;
  display: block;
  object-fit: cover;
  filter: brightness(.72) contrast(1.18) saturate(.85);
}

/* ── Reticle ─────────────────────────────────────────────────── */

.nc-ol-reticle {
  position: absolute;
  left: 42px;
  top: 34px;
  width: 445px;
  height: 352px;
  z-index: 4;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
  transition: opacity .3s ease;
}

.nc-ol-ring {
  position: absolute;
  border-radius: 50%;
}

.nc-ol-ring-1 {
  width: 270px;
  height: 270px;
  border: 2px solid rgba(80,245,255,.75);
  box-shadow: 0 0 18px rgba(80,245,255,.5);
  animation: hudPulseOuter 4s ease-in-out infinite;
}

.nc-ol-ring-2 {
  width: 210px;
  height: 210px;
  border: 2px dashed rgba(80,245,255,.82);
  animation: hudRotate 10s linear infinite;
}

.nc-ol-ring-3 {
  width: 120px;
  height: 120px;
  border: 1px solid rgba(170,255,255,.88);
  opacity: .65;
  animation: hudPulseInner 2.5s ease-in-out infinite;
}

.nc-ol-cross {
  position: absolute;
  width: 220px;
  height: 220px;
}

.nc-ol-cross::before,
.nc-ol-cross::after {
  content: "";
  position: absolute;
  background: rgba(120,255,255,.85);
  box-shadow: 0 0 10px rgba(80,245,255,.72);
}

.nc-ol-cross::before {
  width: 100%;
  height: 1px;
  top: 50%;
  left: 0;
}

.nc-ol-cross::after {
  width: 1px;
  height: 100%;
  left: 50%;
  top: 0;
}

.nc-ol-lock-box {
  position: absolute;
  width: 150px;
  height: 150px;
  animation: hudLockBoxPulse 3.2s ease-in-out infinite;
}

.nc-ol-lock-box span {
  position: absolute;
  display: block;
  width: 34px;
  height: 34px;
  filter: drop-shadow(0 0 8px rgba(80,245,255,.9));
}

.nc-ol-lock-box span::before,
.nc-ol-lock-box span::after {
  content: "";
  position: absolute;
  background: rgba(150,255,255,.95);
  box-shadow: 0 0 10px rgba(80,245,255,.75);
}

.nc-ol-lock-box span::before { width: 34px; height: 3px; }
.nc-ol-lock-box span::after  { width: 3px;  height: 34px; }

.nc-ol-lock-box span:nth-child(1)         { top: 0;    left: 0; }
.nc-ol-lock-box span:nth-child(1)::before { top: 0;    left: 0; }
.nc-ol-lock-box span:nth-child(1)::after  { top: 0;    left: 0; }

.nc-ol-lock-box span:nth-child(2)         { top: 0;    right: 0; }
.nc-ol-lock-box span:nth-child(2)::before { top: 0;    right: 0; }
.nc-ol-lock-box span:nth-child(2)::after  { top: 0;    right: 0; }

.nc-ol-lock-box span:nth-child(3)         { bottom: 0; left: 0; }
.nc-ol-lock-box span:nth-child(3)::before { bottom: 0; left: 0; }
.nc-ol-lock-box span:nth-child(3)::after  { bottom: 0; left: 0; }

.nc-ol-lock-box span:nth-child(4)         { bottom: 0; right: 0; }
.nc-ol-lock-box span:nth-child(4)::before { bottom: 0; right: 0; }
.nc-ol-lock-box span:nth-child(4)::after  { bottom: 0; right: 0; }

.nc-ol-corner {
  position: absolute;
  width: 38px;
  height: 38px;
  border-color: rgba(210,255,255,.95);
}

.nc-ol-corner-tl { top: 46px;    left: 46px;  border-top: 2px solid; border-left: 2px solid; }
.nc-ol-corner-tr { top: 46px;    right: 46px; border-top: 2px solid; border-right: 2px solid; }
.nc-ol-corner-bl { bottom: 46px; left: 46px;  border-bottom: 2px solid; border-left: 2px solid; }
.nc-ol-corner-br { bottom: 46px; right: 46px; border-bottom: 2px solid; border-right: 2px solid; }

.nc-ol-center-dot {
  width: 14px;
  height: 14px;
  border-radius: 50%;
  border: 2px solid rgba(255,255,255,.92);
  box-shadow: 0 0 18px rgba(80,245,255,.95);
}

.nc-ol-target-lock {
  position: absolute;
  bottom: 30px;
  font-family: "Share Tech Mono", monospace;
  font-size: 13px;
  letter-spacing: .26em;
  color: #d8ffff;
  text-shadow: 0 0 12px rgba(80,245,255,.95);
  animation: hudBlinkSoft 1.4s ease-in-out infinite;
}

/* ── Right data column ───────────────────────────────────────── */

.nc-ol-right {
  position: relative;
  z-index: 2;
  min-width: 0;
  height: 420px;
  padding: 52px 68px 30px 10px;
  color: #cfffff;
  background: linear-gradient(90deg, rgba(0,18,32,.97), rgba(0,12,26,.99));
  text-shadow: 0 0 8px rgba(80,245,255,.55);
  overflow: hidden;
}

.nc-ol-right > * {
  position: relative;
  z-index: 4;
}

.nc-ol-title {
  position: relative;
  margin: 0 auto 34px auto;
  width: max-content;
  font-family: "Share Tech Mono", monospace;
  font-size: 30px;
  line-height: 1;
  font-weight: 400;
  letter-spacing: .16em;
  text-transform: uppercase;
  color: #8ff6ff;
  text-shadow:
    0 0 6px rgba(80,245,255,.72),
    0 0 14px rgba(80,245,255,.28);
}

.nc-ol-title::before,
.nc-ol-title::after {
  content: "";
  position: absolute;
  top: var(--nc-title-bracket-top, -12px);
  width: var(--nc-title-bracket-size, 18px);
  height: var(--nc-title-bracket-size, 18px);
  border-color: rgba(120,245,255,.85);
}

.nc-ol-title::before {
  left: calc(-1 * var(--nc-title-bracket-gap, 40px));
  border-top: var(--nc-title-bracket-stroke, 2px) solid;
  border-left: var(--nc-title-bracket-stroke, 2px) solid;
}

.nc-ol-title::after {
  right: calc(-1 * var(--nc-title-bracket-gap, 40px));
  border-top: var(--nc-title-bracket-stroke, 2px) solid;
  border-right: var(--nc-title-bracket-stroke, 2px) solid;
}

.nc-ol-title-brackets {
  position: absolute;
  top: 116px;
  left: 50%;
  width: 260px;
  height: 1px;
  transform: translateX(-50%);
  background: linear-gradient(90deg, transparent, rgba(80,245,255,.75), transparent);
  box-shadow: 0 0 10px rgba(80,245,255,.8);
}

.nc-ol-grid {
  display: grid;
  grid-template-columns: max-content minmax(0, 1fr) max-content minmax(0, 1fr);
  row-gap: 8px;
  column-gap: 10px;
  padding-top: 26px;
  font-family: "Share Tech Mono", monospace;
  font-size: var(--nc-data-font-size, 12px);
  line-height: 1.05;
  letter-spacing: .05em;
}

.nc-ol-label,
.nc-ol-value {
  padding-bottom: 4px;
  border-bottom: 1px solid rgba(80,220,255,.26);
}

.nc-ol-label {
  font-weight: 400;
  color: #dfffff;
  white-space: nowrap;
}

.nc-ol-value {
  color: #9ffcff;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  text-align: right;
}

/* ── Progress bar ────────────────────────────────────────────── */

.nc-ol-progress {
  position: absolute;
  top: 33px;
  left: 14%;
  width: 31.5%;
  height: 24px;
  z-index: 25;
  pointer-events: none;
  overflow: hidden;
  border-radius: 2px;
  background:
    repeating-linear-gradient(
      90deg,
      rgba(80,220,255,.10)  0px,
      rgba(80,220,255,.10) 13px,
      rgba(0,10,30,.55)    13px,
      rgba(0,10,30,.55)    14px
    );
  box-shadow:
    0 0 4px rgba(80,220,255,.20),
    inset 0 0 4px rgba(0,0,0,.40);
}

.nc-ol-progress__fill {
  position: absolute;
  inset: 0;
  width: 100%;
  background: linear-gradient(
    90deg,
    transparent              0%,
    rgba(80,220,255,.08)    25%,
    rgba(80,240,255,.65)    50%,
    rgba(80,220,255,.08)    75%,
    transparent             100%
  );
  filter: blur(1px);
  animation: hudProgressScan 3s linear infinite;
}

/* ── Aladin Lite layer ───────────────────────────────────────── */

.nc-hud-01-aladdin {
  display: block;
  opacity: 0;
  pointer-events: none;
  transition: opacity 600ms ease;
  position: absolute;
  left: var(--nc-aladin-left, 42px);
  top: var(--nc-aladin-top, 34px);
  width: var(--nc-aladin-width, 445px);
  height: var(--nc-aladin-height, 352px);
  z-index: 2;
  overflow: hidden;
  background: rgba(0, 8, 18, .96);
  border: 1px solid rgba(80,220,255,.28);
  box-shadow: inset 0 0 40px rgba(0,220,255,.06);
}

.nc-hud-01-aladdin > div:first-child {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

/* ── Aladin UI — dark theme overrides ──────────────────────────── */
.nc-hud-01-aladdin .aladin-logo-container,
.nc-hud-01-aladdin .aladin-logo,
.nc-hud-01-aladdin .aladin-location {
  display: none !important;
}

.nc-hud-01-aladdin .aladin-location {
  background:    rgba(0, 8, 20, 0.82) !important;
  border:        1px solid rgba(80, 220, 255, 0.28) !important;
  border-radius: 2px !important;
  backdrop-filter: blur(3px);
}

.nc-hud-01-aladdin .aladin-location .aladin-input-text {
  background:     transparent !important;
  color:          rgba(80, 220, 255, 0.90) !important;
  border:         none !important;
  font-family:    "Share Tech Mono", monospace;
  font-size:      0.65rem;
  letter-spacing: 0.04em;
  padding:        2px 4px;
}

.nc-hud-01-aladdin .aladin-location .aladin-input-text::placeholder {
  color:       rgba(80, 220, 255, 0.32);
  font-family: "Share Tech Mono", monospace;
}

.nc-hud-01-aladdin .aladin-location .aladin-input-text:focus {
  outline: none;
  color:   rgba(120, 240, 255, 1) !important;
  box-shadow: 0 0 6px rgba(80, 220, 255, 0.25);
}

.nc-hud-01-aladdin .aladin-location .aladin-location-copy {
  background:   rgba(0, 8, 20, 0.82) !important;
  border-color: rgba(80, 220, 255, 0.28) !important;
}

.nc-hud-01-aladdin .aladin-location .aladin-location-copy img {
  filter: invert(75%) sepia(60%) saturate(600%) hue-rotate(162deg) brightness(1.2);
}

.nc-hud-01-aladdin__placeholder {
  position: absolute;
  inset: 0;
  z-index: 2;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  pointer-events: none;
}

.nc-hud-01-aladdin--ready .nc-hud-01-aladdin__placeholder {
  display: none;
}

.nc-hud-01-aladdin__label {
  font-family: "Share Tech Mono", monospace;
  font-size: 13px;
  letter-spacing: .20em;
  text-transform: uppercase;
  color: rgba(80,220,255,.60);
  text-shadow: 0 0 10px rgba(80,220,255,.40);
}

.nc-hud-01-aladdin__sub {
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  letter-spacing: .15em;
  text-transform: uppercase;
  color: rgba(80,220,255,.30);
}

/* ── Toolbar ─────────────────────────────────────────────────── */

.nc-ol-toolbar {
  position: absolute;
  bottom: 16px;
  right: 72px;
  z-index: 35;
  display: flex;
  align-items: center;
  gap: 4px;
  pointer-events: all;
}

.nc-ol-tb-group {
  display: flex;
  gap: 6px;
}

.nc-ol-tb-sep {
  width: 1px;
  align-self: stretch;
  background: rgba(80, 220, 255, 0.18);
  margin: 0 6px;
}

.nc-ol-tb-btn {
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: rgba(150,245,255,.75);
  background: rgba(0,12,26,.80);
  border: 1px solid rgba(80,220,255,.22);
  padding: 4px 10px;
  cursor: pointer;
  clip-path: polygon(
    6px 0%, calc(100% - 6px) 0%,
    100% 6px, 100% calc(100% - 6px),
    calc(100% - 6px) 100%, 6px 100%,
    0% calc(100% - 6px), 0% 6px
  );
  transition:
    border-color .18s ease,
    color        .18s ease,
    box-shadow   .18s ease;
}

.nc-ol-tb-btn:hover {
  border-color: rgba(80,220,255,.55);
  color: rgba(150,245,255,.92);
}

.nc-ol-tb-btn[aria-pressed="true"] {
  color: rgba(150,245,255,.95);
  border-color: rgba(80,220,255,.75);
  box-shadow: 0 0 8px rgba(80,220,255,.35);
}

/* ── Data-attribute CSS rules ────────────────────────────────── */

.nc-ol-widget[data-target-lock="off"] .nc-ol-reticle,
.nc-ol-widget[data-reticle="off"] .nc-ol-reticle {
  opacity: 0;
  pointer-events: none;
}

.nc-ol-widget[data-aladdin="on"] .nc-hud-01-aladdin {
  opacity: 1;
  pointer-events: all;
}

/* ── Info panel visibility ───────────────────────────────────── */

.nc-ol-info-panel {
  display: none !important;
}

.nc-ol-widget[data-info-mode="data"] .nc-ol-info-panel--data {
  display: grid !important;
}

.nc-ol-widget[data-info-mode="catalog"] .nc-ol-info-panel--catalog {
  display: flex !important;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 160px;
}

.nc-ol-widget[data-info-mode="catalog"] .nc-ol-papers-list {
  display: block !important;
  overflow-y: auto;
  max-height: 270px;
  min-height: 0;
  padding: 4px 0;
  scrollbar-width: thin;
  scrollbar-color: rgba(80, 220, 255, 0.30) transparent;
}

.nc-ol-widget[data-info-mode="papers"] .nc-ol-info-panel--papers {
  display: flex !important;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 160px;
}

.nc-ol-widget[data-info-mode="papers"] .nc-hud-papers-list {
  display: block !important;
  overflow-y: auto;
  max-height: 270px;
  min-height: 0;
  padding: 4px 0;
  scrollbar-width: thin;
  scrollbar-color: rgba(80, 220, 255, 0.30) transparent;
}

.nc-ol-info-panel--catalog::-webkit-scrollbar,
.nc-ol-info-panel--papers::-webkit-scrollbar  { width: 3px; }
.nc-ol-info-panel--catalog::-webkit-scrollbar-track,
.nc-ol-info-panel--papers::-webkit-scrollbar-track  { background: transparent; }
.nc-ol-info-panel--catalog::-webkit-scrollbar-thumb,
.nc-ol-info-panel--papers::-webkit-scrollbar-thumb  { background: rgba(80, 220, 255, 0.25); border-radius: 2px; }

.nc-ol-info-placeholder {
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .20em;
  text-transform: uppercase;
  color: rgba(80, 220, 255, 0.55);
  text-align: center;
  line-height: 1.8;
  text-shadow: 0 0 10px rgba(80, 220, 255, 0.25);
}

/* ── DATA panel status / spinner / note ──────────────────────── */

.nc-ol-data-status {
  grid-column: 1 / -1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 20px 0 14px;
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .14em;
  text-transform: uppercase;
  text-align: center;
}

.nc-ol-data-status--loading { color: rgba(80, 220, 255, 0.50); }
.nc-ol-data-status--error   { color: rgba(255, 100, 100, 0.60); }

.nc-ol-spinner {
  width: 22px;
  height: 22px;
  border: 2px solid rgba(80, 220, 255, 0.14);
  border-top-color: rgba(80, 220, 255, 0.75);
  border-radius: 50%;
  animation: hudSpinner 0.85s linear infinite;
  flex-shrink: 0;
}

.nc-ol-data-note {
  grid-column: 1 / -1;
  font-family: "Share Tech Mono", monospace;
  font-size: 9px;
  letter-spacing: .12em;
  text-align: center;
  color: rgba(80, 220, 255, 0.22);
  padding-top: 8px;
  border-top: 1px solid rgba(80, 220, 255, 0.10);
  margin-top: 4px;
}

/* ── Catalog / paper cards ───────────────────────────────────── */

.nc-ol-paper-card {
  padding: 8px 10px;
  border-bottom: 1px solid rgba(80, 220, 255, 0.14);
}

.nc-ol-paper-card:last-child { border-bottom: none; }

.nc-ol-paper-title {
  display: block;
  font-family: "Share Tech Mono", monospace;
  font-size: 13px;
  line-height: 1.45;
  letter-spacing: 0.03em;
  color: rgba(130, 245, 255, 0.90);
  text-decoration: none;
  text-shadow: 0 0 6px rgba(80, 220, 255, 0.30);
  margin-bottom: 4px;
}

.nc-ol-paper-title:hover {
  color: rgba(160, 255, 255, 1);
  text-decoration: underline;
  text-decoration-color: rgba(80, 220, 255, 0.40);
  text-underline-offset: 2px;
}

.nc-ol-paper-meta {
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: rgba(80, 200, 255, 0.50);
  margin-bottom: 4px;
}

.nc-ol-paper-abstract {
  font-family: "Share Tech Mono", monospace;
  font-size: 12px;
  line-height: 1.55;
  color: rgba(160, 240, 255, 0.45);
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* .nc-hud-panel-header, .nc-hud-paper-* → blogger-hud-shared.css */

/* ── HTML slot (html mode) ───────────────────────────────────── */

.nc-hud-01-html-slot {
  display: none;
  position: absolute;
  inset: 62px 30px 20px 30px;
  z-index: 10;
  overflow-y: auto;
  overflow-x: hidden;
  padding: 16px 52px 16px 52px;
  scrollbar-width: thin;
  scrollbar-color: rgba(80, 220, 255, 0.30) transparent;
}

.nc-hud-01[data-mode="html"] .nc-hud-01-html-slot { display: block; }

/* HTML mode: hide object-specific layers */
.nc-hud-01[data-mode="html"] .nc-ol-left,
.nc-hud-01[data-mode="html"] .nc-ol-right,
.nc-hud-01[data-mode="html"] .nc-ol-toolbar { display: none !important; }

/* HTML mode: auto-height panel (spec §10 — height adapts to content) */
.nc-hud-01[data-mode="html"] .nc-ol-panel {
  display: block;
  height: auto;
  min-height: 420px;
  overflow: hidden;
}

/* HTML mode maxi: remove widget max-height so long content isn't clipped */
.nc-hud-01[data-mode="html"].nc-ol-widget:not(.is-mini) {
  max-height: none;
  overflow: visible;
}

/* HTML mode: push title below inner SVG frame line (y≈62 in viewBox coords) */
.nc-hud-01[data-mode="html"] .nc-ol-title {
  top: 75px;
}

.nc-hud-01[data-mode="html"] .nc-hud-01-html-slot {
  position: static;
  inset: unset;
  overflow-x: hidden;
  overflow-y: visible;
  /* --nc-inner-left/right: see blogger-hud02-template.css for the full
     explanation — real px distance to the inner-frame SVG line, written
     by ncFrameAdapt (hud01-frame-adapt.js) on every resize. 52px is the
     pre-JS/no-JS fallback. +14px: breathing room so content doesn't land
     flush on the line — see blogger-hud02-template.css for why. */
  padding: 70px calc(var(--nc-inner-right, 52px) + 14px) var(--nc-slot-padding-bottom, 90px) calc(var(--nc-inner-left, 52px) + 14px);
}

/* Clear floats after Blogger / pasted HTML */
.nc-hud-01-html-slot::after,
.nc-hud-02-html-slot::after {
  content: "";
  display: table;
  clear: both;
}

.nc-hud-01-html-slot::-webkit-scrollbar       { width: 4px; }
.nc-hud-01-html-slot::-webkit-scrollbar-track  { background: transparent; }
.nc-hud-01-html-slot::-webkit-scrollbar-thumb  { background: rgba(80, 220, 255, 0.28); border-radius: 2px; }

/* HTML slot typography */
.nc-hud-01-html-slot h1,
.nc-hud-01-html-slot h2,
.nc-hud-01-html-slot h3,
.nc-hud-01-html-slot h4 {
  font-family: "Share Tech Mono", monospace;
  font-weight: 400;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: rgba(130, 245, 255, 0.92);
  text-shadow: 0 0 8px rgba(80, 220, 255, 0.40);
  margin: 0 0 12px;
  line-height: 1.25;
}

.nc-hud-01-html-slot h1 { font-size: 22px; }
.nc-hud-01-html-slot h2 { font-size: 17px; }
.nc-hud-01-html-slot h3 { font-size: 13px; letter-spacing: .16em; }
.nc-hud-01-html-slot h4 { font-size: 11px; letter-spacing: .18em; color: rgba(80, 220, 255, 0.70); }

.nc-hud-01-html-slot p {
  font-family: "Share Tech Mono", monospace;
  font-size: 12px;
  line-height: 1.65;
  color: rgba(190, 250, 255, 0.78);
  margin: 0 0 10px;
}

.nc-hud-01-html-slot a {
  color: rgba(80, 220, 255, 0.90);
  text-decoration: underline;
  text-decoration-color: rgba(80, 220, 255, 0.35);
  text-underline-offset: 3px;
}
.nc-hud-01-html-slot a:hover {
  color: rgba(140, 245, 255, 1);
  text-decoration-color: rgba(140, 245, 255, 0.60);
}

.nc-hud-01-html-slot ul,
.nc-hud-01-html-slot ol {
  font-family: "Share Tech Mono", monospace;
  font-size: 12px;
  color: rgba(190, 250, 255, 0.78);
  margin: 0 0 10px;
  padding-left: 1.4em;
  line-height: 1.6;
}

/* Editorial images (Blogger paste) — not inside .nc-hud-media */
.nc-hud-01-html-slot img:not(.nc-hud-media img) {
  max-width: 100%;
  width: auto !important;
  height: auto !important;
  display: block;
  border: 1px solid rgba(80, 220, 255, 0.22);
  margin-bottom: 10px;
  object-fit: contain;
}

/* Maxi: cap floated Blogger thumbnails (.separator, float:left) */
.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot img:not(.nc-hud-media img),
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot img:not(.nc-hud-media img) {
  max-height: var(--nc-image-maxi-height, 280px);
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .separator,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .separator {
  float: left;
  max-width: var(--nc-image-maxi-width, 42%);
  margin: 0 16px 12px 0;
  box-sizing: border-box;
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .separator a,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .separator a {
  float: none !important;
  display: block;
  max-width: 100%;
  margin: 0 !important;
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .separator img,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .separator img {
  max-width: 100%;
  max-height: var(--nc-image-maxi-height, 280px);
  width: auto !important;
  height: auto !important;
  margin-bottom: 0;
}

.nc-hud-01-html-slot iframe,
.nc-hud-01-html-slot video {
  max-width: 100%;
  height: auto;
  display: block;
  border: 1px solid rgba(80, 220, 255, 0.18);
  margin-bottom: 10px;
}

/* Raw YouTube embed (пастят напрямую из Share → Embed): responsive 16:9 */
.nc-hud-01-html-slot iframe[src*="youtube.com/embed"],
.nc-hud-01-html-slot iframe[src*="youtube-nocookie.com/embed"],
.nc-hud-02-html-slot iframe[src*="youtube.com/embed"],
.nc-hud-02-html-slot iframe[src*="youtube-nocookie.com/embed"] {
  width: 100%;
  height: auto;
  aspect-ratio: 16 / 9;
  border: none;
  display: block;
}

.nc-hud-01-html-slot table {
  width: 100%;
  border-collapse: collapse;
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  color: rgba(190, 250, 255, 0.78);
  margin-bottom: 10px;
}

.nc-hud-01-html-slot th,
.nc-hud-01-html-slot td {
  padding: 4px 8px;
  border-bottom: 1px solid rgba(80, 220, 255, 0.18);
  text-align: left;
}

.nc-hud-01-html-slot th {
  color: rgba(130, 245, 255, 0.90);
  letter-spacing: .08em;
  text-transform: uppercase;
}

/* .nc-hud-media, .nc-yt-facade, .nc-tt-facade, etc. → blogger-hud-shared.css */

/* Video position — inline vars on widget or .nc-hud-media (см. cheat-sheet)
   --nc-video-{maxi|mini|micro}-{top|left|width|height}, fallback --nc-video-{top|left|width|height} */
.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .nc-hud-media,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .nc-hud-media {
  top: var(--nc-video-maxi-top, var(--nc-video-top, 0px));
  left: var(--nc-video-maxi-left, var(--nc-video-left, 0px));
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot .nc-hud-media,
.nc-or-widget.is-mini .nc-hud-02-html-slot .nc-hud-media {
  top: var(--nc-video-mini-top, var(--nc-video-top, 0px));
  left: var(--nc-video-mini-left, var(--nc-video-left, 0px));
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot .nc-hud-media,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot .nc-hud-media {
  top: var(--nc-video-micro-top, var(--nc-video-top, 0px));
  left: var(--nc-video-micro-left, var(--nc-video-left, 0px));
}

/* Opt-in modifier (add alongside .nc-hud-media in markup): true corner anchor.
   Maxi-only — .nc-ol-panel/.nc-or-panel is the nearest position:relative
   ancestor while the html-slot stays position:static in maxi, so absolute
   here is measured from the panel's own top-left corner instead of the
   slot's padded/title-offset flow position. Scoped to :not(.is-mini) so it
   never fights the mini/micro !important position overrides below, and
   opt-in via an extra class so existing --nc-video-maxi-top/left nudges on
   other frames keep their current relative-offset behavior unchanged. */
.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .nc-hud-media.nc-hud-media-corner-anchor,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .nc-hud-media.nc-hud-media-corner-anchor {
  position: absolute;
  top: var(--nc-video-maxi-top, var(--nc-video-top, 0px));
  left: var(--nc-video-maxi-left, var(--nc-video-left, 0px));
}

/* Video (YouTube / HeyGen): description + title only in maxi; mini/micro = media thumbnail */
.nc-hud-only-maxi { }

.nc-ol-widget.is-mini .nc-hud-01-html-slot .nc-hud-only-maxi,
.nc-or-widget.is-mini .nc-hud-02-html-slot .nc-hud-only-maxi {
  display: none !important;
}

/* YouTube maxi: video full width, copy below; mini/micro = video thumbnail only */
.nc-hud-youtube-maxi {
  display: flex;
  flex-direction: column;
  align-items: stretch;
  gap: 12px;
  width: 100%;
  margin-bottom: 14px;
}
.nc-hud-youtube-maxi > .nc-hud-media.nc-hud-media-horizontal {
  flex: 0 0 auto;
  width: var(--nc-video-maxi-width, var(--nc-video-width, 100%));
  height: var(--nc-video-maxi-height, var(--nc-video-height, auto));
  max-width: 100%;
  margin-bottom: 0;
}
.nc-hud-youtube-copy {
  width: 100%;
  min-width: 0;
}
.nc-hud-youtube-copy p {
  margin-bottom: 10px;
}
.nc-hud-youtube-copy p:last-child {
  margin-bottom: 0;
}

/* Vertical video (9:16): HeyGen, YouTube Shorts, TikTok — maxi/mini: video|copy; micro: video only */
.nc-hud-01.nc-ol-widget:has(.nc-hud-vertical-maxi),
.nc-hud-01.nc-ol-widget:has(.nc-hud-heygen-maxi) {
  --nc-vertical-video-h: var(--nc-video-maxi-height, var(--nc-video-height, var(--nc-heygen-video-h, 300px)));
  /* See blogger-hud02-template.css for why the "+20px" was dropped: it
     made the video-taller case end up with a bigger bottom gap than the
     text-taller case. Row height now exactly matches whichever of
     video/text is tallest. */
  --nc-vertical-row-h: var(--nc-vertical-video-h, 300px);
}

.nc-hud-01[data-mode="html"].nc-ol-widget:not(.is-mini) .nc-ol-panel:has(.nc-hud-vertical-maxi),
.nc-hud-01[data-mode="html"].nc-ol-widget:not(.is-mini) .nc-ol-panel:has(.nc-hud-heygen-maxi) {
  height: auto;
  min-height: calc(58px + var(--nc-vertical-row-h, 320px) + 48px);
  max-height: none;
}


.nc-hud-01[data-mode="html"] .nc-hud-01-html-slot:has(.nc-hud-vertical-maxi),
.nc-hud-01[data-mode="html"] .nc-hud-01-html-slot:has(.nc-hud-heygen-maxi) {
  align-items: flex-start;
  text-align: left;
}

.nc-hud-01[data-mode="html"].nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot:has(.nc-hud-vertical-maxi),
.nc-hud-01[data-mode="html"].nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot:has(.nc-hud-heygen-maxi) {
  overflow: visible;
  /* ncFrameAdapt's updateOl()/olPaths() draws the inner frame line at
     H-26 (26px above panel.offsetHeight) — see blogger-hud02-template.css
     for the full explanation of this margin AND why real clearance is
     (padding-bottom - 26), not padding-bottom itself. 48 = 26 + ~22px,
     matching HUD-02's ~20-26px inner-line gap target. */
  padding-bottom: 48px;
}

.nc-hud-vertical-maxi,
.nc-hud-heygen-maxi {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  gap: 16px;
  width: 100%;
  max-width: 100%;
  height: auto;
  min-height: var(--nc-vertical-row-h, 320px);
  margin-bottom: 0;
  box-sizing: border-box;
}

/* min-width: 0 — .nc-hud-media-vertical is a shared "public API" class
   name also used (unscoped) by HUD-03's own CSS, which sets
   min-width:220px for ITS OWN inline-media-in-prose use case. That rule
   has no ancestor scoping, so it silently applies here too whenever
   HUD-03's stylesheet is also loaded on the page (normal — all panel
   types ship in one combined <head> snippet) — nothing above overrides
   min-width, so it wins by default regardless of specificity, clamping
   a computed ~169px-wide vertical video up to 220px and breaking the
   9:16 look. */
.nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-hud-heygen-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-tiktok {
  flex: 0 0 auto;
  align-self: flex-start;
  width: var(--nc-video-maxi-width, var(--nc-video-width, calc(var(--nc-vertical-video-h, 300px) * 9 / 16)));
  min-width: 0;
  height: var(--nc-video-maxi-height, var(--nc-video-height, var(--nc-vertical-video-h, 300px)));
  max-height: var(--nc-video-maxi-height, var(--nc-video-height, var(--nc-vertical-video-h, 300px)));
  max-width: none;
  aspect-ratio: 9 / 16;
  margin: 0;
  position: relative;
  overflow: hidden;
}

/* TikTok direct iframe /embed/v2/ — вписать в 9:16 бокс (как YouTube) */
.nc-hud-media.nc-hud-media-tiktok > iframe {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: 100% !important;
  min-width: 0 !important;
  max-width: 100% !important;
  min-height: 0 !important;
  max-height: 100% !important;
  border: none !important;
  display: block !important;
}

/* Fallback: blockquote + embed.js (если вставлен вручную без facade) */
.nc-hud-media.nc-hud-media-tiktok .tiktok-embed,
.nc-hud-media.nc-hud-media-tiktok .tiktok-embed > div,
.nc-hud-media.nc-hud-media-tiktok .tiktok-embed iframe {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: 100% !important;
  min-width: 0 !important;
  max-width: 100% !important;
  min-height: 0 !important;
  max-height: 100% !important;
  margin: 0 !important;
  padding: 0 !important;
  transform: none !important;
  box-sizing: border-box !important;
  border: none !important;
}

/* TikTok: ширино-управляемый размер.
   --nc-tt-w задаётся на ряде, наследуется медиа-элементом.
   min-height ряда = высота видео + 20px gap (динамически). */
.nc-hud-vertical-maxi:has(.nc-hud-media-tiktok),
.nc-hud-heygen-maxi:has(.nc-hud-media-tiktok) {
  --nc-tt-w: max(200px, var(--nc-video-maxi-width, var(--nc-video-width, 250px)));
  align-items: stretch;
  height: auto;
  min-height: calc(var(--nc-tt-w) * 16 / 9 + 20px);
}

.nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-tiktok {
  width:      var(--nc-tt-w) !important;
  height:     calc(var(--nc-tt-w) * 16 / 9) !important;
  max-height: none !important;
  min-width:  200px;
  min-height: 356px;
}

.nc-hud-vertical-copy,
.nc-hud-heygen-copy {
  display: block;
  flex: 1 1 0;
  min-width: 0;
  text-align: left;
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot .nc-hud-vertical-copy,
.nc-ol-widget.is-mini .nc-hud-heygen-copy {
  display: block !important;
  visibility: visible !important;
  width: auto !important;
  height: auto !important;
  pointer-events: auto !important;
}

.nc-hud-vertical-copy p,
.nc-hud-heygen-copy p {
  margin-bottom: 10px;
}

.nc-hud-vertical-copy p:last-child,
.nc-hud-heygen-copy p:last-child {
  margin-bottom: 0;
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot:has(.nc-hud-media):not(:has(.nc-hud-vertical-maxi)):not(:has(.nc-hud-heygen-maxi)) > h2,
.nc-or-widget.is-mini .nc-hud-02-html-slot:has(.nc-hud-media):not(:has(.nc-hud-vertical-maxi)):not(:has(.nc-hud-heygen-maxi)) > h2 {
  display: none !important;
}

/* Mini slot with non-youtube media (vertical/heygen): keep flex-column layout */
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-01-html-slot:has(.nc-hud-media),
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:has(.nc-hud-media) {
  padding: 58px 24px 18px !important;
  overflow: hidden !important;
  display: flex !important;
  flex-direction: column !important;
  box-sizing: border-box !important;
  height: 420px !important;
  max-height: 420px !important;
}

/* Mini slot with youtube-maxi: switch to block+relative so absolute children work */
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-01-html-slot:has(.nc-hud-youtube-maxi),
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:has(.nc-hud-youtube-maxi) {
  display: block !important;
  position: relative !important;
}

/* Mini youtube-maxi: absolute fill of slot content area */
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-youtube-maxi,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-youtube-maxi {
  position: absolute !important;
  top: 24px !important;
  left: 24px !important;
  right: 24px !important;
  bottom: 24px !important;
  width: auto !important;
  height: auto !important;
  margin: 0 !important;
}

.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-vertical-maxi,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-vertical-maxi,
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-heygen-maxi,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-heygen-maxi,
.nc-ol-widget:not([data-collapse]).is-mini .nc-hud-vertical-maxi,
.nc-ol-widget:not([data-collapse]).is-mini .nc-hud-heygen-maxi {
  flex: 1 1 auto !important;
  display: flex !important;
  flex-direction: row !important;
  align-items: flex-start !important;
  height: 100% !important;
  min-height: 0 !important;
  max-height: none !important;
  width: 100% !important;
  margin: 0 !important;
  gap: 10px !important;
}

.nc-ol-widget.is-mini .nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-ol-widget.is-mini .nc-hud-heygen-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-ol-widget.is-mini .nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-tiktok {
  flex: 0 0 auto !important;
  width: var(--nc-video-mini-width, var(--nc-video-width, auto)) !important;
  min-width: 0 !important;
  max-width: 40% !important;
  height: var(--nc-video-mini-height, var(--nc-video-height, 100%)) !important;
  max-height: var(--nc-video-mini-height, var(--nc-video-height, 100%)) !important;
  aspect-ratio: 9 / 16 !important;
  margin: 0 !important;
  overflow: hidden !important;
}

.nc-ol-widget.is-mini .nc-hud-vertical-maxi:has(.nc-hud-media-tiktok) {
  align-items: stretch !important;
}

.nc-ol-widget.is-mini .nc-hud-vertical-copy,
.nc-ol-widget.is-mini .nc-hud-heygen-copy {
  flex: 1 1 0 !important;
  max-height: 100% !important;
  overflow: hidden !important;
}

.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-01-html-slot:has(.nc-hud-media) > .nc-hud-media,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:has(.nc-hud-media) > .nc-hud-media,
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-youtube-maxi > .nc-hud-media,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-youtube-maxi > .nc-hud-media {
  /* absolute fill: parent (youtube-maxi) is already position:absolute with explicit bounds */
  position: absolute !important;
  inset: 0 !important;
  width: auto !important;
  height: auto !important;
  margin: 0 !important;
  aspect-ratio: unset !important;
}

.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-media,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-media {
  position: relative !important;
}

.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-media iframe,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-media iframe {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: 100% !important;
}

/* Image position — vars on widget: --nc-image-{maxi|mini|micro|}-{top|left|width|height} */
.nc-hud-float-image,
.nc-hud-slot-image {
  position: relative;
  display: block;
  border: 1px solid rgba(80,220,255,.22);
  object-fit: contain;
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .nc-hud-float-image,
.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .nc-hud-slot-image,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .nc-hud-float-image,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .nc-hud-slot-image {
  float: var(--nc-image-maxi-float, left) !important;
  top: var(--nc-image-maxi-top, var(--nc-image-top, 0px));
  left: var(--nc-image-maxi-left, var(--nc-image-left, auto));
  width: var(--nc-image-maxi-width, var(--nc-image-width, 28%)) !important;
  height: var(--nc-image-maxi-height, var(--nc-image-height, auto)) !important;
  max-width: none !important;
  max-height: none !important;
  margin: 0 34px 22px 0;
}

/* Compatibility for snippets generated while `auto` was the maxi default. */
:is(.nc-ol-widget, .nc-or-widget):not(.is-mini):is(
  [style*="--nc-image-maxi-width: auto"],
  [style*="--nc-image-maxi-width:auto"]
) :is(.nc-hud-float-image, .nc-hud-slot-image) {
  width: 28% !important;
}

/* `center` is a HUD API value, not a native CSS float value. */
:is(.nc-ol-widget, .nc-or-widget):not(.is-mini):is(
  [style*="--nc-image-maxi-float: center"],
  [style*="--nc-image-maxi-float:center"]
) :is(.nc-hud-float-image, .nc-hud-slot-image) {
  float: none !important;
  margin-left: auto !important;
  margin-right: auto !important;
}

.nc-ol-widget.is-mini:not([data-collapse="micro"]) .nc-hud-01-html-slot .nc-hud-float-image,
.nc-ol-widget.is-mini:not([data-collapse="micro"]) .nc-hud-01-html-slot .nc-hud-slot-image,
.nc-or-widget.is-mini:not([data-collapse="micro"]) .nc-hud-02-html-slot .nc-hud-float-image,
.nc-or-widget.is-mini:not([data-collapse="micro"]) .nc-hud-02-html-slot .nc-hud-slot-image {
  float: none;
  position: absolute;
  top: var(--nc-image-mini-top, var(--nc-image-top, 0px)) !important;
  left: var(--nc-image-mini-left, var(--nc-image-left, 0px)) !important;
  width: var(--nc-image-mini-width, auto) !important;
  max-width: none !important;
  height: var(--nc-image-mini-height, 100%) !important;
  max-height: none !important;
  margin: 0;
  object-fit: contain;
  object-position: left top;
}

/* HTML mini thumbnail: fit inside 420×0.352 ≈ 148px frame (panelH set in JS) */
.nc-hud-01[data-mode="html"].nc-ol-widget.is-mini:not([data-collapse="micro"]) .nc-ol-panel,
.nc-hud-02[data-mode="html"].nc-or-widget.is-mini:not([data-collapse="micro"]) .nc-or-panel {
  height: 420px !important;
  max-height: 420px !important;
  min-height: 420px !important;
}

.nc-hud-01[data-mode="html"].nc-ol-widget.is-mini:not([data-collapse="micro"]) .nc-hud-01-html-slot:not(:has(.nc-hud-media)),
.nc-hud-02[data-mode="html"].nc-or-widget.is-mini:not([data-collapse="micro"]) .nc-hud-02-html-slot:not(:has(.nc-hud-media)) {
  position: absolute;
  inset: 5px 20px 5px 10px;
  padding: 0 !important;
  overflow: hidden !important;
  box-sizing: border-box;
  display: block !important;
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot img:not(.nc-hud-media img),
.nc-or-widget.is-mini .nc-hud-02-html-slot img:not(.nc-hud-media img) {
  max-height: var(--nc-image-mini-height, 100%);
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot .separator,
.nc-or-widget.is-mini .nc-hud-02-html-slot .separator {
  float: left;
  max-width: var(--nc-image-mini-width, 40%);
  margin: 0 10px 6px 0;
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot .separator img,
.nc-or-widget.is-mini .nc-hud-02-html-slot .separator img {
  max-height: var(--nc-image-mini-height, 160px);
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot p,
.nc-or-widget.is-mini .nc-hud-02-html-slot p {
  font-size: 11px;
  line-height: 1.45;
  margin-bottom: 6px;
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot h2,
.nc-or-widget.is-mini .nc-hud-02-html-slot h2 {
  font-size: 13px;
  margin-bottom: 6px;
}

/* ── HUD-01 title classes (5 independent variants) ───────────── */
/* All five share an identical base (identical to nc-ol-title, which is
   reserved for object-mode use in .nc-ol-right). These suffixed variants
   are for html-slot content — customize each independently.

   NOTE: nc-ol-title (no suffix) is reserved for object-mode use.
   These five use the -maxi / -mini / -micro / -b / -c suffixes to stay clear.

   Variants:
     nc-ol-title-maxi   — expanded (maxi) panel   [default for html mode]
     nc-ol-title-mini   — mini thumbnail
     nc-ol-title-micro  — micro thumbnail
     nc-ol-title-b      — reserve B
     nc-ol-title-c      — reserve C                              */

.nc-ol-title-maxi,
.nc-ol-title-mini,
.nc-ol-title-micro,
.nc-ol-title-b,
.nc-ol-title-c {
  position: relative;
  /* !important: .nc-hud-01-html-slot h1..h4 (generic fallback typography,
     below) has higher specificity (class+element vs. this plain class) and
     would otherwise win, silently shrinking this margin/line-height on the
     live site regardless of what's declared here. */
  margin: 0 auto 34px auto !important;
  width: max-content;
  font-family: "Share Tech Mono", monospace;
  font-size: var(--nc-title-maxi-size, var(--nc-title-size, 30px)) !important;
  line-height: 1 !important;
  font-weight: 400;
  letter-spacing: .16em;
  text-transform: uppercase;
  color: #8ff6ff;
  text-shadow:
    0 0 6px rgba(80,245,255,.72),
    0 0 14px rgba(80,245,255,.28);
}

.nc-ol-widget.is-mini .nc-ol-title-maxi,
.nc-or-widget.is-mini .nc-or-title-maxi {
  font-size: var(--nc-title-mini-size, var(--nc-title-size, 30px)) !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-title-micro,
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-title-micro {
  font-size: var(--nc-title-micro-size, var(--nc-title-size, 30px)) !important;
}

.nc-ol-title-maxi::before,  .nc-ol-title-maxi::after,
.nc-ol-title-mini::before,  .nc-ol-title-mini::after,
.nc-ol-title-micro::before, .nc-ol-title-micro::after,
.nc-ol-title-b::before,     .nc-ol-title-b::after,
.nc-ol-title-c::before,     .nc-ol-title-c::after {
  content: "";
  position: absolute;
  top: -12px;
  width: 18px;
  height: 18px;
  border-color: rgba(120,245,255,.85);
}

.nc-ol-title-maxi::before,
.nc-ol-title-mini::before,
.nc-ol-title-micro::before,
.nc-ol-title-b::before,
.nc-ol-title-c::before {
  left: -40px;
  border-top: 2px solid;
  border-left: 2px solid;
}

.nc-ol-title-maxi::after,
.nc-ol-title-mini::after,
.nc-ol-title-micro::after,
.nc-ol-title-b::after,
.nc-ol-title-c::after {
  right: -40px;
  border-top: 2px solid;
  border-right: 2px solid;
}

/* ── Per-variant position vars ───────────────────────────────── */
/* Set on WIDGET ROOT via style="". Uses position:relative from base.
   Maxi/micro coords are in screen px.
   Mini coords are in UNSCALED panel space (÷ 0.352 → screen px). */
.nc-ol-title-maxi {
  top:  var(--nc-title-maxi-top,  0px);
  left: var(--nc-title-maxi-left, 0px);
}
.nc-ol-title-mini {
  top:  var(--nc-title-mini-top,  0px);
  left: var(--nc-title-mini-left, 0px);
}
/* When widget is is-mini, apply mini-vars regardless of title class suffix */
.nc-ol-widget.is-mini .nc-ol-title-maxi,
.nc-or-widget.is-mini .nc-or-title-maxi {
  top:  var(--nc-title-mini-top,  var(--nc-title-maxi-top,  0px));
  left: var(--nc-title-mini-left, var(--nc-title-maxi-left, 0px));
}
/* nc-ol-title-micro position: see micro-mini state rule below.
   .nc-ol-title-b { }  .nc-ol-title-c { }                       */

/* ── HUD-01 button classes (5 independent variants) ──────────── */
/* All five share an identical base (mirrors nc-ol-tb-btn style).
   NOTE: nc-ol-tb-btn (no suffix) is reserved for object-mode tab buttons.

   Variants:
     nc-ol-btn-maxi   — expanded (maxi) panel   [default for html mode]
     nc-ol-btn-mini   — mini thumbnail
     nc-ol-btn-micro  — micro thumbnail
     nc-ol-btn-b      — reserve B
     nc-ol-btn-c      — reserve C                               */

.nc-ol-btn-maxi,
.nc-ol-btn-mini,
.nc-ol-btn-micro,
.nc-ol-btn-b,
.nc-ol-btn-c {
  display: inline-block;
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  line-height: 1;
  letter-spacing: .12em;
  text-transform: uppercase;
  text-decoration: none;
  color: rgba(150,245,255,.75);
  background: rgba(0,12,26,.80);
  border: 1px solid rgba(80,220,255,.22);
  padding: 6px 14px;
  cursor: pointer;
  clip-path: polygon(
    6px 0%, calc(100% - 6px) 0%,
    100% 6px, 100% calc(100% - 6px),
    calc(100% - 6px) 100%, 6px 100%,
    0% calc(100% - 6px), 0% 6px
  );
  transition:
    border-color .18s ease,
    color        .18s ease,
    box-shadow   .18s ease;
}

.nc-ol-btn-maxi:hover,
.nc-ol-btn-mini:hover,
.nc-ol-btn-micro:hover,
.nc-ol-btn-b:hover,
.nc-ol-btn-c:hover {
  border-color: rgba(80,220,255,.55);
  color: rgba(150,245,255,.92);
  text-decoration: none;
}

.nc-ol-btn-maxi:active,
.nc-ol-btn-mini:active,
.nc-ol-btn-micro:active,
.nc-ol-btn-b:active,
.nc-ol-btn-c:active {
  color: rgba(150,245,255,.95);
  border-color: rgba(80,220,255,.75);
  box-shadow: 0 0 8px rgba(80,220,255,.35);
}

/* ── Per-variant overrides ────────────────────────────────────── */
/* Add properties here to diverge from the shared base.

.nc-ol-btn-maxi   { }
.nc-ol-btn-mini   { }
.nc-ol-btn-micro  { }
.nc-ol-btn-b      { }
.nc-ol-btn-c      { }                                           */

/* ── Mini / Expanded toggle ──────────────────────────────────── */

/* Default (mini) slot — 317×148, scale 0.352 */
.nc-ol-widget.is-mini {
  max-height: 148px;
  max-width: 317px;
  cursor: pointer;
}

/* ── Mini external button (nc-ol-btn-mini) ───────────────────────
   Placement: direct child of widget AFTER the panel, same pattern as nc-ol-btn-micro.
   The panel's layout height is ~420px (scale shrinks it visually, not in flow), so
   the button MUST be position:absolute to escape the flow.
   !important overrides JS widget.style.maxHeight = '148px' and overflow = 'hidden'. */
.nc-ol-widget[data-collapse="mini"].is-mini > .nc-ol-btn-mini,
.nc-ol-widget:not([data-collapse]).is-mini > .nc-ol-btn-mini {
  position: absolute !important;
  top:  var(--nc-btn-mini-top,  152px) !important;  /* thumbnail (148px) + gap (4px) */
  left: var(--nc-btn-mini-left, 0px)   !important;
  z-index: 100 !important;
}
.nc-ol-widget:not(.is-mini) > .nc-ol-btn-mini {
  display: none !important;
}
.nc-ol-widget[data-collapse="mini"].is-mini:has(> .nc-ol-btn-mini),
.nc-ol-widget:not([data-collapse]).is-mini:has(> .nc-ol-btn-mini) {
  max-height: 174px !important;   /* 148px + 4px gap + 22px button */
  overflow: visible !important;
}

/* Micro slot — no scale; custom layout: unscaled h2 label above + portrait image slot.
   JS passes scale=null / panelW=87 / panelH=130 / miniH=149 to NcHudMini by default.
   Widget overflow:visible lets the outer h2 (direct child, before panel) show above. */
.nc-ol-widget[data-collapse="micro"].is-mini {
  max-height: calc(var(--nc-micro-h, 130px) + 19px); /* slot + label (~15px) + gap (4px) */
  max-width: var(--nc-micro-w, 87px);
  padding: 0 !important;
  overflow: visible !important;
}
/* When an external micro button is present — extend height to fit it below the slot */
.nc-ol-widget[data-collapse="micro"].is-mini:has(> .nc-ol-btn-micro) {
  max-height: calc(var(--nc-micro-h, 130px) + 45px); /* +26px btn (22px + 4px gap) */
}

/* Panel in micro mini: portrait slot (not mini 420px html frame) */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-panel,
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-panel {
  min-height: 0 !important;
  height: var(--nc-micro-h, 130px) !important;
  max-height: var(--nc-micro-h, 130px) !important;
  overflow: visible !important; /* allow SVG frame drop-shadow to extend beyond panel bounds */
  margin: 0 !important;
  transform: none !important;
  animation: none !important;
  opacity: 1 !important;
}

/* Scale progress bar to fit the portrait slot.
   In micro the panel aspect ratio flips (portrait vs landscape), so the inner-frame
   top spans ~70% of width — override left + width from the base 14%/31.5% values. */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-progress {
  top:    calc(var(--nc-micro-h, 130px) * 0.079);  /* SVG inner-top y=26/420 */
  left:   14%;                                       /* SVG x=170/1200 ≈ 14% */
  width:  70%;                                       /* inner-top runs to x≈84% of panel */
  height: calc(var(--nc-micro-h, 130px) * 0.070);  /* slightly taller for visibility */
}

/* HTML slot in micro mini: strip padding */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot {
  padding: 0 !important;
  overflow: hidden !important;
}

/* For image content the panel (same origin as the widget) is the containing
   block. The HTML slot must neither offset nor clip the bitmap. */
.nc-ol-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-01-html-slot,
.nc-or-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-02-html-slot {
  position: absolute !important;
  inset: 0 !important;
  overflow: visible !important;
  z-index: auto !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:has(.nc-hud-media) {
  height: 100% !important;
}

/* Micro + video: only .nc-hud-media / wrappers (existing rules below) */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:has(.nc-hud-media) > *:not(.nc-hud-media):not(.nc-hud-youtube-maxi):not(.nc-hud-vertical-maxi):not(.nc-hud-heygen-maxi):not(.nc-ol-btn-micro) {
  display: none !important;
}

/* Micro + image/text (Blogger .separator, float-image): только <img> в слоте */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: var(--nc-micro-h, 130px) !important;
  min-height: var(--nc-micro-h, 130px) !important;
  max-height: var(--nc-micro-h, 130px) !important;
  padding: 0 !important;
  overflow: visible !important;
  z-index: auto !important;
  box-sizing: border-box !important;
}

/* Blogger .separator / <a><img></a>: display:none on wrapper hides nested img */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) .separator,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) .separator,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) a:has(> img),
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) a:has(> img) {
  display: contents !important;
  float: none !important;
  margin: 0 !important;
  padding: 0 !important;
  border: none !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) > *:not(img):not(.nc-ol-btn-micro):not(:has(img)),
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) > *:not(img):not(.nc-or-btn-micro):not(:has(img)),
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) :is(p, h1, h2, h3, h4, h5, h6, ul, ol, li, blockquote, pre, table, .nc-hud-text, .nc-ol-btn-maxi, .nc-or-btn-maxi, br, hr),
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) :is(p, h1, h2, h3, h4, h5, h6, ul, ol, li, blockquote, pre, table, .nc-hud-text, .nc-ol-btn-maxi, .nc-or-btn-maxi, br, hr) {
  display: none !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) img,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) img,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) > img,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) > img,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) iframe,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) iframe,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) object,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) object,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) embed,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) embed {
  display: block !important;
  visibility: visible !important;
  position: absolute !important;
  top: var(--nc-image-micro-top, var(--nc-image-top, 0px)) !important;
  left: var(--nc-image-micro-left, var(--nc-image-left, 0px)) !important;
  right: auto !important;
  bottom: auto !important;
  width: var(--nc-image-micro-width, var(--nc-image-width, 100%)) !important;
  height: var(--nc-image-micro-height, var(--nc-image-height, 100%)) !important;
  max-width: none !important;
  max-height: none !important;
  margin: 0 !important;
  padding: 0 !important;
  float: none !important;
  object-fit: contain !important;
  object-position: center !important;
  border: none !important;
  box-sizing: border-box !important;
  transform: none !important;
  z-index: 0 !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) img ~ img,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) img ~ img {
  display: none !important;
}

/* Authoritative generated-image geometry. Keep this independent of :has():
   if a browser drops/mismatches the content-detection selector above, the
   generic editorial-img rule would otherwise restore width:auto and make the
   bitmap look full-slot despite explicit --nc-image-micro-* values. */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot .nc-hud-float-image,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot .nc-hud-slot-image,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot .nc-hud-float-image,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot .nc-hud-slot-image {
  display: block !important;
  position: absolute !important;
  top: var(--nc-image-micro-top, var(--nc-image-top, 0px)) !important;
  left: var(--nc-image-micro-left, var(--nc-image-left, 0px)) !important;
  right: auto !important;
  bottom: auto !important;
  width: var(--nc-image-micro-width, var(--nc-image-width, 100%)) !important;
  height: var(--nc-image-micro-height, var(--nc-image-height, 100%)) !important;
  max-width: none !important;
  max-height: none !important;
  margin: 0 !important;
  float: none !important;
  object-fit: contain !important;
  z-index: 0 !important;
}

/* Final image-slot override: intentionally after the generic
   :not(:has(.nc-hud-media)) block so left/top use the panel/widget origin. */
.nc-ol-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-01-html-slot,
.nc-or-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-02-html-slot {
  position: absolute !important;
  inset: 0 !important;
  overflow: visible !important;
  z-index: auto !important;
}

/* Video embed fills micro slot (same idea as float image) */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-youtube-maxi {
  width: 100% !important;
  height: 100% !important;
  margin: 0 !important;
  display: block !important;
}

/* Micro: только вертикальное видео по высоте слота (Shorts / TikTok / HeyGen) */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-vertical-copy,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-heygen-copy {
  display: none !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-vertical-maxi,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-heygen-maxi {
  width: 100% !important;
  height: 100% !important;
  min-height: 0 !important;
  max-height: 100% !important;
  margin: 0 !important;
  display: block !important;
  overflow: hidden !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-media {
  float: none !important;
  width: var(--nc-video-micro-width, var(--nc-video-width, 100%)) !important;
  min-width: 0 !important;
  max-width: 100% !important;
  margin: 0 !important;
  max-height: var(--nc-video-micro-height, var(--nc-video-height, var(--nc-micro-h, 130px))) !important;
  aspect-ratio: 16 / 9;
  height: var(--nc-video-micro-height, var(--nc-video-height, auto)) !important;
}

/* Fill the WHOLE micro frame (--nc-micro-w × --nc-micro-h), not a
   letterboxed 9:16 box centered inside it — nc-micro-l's 200×308 panel
   isn't itself 9:16 (0.649 vs 0.5625), so deriving width from height via
   9:16 left a ~27px gap on the right that never reached the frame edge.
   aspect-ratio becomes a no-op once both width and height are explicit
   (it only fills in a *missing* dimension per spec), so it's harmless to
   leave declared — kept for content that only overrides one of the two
   via --nc-video-micro-width/height. The poster (background-size:cover)
   and any object-fit:cover media crop to fill this box; a raw iframe
   (no poster) would stretch instead, since iframes can't crop — that's
   an inherent limitation of unfaceted iframe embeds in micro, not
   something CSS sizing here can fix. */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-heygen-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-tiktok {
  width: var(--nc-video-micro-width, var(--nc-video-width, var(--nc-micro-w, 87px))) !important;
  min-width: 0 !important;
  max-width: 100% !important;
  height: var(--nc-video-micro-height, var(--nc-video-height, var(--nc-micro-h, 130px))) !important;
  max-height: var(--nc-video-micro-height, var(--nc-video-height, var(--nc-micro-h, 130px))) !important;
  aspect-ratio: 9 / 16 !important;
  margin: 0 !important;
  align-self: flex-start !important;
  position: relative !important;
  overflow: hidden !important;
}
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-media iframe {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: 100% !important;
}

/* Image-mode blocks may contain a remote iframe/object instead of <img>.
   Apply image geometry to its outer media box; the iframe fills that box. */
.nc-ol-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-media,
.nc-ol-widget[data-collapse="micro"][style*="--nc-image-micro-width"].is-mini .nc-hud-media {
  position: absolute !important;
  top: var(--nc-image-micro-top, var(--nc-image-top, 0px)) !important;
  left: var(--nc-image-micro-left, var(--nc-image-left, 0px)) !important;
  right: auto !important;
  bottom: auto !important;
  width: var(--nc-image-micro-width, var(--nc-image-width, 100%)) !important;
  height: var(--nc-image-micro-height, var(--nc-image-height, 100%)) !important;
  max-width: none !important;
  max-height: none !important;
  transform: none !important;
  overflow: hidden !important;
  z-index: 0 !important;
}

.nc-ol-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-media :is(iframe, object, embed),
.nc-ol-widget[data-collapse="micro"][style*="--nc-image-micro-width"].is-mini .nc-hud-media :is(iframe, object, embed) {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  min-width: 0 !important;
  max-width: 100% !important;
  height: 100% !important;
  min-height: 0 !important;
  max-height: 100% !important;
  transform: none !important;
  box-sizing: border-box !important;
}

/* (float-image / slot-image — см. правила img в :not(:has(.nc-hud-media)) выше) */

/* Outer h2 (direct child of widget, before panel):
   hidden in expanded, shown as label above the slot in micro mini */
.nc-ol-widget[data-collapse="micro"] > .nc-ol-title-micro {
  display: none !important;
}
.nc-ol-widget[data-collapse="micro"].is-mini > .nc-ol-title-micro {
  display: block !important;
  font-size: var(--nc-title-micro-size, 11px) !important;
  letter-spacing: .10em;
  text-align: center;
  margin: 0 0 4px;
  white-space: nowrap;
  overflow: visible;
  text-overflow: ellipsis;
  max-width: var(--nc-micro-w, 87px);
  width: auto;
  top: 0 !important;
  left: 0 !important;
  transform: translate(var(--nc-title-micro-left, 0px), var(--nc-title-micro-top, 0px)) !important;
}
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot .nc-ol-title-maxi {
  display: none !important;
}
.nc-ol-widget[data-collapse="micro"].is-mini > .nc-ol-title-micro::before,
.nc-ol-widget[data-collapse="micro"].is-mini > .nc-ol-title-micro::after {
  display: none !important;
}

/* In collapsed micro only the direct-child micro label is allowed.
   Hide object/maxi/legacy title decorations inside the panel. */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-panel :is(
  .nc-ol-title, .nc-ol-title-maxi, .nc-ol-title-mini,
  .nc-ol-title-micro, .nc-ol-title-b, .nc-ol-title-c,
  .nc-ol-title-brackets
) {
  display: none !important;
}
.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-panel :is(
  .nc-ol-title, .nc-ol-title-maxi, .nc-ol-title-mini,
  .nc-ol-title-micro, .nc-ol-title-b, .nc-ol-title-c
)::before,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-panel :is(
  .nc-ol-title, .nc-ol-title-maxi, .nc-ol-title-mini,
  .nc-ol-title-micro, .nc-ol-title-b, .nc-ol-title-c
)::after {
  display: none !important;
  content: none !important;
}

/* Toggle button in micro mini: scaled down, coords in screen px.
   top/left override prevents clipping by panel overflow:hidden.
   Per-panel vars: --nc-toggle-micro-top/left/scale (→ --nc-toggle-top/left → --nc-btn-* fallback). */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-panel .nc-ol-toggle-btn {
  top:       var(--nc-toggle-micro-top,   var(--nc-toggle-top,  var(--nc-btn-top,  2px)));
  left:      var(--nc-toggle-micro-left,  var(--nc-toggle-left, var(--nc-btn-left, 2px)));
  transform: scale(var(--nc-toggle-micro-scale, var(--nc-btn-scale, 0.35)));
  transform-origin: top left;
}

/* Micro button: absolute within widget/panel — descendant selector handles any placement */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-btn-micro {
  position: absolute !important;
  top:  var(--nc-btn-micro-top,  19px) !important;  /* label (~15px) + gap (4px) */
  left: var(--nc-btn-micro-left, 0px)  !important;
  z-index: 9999 !important;
}
/* Hidden in expanded state (btn-maxi inside slot handles that) */
.nc-ol-widget[data-collapse="micro"]:not(.is-mini) .nc-ol-btn-micro {
  display: none !important;
}

/* ── Micro size modifier classes ──────────────────────────────── */
/* Add one of these classes to the widget root alongside data-collapse="micro"
   to change the slot dimensions.  Omit the class for the 106×49 default.

   Usage:  <div class="nc-hud-01 nc-ol-widget nc-micro-l" data-collapse="micro" …>

   Sizes (w × h of the image slot; total widget height = h + 19px):
     nc-micro-s    —  87 × 130 px
     (default)     —  87 × 130 px
     nc-micro-l    — 200 × 308 px                                 */

.nc-micro-s { --nc-micro-w: 87px; --nc-micro-h: 130px; }
.nc-micro-l { --nc-micro-w: 200px; --nc-micro-h: 308px; }

/* .nc-ol-widget.is-mini .nc-ol-panel constraints (transform, transformOrigin,
   width, height, margin, overflow) are applied as inline styles by hud-mini.js.
   No CSS rule needed here — removing it avoids duplication and specificity fights. */

/* Toggle button — scoped to HUD-01 only.
   Per-mode CSS vars (all optional, set on widget root):
     --nc-toggle-maxi-top/left      expanded state only
     --nc-toggle-mini-top/left/scale  mini state only (coords in UNSCALED panel space, ÷ 0.352)
     --nc-toggle-micro-top/left/scale micro state only (coords in screen px)
     --nc-toggle-top/left           shared fallback across all modes
   Old vars --nc-btn-top/left/scale remain as ultimate fallback (backward compat). */
.nc-ol-panel .nc-ol-toggle-btn {
  position: absolute;
  top:  var(--nc-toggle-maxi-top,  var(--nc-toggle-top,  var(--nc-btn-top,  -8px)));
  left: var(--nc-toggle-maxi-left, var(--nc-toggle-left, var(--nc-btn-left, -5px)));
  right: auto;
  z-index: 36;
  font-family: "Share Tech Mono", monospace;
  font-size: 38px;
  line-height: 1;
  color: rgba(80, 220, 255, .98);
  background: transparent;
  border: 1px solid transparent;
  padding: 12px 22px;
  cursor: pointer;
  clip-path: polygon(
    5px 0%, calc(100% - 5px) 0%,
    100% 5px, 100% calc(100% - 5px),
    calc(100% - 5px) 100%, 5px 100%,
    0% calc(100% - 5px), 0% 5px
  );
  box-shadow: none;
  transition: border-color .18s ease, box-shadow .18s ease,
              color .18s ease, background .18s ease;
  pointer-events: all;
}

.nc-ol-panel .nc-ol-toggle-btn:hover {
  color: rgba(150, 245, 255, .98);
  background: transparent;
  border-color: rgba(80, 220, 255, .75);
  box-shadow: none;
}

/* Mini mode: button scales with the 0.352× panel.
   Coords in UNSCALED panel space (÷ 0.352 to convert to screen px).
   Per-panel vars: --nc-toggle-mini-top/left/scale (→ --nc-toggle-* → --nc-btn-* fallback). */
.nc-ol-widget.is-mini .nc-ol-panel .nc-ol-toggle-btn {
  top:       var(--nc-toggle-mini-top,   var(--nc-toggle-top,  var(--nc-btn-top,  -8px)));
  left:      var(--nc-toggle-mini-left,  var(--nc-toggle-left, var(--nc-btn-left, -5px)));
  transform: scale(var(--nc-toggle-mini-scale, var(--nc-btn-scale, 1)));
  transform-origin: top left;
}

/* Micro mode: duplicate of the rule in the micro layout section above — kept here
   for cascade legibility. Specificity wins over the base rule and mini rule. */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-panel .nc-ol-toggle-btn {
  top:       var(--nc-toggle-micro-top,   var(--nc-toggle-top,  var(--nc-btn-top,  2px)));
  left:      var(--nc-toggle-micro-left,  var(--nc-toggle-left, var(--nc-btn-left, 2px)));
  transform: scale(var(--nc-toggle-micro-scale, var(--nc-btn-scale, 0.35)));
  transform-origin: top left;
}

/* Tooltip */
.nc-ol-panel .nc-ol-toggle-btn::after {
  content: attr(data-tooltip);
  position: absolute;
  top: calc(100% + 7px);
  left: 0;
  display: none;
  white-space: nowrap;
  font-family: "Share Tech Mono", monospace;
  font-size: 9px;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: rgba(80, 220, 255, .88);
  background: rgba(0, 6, 16, .96);
  border: 1px solid rgba(80, 220, 255, .32);
  padding: 4px 10px;
  pointer-events: none;
  z-index: 50;
  box-shadow: 0 0 10px rgba(80, 220, 255, .18);
}

.nc-ol-panel .nc-ol-toggle-btn:hover::after { display: block; }

/* ── Contour sweep runner ─────────────────────────────────────── */

.nc-hud-01 .nc-ol-sweep { display: none; }

.nc-ol-runner-tail {
  fill: none;
  stroke: rgba(60, 190, 255, .13);
  stroke-width: 72;
  vector-effect: non-scaling-stroke;
  stroke-linecap: round;
  stroke-dasharray: 120 2956;
  filter: blur(32px);
  animation: ncOlSweepBar 22s linear infinite;
}

.nc-ol-runner-body {
  fill: none;
  stroke: rgba(90, 210, 255, .26);
  stroke-width: 32;
  vector-effect: non-scaling-stroke;
  stroke-linecap: round;
  stroke-dasharray: 120 2956;
  filter: blur(16px);
  animation: ncOlSweepBar 22s linear infinite;
}

.nc-ol-runner-head {
  fill: none;
  stroke: rgba(150, 232, 255, .55);
  stroke-width: 10;
  vector-effect: non-scaling-stroke;
  stroke-linecap: round;
  stroke-dasharray: 120 2956;
  filter: blur(3px);
  animation: ncOlSweepBar 22s linear infinite;
}

.nc-ol-runner-spark {
  fill: none;
  stroke: rgba(200, 240, 255, .77);
  stroke-width: 2;
  vector-effect: non-scaling-stroke;
  stroke-linecap: round;
  stroke-dasharray: 120 2956;
  filter:
    drop-shadow(0 0 3px  rgba(180, 240, 255, .80))
    drop-shadow(0 0 10px rgba(80,  200, 255, .64));
  animation: ncOlSweepBar 22s linear infinite;
}

/* .nc-hud-row + widget sizing → blogger-hud-shared.css */

/* ── Keyframes — HUD-01 local ─────────────────────────────────── */

@keyframes hudSpinner {
  to { transform: rotate(360deg); }
}

@keyframes ncOlOpenHud {
  from { opacity: 0; }
  to   { opacity: 1; }
}

@keyframes ncOlTopSvgSlider {
  0%, 100% { transform: translateX(-70px); opacity: .65; }
  50%      { transform: translateX(70px);  opacity: 1; }
}

@keyframes ncOlSweepBar {
  from { stroke-dashoffset: 0; }
  to   { stroke-dashoffset: calc(-1 * var(--nc-ol-runner-p, 3076px)); }
}

/* ── Mobile ──────────────────────────────────────────────────── */

@media (max-width: 900px) {
  .nc-ol-widget { padding: 12px; }

  .nc-ol-panel {
    height: auto;
    grid-template-columns: 1fr;
  }

  .nc-ol-sweep,
  .nc-ol-frame-svg { display: none; }

  .nc-ol-left {
    min-width: 0;
    width: 100%;
    height: 320px;
  }

  .nc-ol-image {
    left: 0; top: 0;
    width: 100%; height: 320px;
  }

  .nc-ol-reticle {
    left: 0; top: 0;
    width: 100%; height: 320px;
  }

  .nc-ol-right {
    min-width: 0;
    height: auto;
    padding: 28px 22px 30px;
  }

  .nc-ol-title {
    font-size: 22px;
    margin-bottom: 28px;
  }

  .nc-ol-grid {
    grid-template-columns: 100px minmax(0, 1fr);
    font-size: 12px;
    row-gap: 12px;
    column-gap: 12px;
  }

  .nc-ol-value { white-space: normal; }
}
/*]]>*/
</style>

<!-- NebulaCast HUD: Panel CSS — HUD-02 (nc-or-*) -->
<style>
/*<![CDATA[*/
/* ================================================================
   NebulaCast HUD-02 — Blogger Template CSS
   Version : 1.1  (2026-06-09)
   Covers  : HUD-02 (object report panel, nc-or-*)
   Scope   : All nc-or-* and nc-hud-02-* classes — safe for Blogger,
             no :root overrides, no global resets.

   REQUIRES: blogger-hud-shared.css must be loaded first.
             (keyframes, .nc-panel-sweep, paper rows,
              media base, .nc-hud-row)

   WHERE TO PUT THIS:
   Blogger → Theme → Edit HTML
   Paste the <style> block from optional/blogger-template-snippet.txt
   just before the closing </head> tag.
   ================================================================ */

/* ── Box-sizing (scoped — does not touch Blogger theme) ────────── */

.nc-or-widget,
.nc-or-widget * {
  box-sizing: border-box;
}

/* Keyframes, .nc-panel-sweep → blogger-hud-shared.css */

/* ================================================================
   WIDGET ROOT
   ================================================================ */

.nc-or-widget {
  position: relative;
  width: 100%;
  margin: 0 auto;
  padding: 0;
  overflow: hidden;
  background: #000;
  font-family: "Share Tech Mono", monospace;
  max-height: 600px;
  transition: max-height 700ms cubic-bezier(.2, .9, .2, 1);
}

/* ── Main panel ──────────────────────────────────────────────── */

.nc-or-panel {
  position: relative;
  width: 120px;
  max-width: 100%;
  min-height: 420px; /* allows html-mode content to grow; 420px fixed via JS in mini */
  margin: 0 auto;

  display: grid;
  grid-template-columns: 30% 70%;

  overflow: clip;
  background: transparent;

  opacity: .65;

  animation:
    ncOrOpenHud 1.2s cubic-bezier(.2,.9,.2,1) forwards,
    hudBreath 5s ease-in-out infinite;

  transform-origin: top center;
  transition: transform 700ms cubic-bezier(.2, .9, .2, 1);
}

/* ── SVG frame ───────────────────────────────────────────────── */

.nc-or-frame-svg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
  pointer-events: none;
}

.nc-or-frame-main {
  fill: rgba(5,18,38,0);
  stroke: rgba(80,220,255,.92);
  stroke-width: 3;
  vector-effect: non-scaling-stroke;
  filter: drop-shadow(0 0 5px rgba(80,220,255,.85));
}

.nc-or-frame-inner,
.nc-or-frame-teeth-box {
  fill: none;
  stroke: rgba(145,245,255,.62);
  stroke-width: 1.4;
  vector-effect: non-scaling-stroke;
  filter: drop-shadow(0 0 5px rgba(80,220,255,.65));
}

/* Same fix as HUD-01: non-scaling-stroke keeps this line at its literal
   width even when the panel is shrunk to mini/micro, so it anti-aliases
   away against the outer frame's glow. Widen only in the collapsed state. */
.nc-or-widget.is-mini .nc-or-frame-inner {
  stroke-width: 2.4;
}

.nc-or-frame-teeth rect {
  fill: rgba(80,220,255,.92);
  filter: drop-shadow(0 0 6px rgba(80,220,255,.8));
}

/* ── Sweep ───────────────────────────────────────────────────── */

.nc-or-sweep {
  z-index: 2;
  opacity: .65;
}

/* ── Columns ─────────────────────────────────────────────────── */

.nc-or-left {
  position: relative;
  z-index: 3;
  min-width: 0;
  width: 100%;
  height: 420px;
  overflow: hidden;
}

.nc-or-right {
  position: relative;
  z-index: 5;
  height: 420px;
  padding: 58px 72px 28px 46px;
  color: #cfffff;
  background: transparent;
  overflow: hidden;
}

.nc-or-right > * {
  position: relative;
  z-index: 7;
}

/* ── Image ───────────────────────────────────────────────────── */

.nc-or-image {
  position: absolute;
  left: 25%;
  top: 18%;
  width: 80%;
  height: 66%;
  object-fit: cover;
  filter: brightness(.72) contrast(1.18) saturate(.85);
}

/* ── Reticle ─────────────────────────────────────────────────── */

.nc-or-reticle {
  position: absolute;
  left: 25%;
  top: 18%;
  width: 80%;
  height: 66%;
  z-index: 6;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
}

.nc-or-ring {
  position: absolute;
  border-radius: 50%;
}

.nc-or-ring-1 {
  width: 210px;
  height: 210px;
  border: 2px solid rgba(80,245,255,.75);
  box-shadow: 0 0 18px rgba(80,245,255,.5);
  animation: hudPulseOuter 4s ease-in-out infinite;
}

.nc-or-ring-2 {
  width: 166px;
  height: 166px;
  border: 2px dashed rgba(80,245,255,.82);
  animation: hudRotate 10s linear infinite;
}

.nc-or-ring-3 {
  width: 94px;
  height: 94px;
  border: 1px solid rgba(170,255,255,.88);
  opacity: .65;
  animation: hudPulseInner 2.5s ease-in-out infinite;
}

.nc-or-cross {
  position: absolute;
  width: 180px;
  height: 180px;
}

.nc-or-cross::before,
.nc-or-cross::after {
  content: "";
  position: absolute;
  background: rgba(120,255,255,.85);
  box-shadow: 0 0 10px rgba(80,245,255,.72);
}

.nc-or-cross::before {
  width: 100%;
  height: 1px;
  top: 50%;
  left: 0;
}

.nc-or-cross::after {
  width: 1px;
  height: 100%;
  left: 50%;
  top: 0;
}

.nc-or-lock-box {
  position: absolute;
  width: 122px;
  height: 122px;
  animation: hudLockBoxPulse 3.2s ease-in-out infinite;
}

.nc-or-lock-box span {
  position: absolute;
  display: block;
  width: 30px;
  height: 30px;
}

.nc-or-lock-box span::before,
.nc-or-lock-box span::after {
  content: "";
  position: absolute;
  background: rgba(150,255,255,.95);
  box-shadow: 0 0 10px rgba(80,245,255,.75);
}

.nc-or-lock-box span::before { width: 30px; height: 3px; }
.nc-or-lock-box span::after  { width: 3px;  height: 30px; }

.nc-or-lock-box span:nth-child(1)         { top: 0;    left: 0; }
.nc-or-lock-box span:nth-child(1)::before { top: 0;    left: 0; }
.nc-or-lock-box span:nth-child(1)::after  { top: 0;    left: 0; }

.nc-or-lock-box span:nth-child(2)         { top: 0;    right: 0; }
.nc-or-lock-box span:nth-child(2)::before { top: 0;    right: 0; }
.nc-or-lock-box span:nth-child(2)::after  { top: 0;    right: 0; }

.nc-or-lock-box span:nth-child(3)         { bottom: 0; left: 0; }
.nc-or-lock-box span:nth-child(3)::before { bottom: 0; left: 0; }
.nc-or-lock-box span:nth-child(3)::after  { bottom: 0; left: 0; }

.nc-or-lock-box span:nth-child(4)         { bottom: 0; right: 0; }
.nc-or-lock-box span:nth-child(4)::before { bottom: 0; right: 0; }
.nc-or-lock-box span:nth-child(4)::after  { bottom: 0; right: 0; }

.nc-or-corner {
  position: absolute;
  width: 34px;
  height: 34px;
  border-color: rgba(210,255,255,.95);
}

.nc-or-corner-tl { top: 26px;    left: 26px;  border-top: 2px solid; border-left: 2px solid; }
.nc-or-corner-tr { top: 26px;    right: 26px; border-top: 2px solid; border-right: 2px solid; }
.nc-or-corner-bl { bottom: 26px; left: 26px;  border-bottom: 2px solid; border-left: 2px solid; }
.nc-or-corner-br { bottom: 26px; right: 26px; border-bottom: 2px solid; border-right: 2px solid; }

.nc-or-center-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  border: 2px solid rgba(255,255,255,.92);
  box-shadow: 0 0 18px rgba(80,245,255,.95);
}

.nc-or-target-lock {
  position: absolute;
  bottom: 16px;
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .24em;
  color: #d8ffff;
  text-shadow: 0 0 12px rgba(80,245,255,.95);
  animation: hudBlinkSoft 1.4s ease-in-out infinite;
}

/* ── Right column content ─────────────────────────────────────── */

.nc-or-system {
  margin-bottom: 10px;
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  letter-spacing: .22em;
  color: rgba(80,220,255,.62);
}

.nc-or-title {
  margin: 0 0 16px 0;
  font-family: "Share Tech Mono", monospace;
  font-size: 34px;
  line-height: 1;
  letter-spacing: .18em;
  color: #8ff6ff;
  text-shadow:
    0 0 6px rgba(80,245,255,.72),
    0 0 14px rgba(80,245,255,.28);
}

.nc-or-desc {
  max-width: 92%;
  margin-bottom: 18px;
  font-family: "Share Tech Mono", monospace;
  font-size: 13px;
  line-height: 1.45;
  letter-spacing: .08em;
  color: rgba(170,245,255,.84);
}

.nc-or-textbox {
  max-width: 82%;
  margin-bottom: 16px;
  padding: 12px;
  border: 1px solid rgba(80,220,255,.28);
  background: rgba(0,32,52,.16);
}

.nc-or-text-title {
  margin-bottom: 7px;
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  letter-spacing: .2em;
  color: rgba(80,220,255,.82);
}

.nc-or-text {
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  line-height: 1.45;
  letter-spacing: .06em;
  color: rgba(190,250,255,.78);
}

.nc-or-log {
  max-width: 82%;
  margin-top: 14px;
  padding: 9px 11px;
  border-left: 2px solid rgba(80,220,255,.6);
  background: rgba(0,24,42,.16);
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  line-height: 1.55;
  color: rgba(170,245,255,.72);
}

/* Object mode title centering */
.nc-hud-02[data-mode="object"] .nc-or-title {
  margin: 23px auto 18px auto;
  width: max-content;
}

/* ── Animated loader bar ──────────────────────────────────────── */

.nc-or-frame-loader {
  position: absolute;
  top: 38px;
  left: 23.67%;
  width: 200px;
  height: 24px;
  z-index: 25;
  pointer-events: none;

  display: flex;
  gap: 10px;
  align-items: stretch;

  transform: skewX(49deg);
  padding: 3px;
  border: 1px solid rgba(80,220,255,.45);

  background: linear-gradient(
    90deg,
    rgba(80,220,255,.08),
    rgba(80,220,255,.02)
  );

  filter: drop-shadow(0 0 8px rgba(80,220,255,.55));
}

.nc-or-frame-loader span {
  flex: 1;
  opacity: 0;
  background: rgba(80,220,255,.95);
  box-shadow: 0 0 12px rgba(80,220,255,.85);
  animation-duration: 6.2s;
  animation-timing-function: linear;
  animation-iteration-count: infinite;
}

.nc-or-frame-loader span:nth-child(1) { animation-name: ncOrLoader1; }
.nc-or-frame-loader span:nth-child(2) { animation-name: ncOrLoader2; }
.nc-or-frame-loader span:nth-child(3) { animation-name: ncOrLoader3; }
.nc-or-frame-loader span:nth-child(4) { animation-name: ncOrLoader4; }
.nc-or-frame-loader span:nth-child(5) { animation-name: ncOrLoader5; }
.nc-or-frame-loader span:nth-child(6) { animation-name: ncOrLoader6; }
.nc-or-frame-loader span:nth-child(7) { animation-name: ncOrLoader7; }
.nc-or-frame-loader span:nth-child(8) { animation-name: ncOrLoader8; }

/* ── Aladin Lite layer (HUD02-2) ─────────────────────────────── */

.nc-hud-02-aladdin {
  display: none;
  position: absolute;
  left: var(--nc-aladin-left, 25%);
  top: var(--nc-aladin-top, 18%);
  width: var(--nc-aladin-width, 80%);
  height: var(--nc-aladin-height, 66%);
  z-index: 2;
  pointer-events: all;
  overflow: hidden;
  background: rgba(0, 8, 18, .96);
  border: 1px solid rgba(80,220,255,.28);
  box-shadow: inset 0 0 40px rgba(0,220,255,.06);
}

.nc-hud-02-aladdin > div:first-child {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.nc-hud-02-aladdin__placeholder {
  position: absolute;
  inset: 0;
  z-index: 2;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10px;
  pointer-events: none;
}

.nc-hud-02-aladdin--ready .nc-hud-02-aladdin__placeholder {
  display: none;
}

/* ── Aladin UI — dark theme overrides ──────────────────────────── */
.nc-hud-02-aladdin .aladin-logo-container,
.nc-hud-02-aladdin .aladin-logo,
.nc-hud-02-aladdin .aladin-location {
  display: none !important;
}

.nc-hud-02-aladdin .aladin-location {
  background:    rgba(0, 8, 20, 0.82) !important;
  border:        1px solid rgba(80, 220, 255, 0.28) !important;
  border-radius: 2px !important;
  backdrop-filter: blur(3px);
}

.nc-hud-02-aladdin .aladin-location .aladin-input-text {
  background:     transparent !important;
  color:          rgba(80, 220, 255, 0.90) !important;
  border:         none !important;
  font-family:    "Share Tech Mono", monospace;
  font-size:      0.65rem;
  letter-spacing: 0.04em;
  padding:        2px 4px;
}

.nc-hud-02-aladdin .aladin-location .aladin-input-text::placeholder {
  color:       rgba(80, 220, 255, 0.32);
  font-family: "Share Tech Mono", monospace;
}

.nc-hud-02-aladdin .aladin-location .aladin-input-text:focus {
  outline: none;
  color:   rgba(120, 240, 255, 1) !important;
  box-shadow: 0 0 6px rgba(80, 220, 255, 0.25);
}

.nc-hud-02-aladdin .aladin-location .aladin-location-copy {
  background:   rgba(0, 8, 20, 0.82) !important;
  border-color: rgba(80, 220, 255, 0.28) !important;
}

.nc-hud-02-aladdin .aladin-location .aladin-location-copy img {
  filter: invert(75%) sepia(60%) saturate(600%) hue-rotate(162deg) brightness(1.2);
}

.nc-hud-02-aladdin__label {
  font-family: "Share Tech Mono", monospace;
  font-size: 13px;
  letter-spacing: .20em;
  text-transform: uppercase;
  color: rgba(80,220,255,.60);
  text-shadow: 0 0 10px rgba(80,220,255,.40);
}

.nc-hud-02-aladdin__sub {
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  letter-spacing: .15em;
  text-transform: uppercase;
  color: rgba(80,220,255,.30);
}

/* ── Toolbar ──────────────────────────────────────────────────── */

.nc-or-toolbar {
  position: absolute;
  bottom: 42px;
  right: 142px;
  z-index: 35;
  display: flex;
  align-items: center;
  gap: 4px;
  pointer-events: all;
}

.nc-or-tb-group {
  display: flex;
  gap: 6px;
}

.nc-or-tb-sep {
  width: 1px;
  align-self: stretch;
  background: rgba(80, 220, 255, 0.18);
  margin: 0 6px;
}

.nc-or-tb-btn {
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: rgba(150,245,255,.75);
  background: rgba(0,12,26,.80);
  border: 1px solid rgba(80,220,255,.22);
  padding: 4px 10px;
  cursor: pointer;
  clip-path: polygon(
    6px 0%, calc(100% - 6px) 0%,
    100% 6px, 100% calc(100% - 6px),
    calc(100% - 6px) 100%, 6px 100%,
    0% calc(100% - 6px), 0% 6px
  );
  transition:
    border-color .18s ease,
    color        .18s ease,
    box-shadow   .18s ease;
}

.nc-or-tb-btn:hover {
  border-color: rgba(80,220,255,.55);
  color: rgba(150,245,255,.92);
}

.nc-or-tb-btn[aria-pressed="true"] {
  color: rgba(150,245,255,.95);
  border-color: rgba(80,220,255,.75);
  box-shadow: 0 0 8px rgba(80,220,255,.35);
}

/* ── Data-attribute CSS rules ─────────────────────────────────── */

.nc-or-widget[data-target-lock="off"] .nc-or-reticle,
.nc-or-widget[data-reticle="off"] .nc-or-reticle {
  opacity: 0;
  pointer-events: none;
}

.nc-or-reticle {
  transition: opacity .3s ease;
}

.nc-or-widget[data-aladdin="on"] .nc-hud-02-aladdin {
  display: block;
}

/* ── Info panel visibility ────────────────────────────────────── */

.nc-or-info-panel {
  display: none !important;
}

.nc-or-widget[data-info-mode="data"] .nc-or-info-panel--data {
  display: grid !important;
}

.nc-or-widget[data-info-mode="catalog"] .nc-or-info-panel--catalog {
  display: flex !important;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 120px;
}

.nc-or-widget[data-info-mode="catalog"] .nc-or-papers-list {
  display: block !important;
  overflow-y: auto;
  max-height: 200px;
  min-height: 0;
  padding: 4px 0;
  scrollbar-width: thin;
  scrollbar-color: rgba(80, 220, 255, 0.30) transparent;
}

.nc-or-widget[data-info-mode="papers"] .nc-or-info-panel--papers {
  display: flex !important;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 120px;
}

.nc-or-widget[data-info-mode="papers"] .nc-hud-papers-list {
  display: block !important;
  overflow-y: auto;
  max-height: 200px;
  min-height: 0;
  padding: 4px 0;
  scrollbar-width: thin;
  scrollbar-color: rgba(80, 220, 255, 0.30) transparent;
}

.nc-or-info-panel--catalog::-webkit-scrollbar,
.nc-or-info-panel--papers::-webkit-scrollbar  { width: 3px; }
.nc-or-info-panel--catalog::-webkit-scrollbar-track,
.nc-or-info-panel--papers::-webkit-scrollbar-track  { background: transparent; }
.nc-or-info-panel--catalog::-webkit-scrollbar-thumb,
.nc-or-info-panel--papers::-webkit-scrollbar-thumb  { background: rgba(80, 220, 255, 0.25); border-radius: 2px; }

.nc-or-info-placeholder {
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .20em;
  text-transform: uppercase;
  color: rgba(80, 220, 255, 0.55);
  text-align: center;
  line-height: 1.8;
  text-shadow: 0 0 10px rgba(80, 220, 255, 0.25);
}

/* ── DATA panel grid ──────────────────────────────────────────── */

.nc-or-grid {
  display: grid;
  grid-template-columns: 95px minmax(0, 1fr) 95px minmax(0, 1fr);
  row-gap: 8px;
  column-gap: 14px;
  padding-top: 26px;
  font-family: "Share Tech Mono", monospace;
  font-size: 12px;
  line-height: 1.05;
  letter-spacing: .05em;
}

.nc-or-label,
.nc-or-value {
  padding-bottom: 4px;
  border-bottom: 1px solid rgba(80,220,255,.26);
}

.nc-or-label {
  font-weight: 400;
  color: #dfffff;
  white-space: nowrap;
}

.nc-or-value {
  color: #9ffcff;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  text-align: right;
}

/* ── Data status / spinner / note ─────────────────────────────── */

.nc-or-data-status {
  grid-column: 1 / -1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 16px 0 10px;
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .14em;
  text-transform: uppercase;
  text-align: center;
}

.nc-or-data-status--loading { color: rgba(80, 220, 255, 0.50); }
.nc-or-data-status--error   { color: rgba(255, 100, 100, 0.60); }

.nc-or-spinner {
  width: 22px;
  height: 22px;
  border: 2px solid rgba(80, 220, 255, 0.14);
  border-top-color: rgba(80, 220, 255, 0.75);
  border-radius: 50%;
  animation: hudSpinner02 0.85s linear infinite;
  flex-shrink: 0;
}

.nc-or-data-note {
  grid-column: 1 / -1;
  font-family: "Share Tech Mono", monospace;
  font-size: 9px;
  letter-spacing: .12em;
  text-align: center;
  color: rgba(80, 220, 255, 0.22);
  padding-top: 8px;
  border-top: 1px solid rgba(80, 220, 255, 0.10);
  margin-top: 4px;
}

/* ── Catalog / paper cards ────────────────────────────────────── */

.nc-or-paper-card {
  padding: 8px 10px;
  border-bottom: 1px solid rgba(80, 220, 255, 0.14);
}

.nc-or-paper-card:last-child {
  border-bottom: none;
}

.nc-or-paper-title {
  display: block;
  font-family: "Share Tech Mono", monospace;
  font-size: 13px;
  line-height: 1.45;
  letter-spacing: 0.03em;
  color: rgba(130, 245, 255, 0.90);
  text-decoration: none;
  text-shadow: 0 0 6px rgba(80, 220, 255, 0.30);
  margin-bottom: 4px;
}

.nc-or-paper-title:hover {
  color: rgba(160, 255, 255, 1);
  text-decoration: underline;
  text-decoration-color: rgba(80, 220, 255, 0.40);
  text-underline-offset: 2px;
}

.nc-or-paper-meta {
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: rgba(80, 200, 255, 0.50);
  margin-bottom: 4px;
}

.nc-or-paper-abstract {
  font-family: "Share Tech Mono", monospace;
  font-size: 12px;
  line-height: 1.55;
  color: rgba(160, 240, 255, 0.45);
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* .nc-hud-panel-header, .nc-hud-paper-* → blogger-hud-shared.css */

/* ── Mode: object / html ──────────────────────────────────────── */

.nc-hud-02[data-mode="html"] .nc-or-left,
.nc-hud-02[data-mode="html"] .nc-or-right,
.nc-hud-02[data-mode="html"] .nc-or-toolbar {
  display: none !important;
}

/* HTML slot — positioned in fixed-height object mode */
.nc-hud-02-html-slot {
  display: none;
  position: absolute;
  inset: 76px 30px 20px 30px;
  z-index: 10;
  overflow-y: auto;
  overflow-x: hidden;
  padding: 16px 40px 16px 40px;
  scrollbar-width: thin;
  scrollbar-color: rgba(80, 220, 255, 0.30) transparent;
}

.nc-hud-02[data-mode="html"] .nc-hud-02-html-slot {
  display: block;
}

.nc-hud-02-html-slot::-webkit-scrollbar       { width: 4px; }
.nc-hud-02-html-slot::-webkit-scrollbar-track  { background: transparent; }
.nc-hud-02-html-slot::-webkit-scrollbar-thumb  { background: rgba(80, 220, 255, 0.28); border-radius: 2px; }

/* HTML slot typography */
.nc-hud-02-html-slot h1,
.nc-hud-02-html-slot h2,
.nc-hud-02-html-slot h3,
.nc-hud-02-html-slot h4 {
  font-family: "Share Tech Mono", monospace;
  font-weight: 400;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: rgba(130, 245, 255, 0.92);
  text-shadow: 0 0 8px rgba(80, 220, 255, 0.40);
  margin: 0 0 12px;
  line-height: 1.25;
}

.nc-hud-02-html-slot h1 { font-size: 22px; }
.nc-hud-02-html-slot h2 { font-size: 17px; }
.nc-hud-02-html-slot h3 { font-size: 13px; letter-spacing: .16em; }
.nc-hud-02-html-slot h4 { font-size: 11px; letter-spacing: .18em; color: rgba(80, 220, 255, 0.70); }

.nc-hud-02-html-slot p {
  font-family: "Share Tech Mono", monospace;
  font-size: 12px;
  line-height: 1.65;
  color: rgba(190, 250, 255, 0.78);
  margin: 0 0 10px;
}

.nc-hud-02-html-slot a {
  color: rgba(80, 220, 255, 0.90);
  text-decoration: underline;
  text-decoration-color: rgba(80, 220, 255, 0.35);
  text-underline-offset: 3px;
}

.nc-hud-02-html-slot a:hover {
  color: rgba(140, 245, 255, 1);
  text-decoration-color: rgba(140, 245, 255, 0.60);
}

.nc-hud-02-html-slot img:not(.nc-hud-media img) {
  max-width: 100%;
  width: auto !important;
  height: auto !important;
  display: block;
  border: 1px solid rgba(80, 220, 255, 0.22);
  margin-bottom: 10px;
  object-fit: contain;
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot img:not(.nc-hud-media img),
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot img:not(.nc-hud-media img) {
  max-height: var(--nc-image-maxi-height, 280px);
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .separator,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .separator {
  float: left;
  max-width: var(--nc-image-maxi-width, 42%);
  margin: 0 16px 12px 0;
  box-sizing: border-box;
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .separator a,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .separator a {
  float: none !important;
  display: block;
  max-width: 100%;
  margin: 0 !important;
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .separator img,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .separator img {
  max-width: 100%;
  max-height: var(--nc-image-maxi-height, 280px);
  width: auto !important;
  height: auto !important;
  margin-bottom: 0;
}

/* ── HUD-02 title classes (5 independent variants) ───────────── */
/* All five share an identical base (mirrors HUD-01 nc-ol-title).
   Edit the comma block once to change the base for all variants.
   Add per-variant overrides in the stubs below — no cascade bleed.

   NOTE: nc-or-title (no suffix) is reserved for object-mode use (line ~372).
   These five use the -maxi / -mini / -micro / -b / -c suffixes to stay clear.

   Variants:
     nc-or-title-maxi   — expanded (maxi) panel   [default for html mode]
     nc-or-title-mini   — mini thumbnail
     nc-or-title-micro  — micro thumbnail
     nc-or-title-b      — reserve B
     nc-or-title-c      — reserve C                              */

.nc-or-title-maxi,
.nc-or-title-mini,
.nc-or-title-micro,
.nc-or-title-b,
.nc-or-title-c {
  position: relative;
  /* !important: .nc-hud-02-html-slot h1..h4 (generic fallback typography,
     below) has higher specificity (class+element vs. this plain class) and
     would otherwise win, silently shrinking this margin/line-height on the
     live site regardless of what's declared here. */
  margin: 0 auto 34px auto !important;
  width: max-content;
  font-family: "Share Tech Mono", monospace;
  font-size: var(--nc-title-maxi-size, var(--nc-title-size, 30px)) !important;
  line-height: 1 !important;
  font-weight: 400;
  letter-spacing: .16em;
  text-transform: uppercase;
  color: #8ff6ff;
  text-shadow:
    0 0 6px rgba(80,245,255,.72),
    0 0 14px rgba(80,245,255,.28);
}

.nc-ol-widget.is-mini .nc-ol-title-maxi,
.nc-or-widget.is-mini .nc-or-title-maxi {
  font-size: var(--nc-title-mini-size, var(--nc-title-size, 30px)) !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-title-micro,
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-title-micro {
  font-size: var(--nc-title-micro-size, var(--nc-title-size, 30px)) !important;
}

/* Decorative corner brackets — shared across all variants */
.nc-or-title-maxi::before,  .nc-or-title-maxi::after,
.nc-or-title-mini::before,  .nc-or-title-mini::after,
.nc-or-title-micro::before, .nc-or-title-micro::after,
.nc-or-title-b::before,     .nc-or-title-b::after,
.nc-or-title-c::before,     .nc-or-title-c::after {
  content: "";
  position: absolute;
  top: var(--nc-title-bracket-top, -12px);
  width: var(--nc-title-bracket-size, 18px);
  height: var(--nc-title-bracket-size, 18px);
  border-color: rgba(120,245,255,.85);
}

.nc-or-title-maxi::before,
.nc-or-title-mini::before,
.nc-or-title-micro::before,
.nc-or-title-b::before,
.nc-or-title-c::before {
  left: calc(-1 * var(--nc-title-bracket-gap, 40px));
  border-top: var(--nc-title-bracket-stroke, 2px) solid;
  border-left: var(--nc-title-bracket-stroke, 2px) solid;
}

.nc-or-title-maxi::after,
.nc-or-title-mini::after,
.nc-or-title-micro::after,
.nc-or-title-b::after,
.nc-or-title-c::after {
  right: calc(-1 * var(--nc-title-bracket-gap, 40px));
  border-top: var(--nc-title-bracket-stroke, 2px) solid;
  border-right: var(--nc-title-bracket-stroke, 2px) solid;
}

/* ── Per-variant position vars ───────────────────────────────── */
/* Set on WIDGET ROOT via style="". Uses position:relative from base.
   Maxi/micro coords are in screen px.
   Mini coords are in UNSCALED panel space (÷ 0.352 → screen px). */
.nc-or-title-maxi {
  top:  var(--nc-title-maxi-top,  0px);
  left: var(--nc-title-maxi-left, 0px);
}
.nc-or-title-mini {
  top:  var(--nc-title-mini-top,  0px);
  left: var(--nc-title-mini-left, 0px);
}
/* When widget is is-mini, apply mini-vars regardless of title class suffix */
.nc-ol-widget.is-mini .nc-ol-title-maxi,
.nc-or-widget.is-mini .nc-or-title-maxi {
  top:  var(--nc-title-mini-top,  var(--nc-title-maxi-top,  0px));
  left: var(--nc-title-mini-left, var(--nc-title-maxi-left, 0px));
}
/* nc-or-title-micro position: see micro-mini state rule below.
   .nc-or-title-b { }  .nc-or-title-c { }                       */

/* ── HUD-02 button classes (5 independent variants) ──────────── */
/* All five share an identical base (mirrors nc-or-tb-btn style).
   Edit the comma block once to change the base for all variants.
   Add per-variant overrides in the stubs below — no cascade bleed.

   NOTE: nc-or-tb-btn (no suffix) is reserved for object-mode tab buttons.
   These five use the -maxi / -mini / -micro / -b / -c suffixes to stay clear.

   Variants:
     nc-or-btn-maxi   — expanded (maxi) panel   [default for html mode]
     nc-or-btn-mini   — mini thumbnail
     nc-or-btn-micro  — micro thumbnail
     nc-or-btn-b      — reserve B
     nc-or-btn-c      — reserve C                               */

.nc-or-btn-maxi,
.nc-or-btn-mini,
.nc-or-btn-micro,
.nc-or-btn-b,
.nc-or-btn-c {
  display: inline-block;
  font-family: "Share Tech Mono", monospace;
  font-size: 10px;
  line-height: 1;
  letter-spacing: .12em;
  text-transform: uppercase;
  text-decoration: none;
  color: rgba(150,245,255,.75);
  background: rgba(0,12,26,.80);
  border: 1px solid rgba(80,220,255,.22);
  padding: 6px 14px;
  cursor: pointer;
  clip-path: polygon(
    6px 0%, calc(100% - 6px) 0%,
    100% 6px, 100% calc(100% - 6px),
    calc(100% - 6px) 100%, 6px 100%,
    0% calc(100% - 6px), 0% 6px
  );
  transition:
    border-color .18s ease,
    color        .18s ease,
    box-shadow   .18s ease;
}

.nc-or-btn-maxi:hover,
.nc-or-btn-mini:hover,
.nc-or-btn-micro:hover,
.nc-or-btn-b:hover,
.nc-or-btn-c:hover {
  border-color: rgba(80,220,255,.55);
  color: rgba(150,245,255,.92);
  text-decoration: none;
}

.nc-or-btn-maxi:active,
.nc-or-btn-mini:active,
.nc-or-btn-micro:active,
.nc-or-btn-b:active,
.nc-or-btn-c:active {
  color: rgba(150,245,255,.95);
  border-color: rgba(80,220,255,.75);
  box-shadow: 0 0 8px rgba(80,220,255,.35);
}

/* ── Per-variant overrides ────────────────────────────────────── */
/* Add properties here to diverge from the shared base.
   Same specificity, later source order → always wins over the base block.

.nc-or-btn-maxi   { }
.nc-or-btn-mini   { }
.nc-or-btn-micro  { }
.nc-or-btn-b      { }
.nc-or-btn-c      { }                                           */

/* ── HTML mode auto-height override (Blogger — content grows panel) */

.nc-hud-02[data-mode="html"] .nc-or-panel {
  display: block;
  height: auto;
  min-height: 420px;
  overflow: hidden;
}

/* HTML mode maxi: remove widget max-height so long content isn't clipped */
.nc-hud-02[data-mode="html"].nc-or-widget:not(.is-mini) {
  max-height: none;
  overflow: visible;
}

.nc-hud-02[data-mode="html"].nc-or-widget:not(.is-mini) .nc-or-panel:has(.nc-hud-vertical-maxi),
.nc-hud-02[data-mode="html"].nc-or-widget:not(.is-mini) .nc-or-panel:has(.nc-hud-heygen-maxi) {
  min-height: calc(58px + var(--nc-vertical-row-h, 320px) + 76px);
}


.nc-hud-02[data-mode="html"] .nc-hud-02-html-slot {
  position: static;
  inset: unset;
  overflow-x: hidden;
  overflow-y: visible;
  /* --nc-inner-left/right: real px distance from the panel edge to the
     decorative inner-frame SVG line, written by ncFrameAdapt on every
     resize (see hud02-frame-adapt.js / the playground's inline copy).
     The frame is a non-uniformly-scaled SVG (preserveAspectRatio="none"),
     so that distance isn't a fixed constant — it moves with panel width.
     52px is the pre-JS/no-JS fallback (close at typical panel widths).
     Content positioned via --nc-text-left etc. now aligns to this same
     reference: see textStyleAttr()/absTextStyleAttr() in hud-playground.
     +14px on both sides: landing content flush on the inner line (0px
     gap) read as touching the frame rather than framed by it — at usual
     panel widths inner/outer are only ~15-30px apart, close enough that
     "flush with inner" was easy to mistake for "flush with outer" at a
     glance. This keeps --nc-inner-left/right meaning the frame's literal
     position (useful as-is elsewhere) and adds the breathing room here,
     at the point of use, instead of baking it into that variable. */
  padding: 76px calc(var(--nc-inner-right, 52px) + 14px) var(--nc-slot-padding-bottom, 90px) calc(var(--nc-inner-left, 52px) + 14px);
}

/* Vertical/HeyGen row: the generic 90px slot bottom padding (sized for
   scrolling paper-mode text) left a large fixed gap below the video/copy
   whenever they were shorter than that padding, so the panel looked like
   it wasn't really hugging the block's actual bottom edge.
   76px, not less: ncFrameAdapt's updateOr()/orPaths() draws the decorative
   INNER frame line (the thin bracket, not the big glowing outer one) at
   H-54 (54px above the panel's own bottom edge, H = panel.offsetHeight —
   see the <script> block below). The gap between the row's actual bottom
   and that inner line is (padding-bottom - 54), NOT padding-bottom itself,
   since the row sits (panel-bottom - padding-bottom) from the top.
   76 = 54 + ~22px, landing the inner-line gap in the ~20-26px range that
   actually reads as "framing" the block instead of a stray empty margin
   below it. Matches the equivalent HUD-01 override, against HUD-01's own
   (smaller) inner-frame inset. */
.nc-hud-02[data-mode="html"].nc-or-widget:not(.is-mini) .nc-hud-02-html-slot:has(.nc-hud-vertical-maxi),
.nc-hud-02[data-mode="html"].nc-or-widget:not(.is-mini) .nc-hud-02-html-slot:has(.nc-hud-heygen-maxi) {
  padding-bottom: 76px;
}

.nc-hud-01-html-slot::after,
.nc-hud-02-html-slot::after {
  content: "";
  display: table;
  clear: both;
}

/* .nc-hud-media, .nc-yt-facade, .nc-tt-facade, etc. → blogger-hud-shared.css */

/* Video position — vars on widget or .nc-hud-media */
.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .nc-hud-media,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .nc-hud-media {
  top: var(--nc-video-maxi-top, var(--nc-video-top, 0px));
  left: var(--nc-video-maxi-left, var(--nc-video-left, 0px));
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot .nc-hud-media,
.nc-or-widget.is-mini .nc-hud-02-html-slot .nc-hud-media {
  top: var(--nc-video-mini-top, var(--nc-video-top, 0px));
  left: var(--nc-video-mini-left, var(--nc-video-left, 0px));
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot .nc-hud-media,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot .nc-hud-media {
  top: var(--nc-video-micro-top, var(--nc-video-top, 0px));
  left: var(--nc-video-micro-left, var(--nc-video-left, 0px));
}

/* Opt-in modifier (add alongside .nc-hud-media in markup): true corner anchor.
   Maxi-only — .nc-ol-panel/.nc-or-panel is the nearest position:relative
   ancestor while the html-slot stays position:static in maxi, so absolute
   here is measured from the panel's own top-left corner instead of the
   slot's padded/title-offset flow position. Scoped to :not(.is-mini) so it
   never fights the mini/micro !important position overrides below, and
   opt-in via an extra class so existing --nc-video-maxi-top/left nudges on
   other frames keep their current relative-offset behavior unchanged. */
.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .nc-hud-media.nc-hud-media-corner-anchor,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .nc-hud-media.nc-hud-media-corner-anchor {
  position: absolute;
  top: var(--nc-video-maxi-top, var(--nc-video-top, 0px));
  left: var(--nc-video-maxi-left, var(--nc-video-left, 0px));
}

/* Video (YouTube / HeyGen): description + title only in maxi; mini/micro = media thumbnail */
.nc-hud-only-maxi { }

.nc-ol-widget.is-mini .nc-hud-01-html-slot .nc-hud-only-maxi,
.nc-or-widget.is-mini .nc-hud-02-html-slot .nc-hud-only-maxi {
  display: none !important;
}

/* YouTube maxi: video full width, copy below; mini/micro = video thumbnail only */
.nc-hud-youtube-maxi {
  display: flex;
  flex-direction: column;
  align-items: stretch;
  gap: 12px;
  width: 100%;
  margin-bottom: 14px;
}
.nc-hud-youtube-maxi > .nc-hud-media.nc-hud-media-horizontal {
  flex: 0 0 auto;
  width: var(--nc-video-maxi-width, var(--nc-video-width, 100%));
  height: var(--nc-video-maxi-height, var(--nc-video-height, auto));
  max-width: 100%;
  margin-bottom: 0;
}
.nc-hud-youtube-copy {
  width: 100%;
  min-width: 0;
}
.nc-hud-youtube-copy p {
  margin-bottom: 10px;
}
.nc-hud-youtube-copy p:last-child {
  margin-bottom: 0;
}

/* Vertical video (9:16): HeyGen, YouTube Shorts, TikTok */
.nc-or-widget:has(.nc-hud-vertical-maxi),
.nc-or-widget:has(.nc-hud-heygen-maxi) {
  --nc-vertical-video-h: var(--nc-video-maxi-height, var(--nc-video-height, var(--nc-heygen-video-h, 300px)));
  /* No "+20px" here on purpose: that used to make the row taller than the
     video even when video was the tallest child, so the gap to the frame
     below ended up ~20px bigger than in the text-taller case — an
     inconsistent bottom margin depending on which side was taller. Row
     height now exactly matches whichever of video/text is tallest. */
  --nc-vertical-row-h: var(--nc-vertical-video-h, 300px);
}

.nc-hud-vertical-maxi,
.nc-hud-heygen-maxi {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  gap: 16px;
  width: 100%;
  height: auto;
  min-height: var(--nc-vertical-row-h, 320px);
  margin-bottom: 0;
  box-sizing: border-box;
}
/* min-width: 0 — see the matching HUD-01 rule in blogger-hud01-template.css
   for the full explanation: .nc-hud-media-vertical is a shared "public
   API" class also used unscoped by HUD-03's CSS (min-width:220px), which
   otherwise wins by default here since nothing below sets min-width. */
.nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-hud-heygen-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-tiktok {
  flex: 0 0 auto;
  align-self: flex-start;
  width: var(--nc-video-maxi-width, var(--nc-video-width, calc(var(--nc-vertical-video-h, 300px) * 9 / 16)));
  min-width: 0;
  height: var(--nc-video-maxi-height, var(--nc-video-height, var(--nc-vertical-video-h, 300px)));
  max-height: var(--nc-video-maxi-height, var(--nc-video-height, var(--nc-vertical-video-h, 300px)));
  aspect-ratio: 9 / 16;
  margin: 0;
  position: relative;
  overflow: hidden;
}

/* TikTok direct iframe /embed/v2/ — вписать в 9:16 бокс (как YouTube) */
.nc-hud-media.nc-hud-media-tiktok > iframe {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: 100% !important;
  min-width: 0 !important;
  max-width: 100% !important;
  min-height: 0 !important;
  max-height: 100% !important;
  border: none !important;
  display: block !important;
}

/* Fallback: blockquote + embed.js (если вставлен вручную без facade) */
.nc-hud-media.nc-hud-media-tiktok .tiktok-embed,
.nc-hud-media.nc-hud-media-tiktok .tiktok-embed > div,
.nc-hud-media.nc-hud-media-tiktok .tiktok-embed iframe {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: 100% !important;
  min-width: 0 !important;
  max-width: 100% !important;
  min-height: 0 !important;
  max-height: 100% !important;
  margin: 0 !important;
  padding: 0 !important;
  transform: none !important;
  box-sizing: border-box !important;
  border: none !important;
}

/* TikTok: ширино-управляемый размер.
   --nc-tt-w задаётся на ряде, наследуется медиа-элементом.
   min-height ряда = высота видео + 20px gap (динамически). */
.nc-hud-vertical-maxi:has(.nc-hud-media-tiktok),
.nc-hud-heygen-maxi:has(.nc-hud-media-tiktok) {
  --nc-tt-w: max(200px, var(--nc-video-maxi-width, var(--nc-video-width, 250px)));
  align-items: stretch;
  height: auto;
  min-height: calc(var(--nc-tt-w) * 16 / 9 + 20px);
}

.nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-tiktok {
  width:      var(--nc-tt-w) !important;
  height:     calc(var(--nc-tt-w) * 16 / 9) !important;
  max-height: none !important;
  min-width:  200px;
  min-height: 356px;
}

.nc-hud-vertical-copy,
.nc-hud-heygen-copy {
  display: block;
  flex: 1 1 0;
  min-width: 0;
  text-align: left;
}

.nc-hud-vertical-copy p,
.nc-hud-heygen-copy p {
  margin-bottom: 10px;
}

.nc-hud-vertical-copy p:last-child,
.nc-hud-heygen-copy p:last-child {
  margin-bottom: 0;
}

.nc-ol-widget.is-mini .nc-hud-vertical-copy,
.nc-ol-widget.is-mini .nc-hud-heygen-copy {
  display: block !important;
  visibility: visible !important;
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot:has(.nc-hud-media):not(:has(.nc-hud-vertical-maxi)):not(:has(.nc-hud-heygen-maxi)) > h2,
.nc-or-widget.is-mini .nc-hud-02-html-slot:has(.nc-hud-media):not(:has(.nc-hud-vertical-maxi)):not(:has(.nc-hud-heygen-maxi)) > h2 {
  display: none !important;
}

/* Mini slot with non-youtube media (vertical/heygen): keep flex-column layout */
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-01-html-slot:has(.nc-hud-media),
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:has(.nc-hud-media) {
  padding: 58px 24px 18px !important;
  overflow: hidden !important;
  display: flex !important;
  flex-direction: column !important;
  box-sizing: border-box !important;
  height: 420px !important;
  max-height: 420px !important;
}

/* Mini slot with youtube-maxi: switch to block+relative so absolute children work */
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-01-html-slot:has(.nc-hud-youtube-maxi),
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:has(.nc-hud-youtube-maxi) {
  display: block !important;
  position: relative !important;
}

/* Mini youtube-maxi: absolute fill of slot content area */
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-youtube-maxi,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-youtube-maxi {
  position: absolute !important;
  top: 24px !important;
  left: 24px !important;
  right: 24px !important;
  bottom: 24px !important;
  width: auto !important;
  height: auto !important;
  margin: 0 !important;
}

.nc-or-widget.is-mini .nc-hud-vertical-maxi,
.nc-or-widget.is-mini .nc-hud-heygen-maxi {
  flex: 1 1 auto !important;
  display: flex !important;
  flex-direction: row !important;
  height: 100% !important;
  min-height: 0 !important;
  gap: 10px !important;
}

.nc-or-widget.is-mini .nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-or-widget.is-mini .nc-hud-heygen-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-or-widget.is-mini .nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-tiktok {
  flex: 0 0 auto !important;
  max-width: 40% !important;
  min-width: 0 !important;
  width: var(--nc-video-mini-width, var(--nc-video-width, auto)) !important;
  height: var(--nc-video-mini-height, var(--nc-video-height, 100%)) !important;
  max-height: var(--nc-video-mini-height, var(--nc-video-height, 100%)) !important;
  aspect-ratio: 9 / 16 !important;
  overflow: hidden !important;
}

.nc-or-widget.is-mini .nc-hud-vertical-maxi:has(.nc-hud-media-tiktok) {
  align-items: stretch !important;
}

.nc-or-widget.is-mini .nc-hud-vertical-copy,
.nc-or-widget.is-mini .nc-hud-heygen-copy {
  flex: 1 1 0 !important;
  max-height: 100% !important;
  overflow: hidden !important;
}

.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-01-html-slot:has(.nc-hud-media) > .nc-hud-media,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:has(.nc-hud-media) > .nc-hud-media,
.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-youtube-maxi > .nc-hud-media,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-youtube-maxi > .nc-hud-media {
  /* absolute fill: parent (youtube-maxi) is position:absolute with explicit bounds */
  position: absolute !important;
  inset: 0 !important;
  width: auto !important;
  height: auto !important;
  margin: 0 !important;
  aspect-ratio: unset !important;
}

.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-media,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-media {
  position: relative !important;
}

.nc-ol-widget[data-collapse="mini"].is-mini .nc-hud-media iframe,
.nc-or-widget[data-collapse="mini"].is-mini .nc-hud-media iframe {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: 100% !important;
}

/* Image position — vars on widget (см. cheat-sheet) */
.nc-hud-float-image,
.nc-hud-slot-image {
  position: relative;
  display: block;
  border: 1px solid rgba(80,220,255,.22);
  object-fit: contain;
}

.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .nc-hud-float-image,
.nc-ol-widget:not(.is-mini) .nc-hud-01-html-slot .nc-hud-slot-image,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .nc-hud-float-image,
.nc-or-widget:not(.is-mini) .nc-hud-02-html-slot .nc-hud-slot-image {
  float: var(--nc-image-maxi-float, left) !important;
  top: var(--nc-image-maxi-top, var(--nc-image-top, 0px));
  left: var(--nc-image-maxi-left, var(--nc-image-left, auto));
  width: var(--nc-image-maxi-width, var(--nc-image-width, 28%)) !important;
  height: var(--nc-image-maxi-height, var(--nc-image-height, auto)) !important;
  max-width: none !important;
  max-height: none !important;
  margin: 0 34px 22px 0;
}

/* Compatibility for snippets generated while `auto` was the maxi default. */
:is(.nc-ol-widget, .nc-or-widget):not(.is-mini):is(
  [style*="--nc-image-maxi-width: auto"],
  [style*="--nc-image-maxi-width:auto"]
) :is(.nc-hud-float-image, .nc-hud-slot-image) {
  width: 28% !important;
}

/* `center` is a HUD API value, not a native CSS float value. */
:is(.nc-ol-widget, .nc-or-widget):not(.is-mini):is(
  [style*="--nc-image-maxi-float: center"],
  [style*="--nc-image-maxi-float:center"]
) :is(.nc-hud-float-image, .nc-hud-slot-image) {
  float: none !important;
  margin-left: auto !important;
  margin-right: auto !important;
}

.nc-ol-widget.is-mini:not([data-collapse="micro"]) .nc-hud-01-html-slot .nc-hud-float-image,
.nc-ol-widget.is-mini:not([data-collapse="micro"]) .nc-hud-01-html-slot .nc-hud-slot-image,
.nc-or-widget.is-mini:not([data-collapse="micro"]) .nc-hud-02-html-slot .nc-hud-float-image,
.nc-or-widget.is-mini:not([data-collapse="micro"]) .nc-hud-02-html-slot .nc-hud-slot-image {
  float: none;
  position: absolute;
  top: var(--nc-image-mini-top, var(--nc-image-top, 0px)) !important;
  left: var(--nc-image-mini-left, var(--nc-image-left, 0px)) !important;
  width: var(--nc-image-mini-width, auto) !important;
  max-width: none !important;
  height: var(--nc-image-mini-height, 100%) !important;
  max-height: none !important;
  margin: 0;
  object-fit: contain;
  object-position: left top;
}

.nc-hud-01[data-mode="html"].nc-ol-widget.is-mini:not([data-collapse="micro"]) .nc-ol-panel,
.nc-hud-02[data-mode="html"].nc-or-widget.is-mini:not([data-collapse="micro"]) .nc-or-panel {
  height: 420px !important;
  max-height: 420px !important;
  min-height: 420px !important;
}

.nc-hud-01[data-mode="html"].nc-ol-widget.is-mini:not([data-collapse="micro"]) .nc-hud-01-html-slot:not(:has(.nc-hud-media)),
.nc-hud-02[data-mode="html"].nc-or-widget.is-mini:not([data-collapse="micro"]) .nc-hud-02-html-slot:not(:has(.nc-hud-media)) {
  position: absolute;
  inset: 5px 20px 5px 10px;
  padding: 0 !important;
  overflow: hidden !important;
  box-sizing: border-box;
  display: block !important;
}

/* HUD-02 mini html: tighten text box to avoid left overflow */
.nc-hud-02[data-mode="html"].nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) {
  inset: 5px 24px 5px 24px;
  padding-left: var(--nc-hud02-mini-left-offset, 14px) !important;
}

.nc-hud-02[data-mode="html"].nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) h2 {
  font-size: 12px;
  line-height: 1.2;
  letter-spacing: .08em;
  margin: 0 0 4px;
  max-width: 100%;
  overflow-wrap: anywhere;
  word-break: break-word;
}

.nc-hud-02[data-mode="html"].nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) h2::before,
.nc-hud-02[data-mode="html"].nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) h2::after {
  display: none;
}

/* HUD-02 mini html: default left offset for floated image */
.nc-hud-02[data-mode="html"].nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot .nc-hud-float-image,
.nc-hud-02[data-mode="html"].nc-or-widget[data-collapse="mini"].is-mini .nc-hud-02-html-slot .nc-hud-slot-image {
  left: var(--nc-image-mini-left, var(--nc-image-left, 14px));
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot img:not(.nc-hud-media img),
.nc-or-widget.is-mini .nc-hud-02-html-slot img:not(.nc-hud-media img) {
  max-height: var(--nc-image-mini-height, 100%);
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot .separator,
.nc-or-widget.is-mini .nc-hud-02-html-slot .separator {
  float: left;
  max-width: var(--nc-image-mini-width, 40%);
  margin: 0 10px 6px 0;
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot .separator img,
.nc-or-widget.is-mini .nc-hud-02-html-slot .separator img {
  max-height: var(--nc-image-mini-height, 160px);
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot p,
.nc-or-widget.is-mini .nc-hud-02-html-slot p {
  font-size: 11px;
  line-height: 1.45;
  margin-bottom: 6px;
}

.nc-ol-widget.is-mini .nc-hud-01-html-slot h2,
.nc-or-widget.is-mini .nc-hud-02-html-slot h2 {
  font-size: 13px;
  margin-bottom: 6px;
}

/* Prevent non-floated images from overflowing the slot width */
.nc-hud-02-html-slot img:not(.nc-hud-float-image):not(.nc-hud-slot-image) {
  max-width: 100%;
  height: auto;
}

/* ── Mini / Expanded toggle ───────────────────────────────────── */

/* Default (mini) slot — 317×148, scale 0.352 */
.nc-or-widget.is-mini {
  max-height: 148px;
  max-width: 317px;
  cursor: pointer;
}

/* ── Mini external button (nc-or-btn-mini) ───────────────────────
   Placement: direct child of widget AFTER the panel, same pattern as nc-or-btn-micro.
   The panel's layout height is ~420px (scale shrinks it visually, not in flow), so
   the button MUST be position:absolute to escape the flow.
   !important overrides JS widget.style.maxHeight = '148px' and overflow = 'hidden'. */
.nc-or-widget[data-collapse="mini"].is-mini > .nc-or-btn-mini,
.nc-or-widget:not([data-collapse]).is-mini > .nc-or-btn-mini {
  position: absolute !important;
  top:  var(--nc-btn-mini-top,  152px) !important;  /* thumbnail (148px) + gap (4px) */
  left: var(--nc-btn-mini-left, 0px)   !important;
  z-index: 100 !important;
}
.nc-or-widget:not(.is-mini) > .nc-or-btn-mini {
  display: none !important;
}
.nc-or-widget[data-collapse="mini"].is-mini:has(> .nc-or-btn-mini),
.nc-or-widget:not([data-collapse]).is-mini:has(> .nc-or-btn-mini) {
  max-height: 174px !important;   /* 148px + 4px gap + 22px button */
  overflow: visible !important;
}

/* Micro slot — no scale; custom layout: unscaled h2 label above + portrait image slot.
   JS passes scale=null / panelW=87 / panelH=130 / miniH=149 to NcHudMini by default.
   Widget overflow:visible lets the outer h2 (direct child, before panel) show above. */
.nc-or-widget[data-collapse="micro"].is-mini {
  max-height: calc(var(--nc-micro-h, 130px) + 19px); /* slot + label (~15px) + gap (4px) */
  max-width: var(--nc-micro-w, 87px);
  padding: 0 !important;
  overflow: visible !important;
}
/* When an external micro button is present — extend height to fit it below the slot */
.nc-or-widget[data-collapse="micro"].is-mini:has(> .nc-or-btn-micro) {
  max-height: calc(var(--nc-micro-h, 130px) + 45px); /* +26px btn (22px + 4px gap) */
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-ol-panel,
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-panel {
  min-height: 0 !important;
  height: var(--nc-micro-h, 130px) !important;
  max-height: var(--nc-micro-h, 130px) !important;
  overflow: visible !important; /* allow SVG frame drop-shadow to extend beyond panel bounds */
  margin: 0 !important;
  transform: none !important;
  animation: none !important;
  opacity: 1 !important;
}

/* Scale loader bar to fit the portrait slot (original bar is sized for 1200×420 panel) */
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-frame-loader {
  top:    calc(var(--nc-micro-h, 130px) * 0.09);   /* SVG bevel y=38/420 */
  left:   20%;
  width:  calc(var(--nc-micro-w, 87px) * 0.72);   /* usable bevel width */
  height: calc(var(--nc-micro-h, 130px) * 0.055);  /* SVG bevel span 24/420 */
  padding: 2px;
  gap: 4px;
}

.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot {
  padding: 0 !important;
  overflow: hidden !important;
}

.nc-ol-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-01-html-slot,
.nc-or-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-02-html-slot {
  position: absolute !important;
  inset: 0 !important;
  overflow: visible !important;
  z-index: auto !important;
}

.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:has(.nc-hud-media) {
  height: 100% !important;
}

.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:has(.nc-hud-media) > *:not(.nc-hud-media):not(.nc-hud-youtube-maxi):not(.nc-hud-vertical-maxi):not(.nc-hud-heygen-maxi):not(.nc-or-btn-micro) {
  display: none !important;
}

.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: var(--nc-micro-h, 130px) !important;
  min-height: var(--nc-micro-h, 130px) !important;
  max-height: var(--nc-micro-h, 130px) !important;
  padding: 0 !important;
  overflow: visible !important;
  z-index: auto !important;
  box-sizing: border-box !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) .separator,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) .separator,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) a:has(> img),
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) a:has(> img) {
  display: contents !important;
  float: none !important;
  margin: 0 !important;
  padding: 0 !important;
  border: none !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) > *:not(img):not(.nc-ol-btn-micro):not(:has(img)),
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) > *:not(img):not(.nc-or-btn-micro):not(:has(img)),
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) :is(p, h1, h2, h3, h4, h5, h6, ul, ol, li, blockquote, pre, table, .nc-hud-text, .nc-ol-btn-maxi, .nc-or-btn-maxi, br, hr),
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) :is(p, h1, h2, h3, h4, h5, h6, ul, ol, li, blockquote, pre, table, .nc-hud-text, .nc-ol-btn-maxi, .nc-or-btn-maxi, br, hr) {
  display: none !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) img,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) img,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) > img,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) > img,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) iframe,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) iframe,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) object,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) object,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) embed,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) embed {
  display: block !important;
  visibility: visible !important;
  position: absolute !important;
  top: var(--nc-image-micro-top, var(--nc-image-top, 0px)) !important;
  left: var(--nc-image-micro-left, var(--nc-image-left, 0px)) !important;
  right: auto !important;
  bottom: auto !important;
  width: var(--nc-image-micro-width, var(--nc-image-width, 100%)) !important;
  height: var(--nc-image-micro-height, var(--nc-image-height, 100%)) !important;
  max-width: none !important;
  max-height: none !important;
  margin: 0 !important;
  padding: 0 !important;
  float: none !important;
  object-fit: contain !important;
  object-position: center !important;
  border: none !important;
  box-sizing: border-box !important;
  transform: none !important;
  z-index: 0 !important;
}

.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot:not(:has(.nc-hud-media)) img ~ img,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot:not(:has(.nc-hud-media)) img ~ img {
  display: none !important;
}

/* Explicit slot-image rule deliberately avoids :has(); see HUD-01 template. */
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot .nc-hud-float-image,
.nc-ol-widget[data-collapse="micro"].is-mini .nc-hud-01-html-slot .nc-hud-slot-image,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot .nc-hud-float-image,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot .nc-hud-slot-image {
  display: block !important;
  position: absolute !important;
  top: var(--nc-image-micro-top, var(--nc-image-top, 0px)) !important;
  left: var(--nc-image-micro-left, var(--nc-image-left, 0px)) !important;
  right: auto !important;
  bottom: auto !important;
  width: var(--nc-image-micro-width, var(--nc-image-width, 100%)) !important;
  height: var(--nc-image-micro-height, var(--nc-image-height, 100%)) !important;
  max-width: none !important;
  max-height: none !important;
  margin: 0 !important;
  float: none !important;
  object-fit: contain !important;
  z-index: 0 !important;
}

.nc-ol-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-01-html-slot,
.nc-or-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-02-html-slot {
  position: absolute !important;
  inset: 0 !important;
  overflow: visible !important;
  z-index: auto !important;
}

/* Video embed fills micro slot */
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-youtube-maxi {
  width: 100% !important;
  height: 100% !important;
  margin: 0 !important;
  display: block !important;
}

.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-vertical-copy,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-heygen-copy {
  display: none !important;
}

.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-vertical-maxi,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-heygen-maxi {
  width: 100% !important;
  height: 100% !important;
  display: block !important;
  overflow: hidden !important;
}

/* Fill the whole micro frame instead of a 9:16-letterboxed box inside
   it — see the matching HUD-01 rule for the full explanation. */
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-heygen-maxi > .nc-hud-media.nc-hud-media-vertical,
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-vertical-maxi > .nc-hud-media.nc-hud-media-tiktok {
  width: var(--nc-video-micro-width, var(--nc-video-width, var(--nc-micro-w, 87px))) !important;
  min-width: 0 !important;
  max-width: 100% !important;
  height: var(--nc-video-micro-height, var(--nc-video-height, var(--nc-micro-h, 130px))) !important;
  max-height: var(--nc-video-micro-height, var(--nc-video-height, var(--nc-micro-h, 130px))) !important;
  aspect-ratio: 9 / 16 !important;
  position: relative !important;
  overflow: hidden !important;
}

.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-media {
  float: none !important;
  width: var(--nc-video-micro-width, var(--nc-video-width, 100%)) !important;
  min-width: 0 !important;
  max-width: 100% !important;
  margin: 0 !important;
  max-height: var(--nc-video-micro-height, var(--nc-video-height, var(--nc-micro-h, 130px))) !important;
  aspect-ratio: 16 / 9;
  height: var(--nc-video-micro-height, var(--nc-video-height, auto)) !important;
}
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-media-vertical {
  aspect-ratio: 9 / 16;
}
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-media iframe {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  height: 100% !important;
}

/* Some pasted Blogger blocks use an iframe/object as the image source.
   When the widget explicitly says data-content="image", size the outer
   media box with the image variables; the cross-origin iframe then fills it. */
.nc-or-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-media,
.nc-or-widget[data-collapse="micro"][style*="--nc-image-micro-width"].is-mini .nc-hud-media {
  position: absolute !important;
  top: var(--nc-image-micro-top, var(--nc-image-top, 0px)) !important;
  left: var(--nc-image-micro-left, var(--nc-image-left, 0px)) !important;
  right: auto !important;
  bottom: auto !important;
  width: var(--nc-image-micro-width, var(--nc-image-width, 100%)) !important;
  height: var(--nc-image-micro-height, var(--nc-image-height, 100%)) !important;
  max-width: none !important;
  max-height: none !important;
  transform: none !important;
  overflow: hidden !important;
  z-index: 0 !important;
}

.nc-or-widget[data-collapse="micro"][data-content="image"].is-mini .nc-hud-media :is(iframe, object, embed),
.nc-or-widget[data-collapse="micro"][style*="--nc-image-micro-width"].is-mini .nc-hud-media :is(iframe, object, embed) {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important;
  min-width: 0 !important;
  max-width: 100% !important;
  height: 100% !important;
  min-height: 0 !important;
  max-height: 100% !important;
  transform: none !important;
  box-sizing: border-box !important;
}

/* (float-image — см. img в :not(:has(.nc-hud-media)) выше) */

/* Outer h2 (direct child of widget, before panel):
   hidden in expanded, shown as label above the slot in micro mini */
.nc-or-widget[data-collapse="micro"] > .nc-or-title-micro {
  display: none !important;
}
.nc-or-widget[data-collapse="micro"].is-mini > .nc-or-title-micro {
  display: block !important;
  font-size: var(--nc-title-micro-size, 11px) !important;
  letter-spacing: .10em;
  text-align: center;
  margin: 0 0 4px;
  white-space: nowrap;
  overflow: visible;
  text-overflow: ellipsis;
  max-width: var(--nc-micro-w, 87px);
  width: auto;
  top: 0 !important;
  left: 0 !important;
  transform: translate(var(--nc-title-micro-left, 0px), var(--nc-title-micro-top, 0px)) !important;
}
.nc-or-widget[data-collapse="micro"].is-mini .nc-hud-02-html-slot .nc-or-title-maxi {
  display: none !important;
}
.nc-or-widget[data-collapse="micro"].is-mini > .nc-or-title-micro::before,
.nc-or-widget[data-collapse="micro"].is-mini > .nc-or-title-micro::after {
  display: none !important;
}

/* In collapsed micro only the direct-child micro label is allowed. */
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-panel :is(
  .nc-or-title, .nc-or-title-maxi, .nc-or-title-mini,
  .nc-or-title-micro, .nc-or-title-b, .nc-or-title-c,
  .nc-ol-title-brackets
) {
  display: none !important;
}
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-panel :is(
  .nc-or-title, .nc-or-title-maxi, .nc-or-title-mini,
  .nc-or-title-micro, .nc-or-title-b, .nc-or-title-c
)::before,
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-panel :is(
  .nc-or-title, .nc-or-title-maxi, .nc-or-title-mini,
  .nc-or-title-micro, .nc-or-title-b, .nc-or-title-c
)::after {
  display: none !important;
  content: none !important;
}

/* Toggle button in micro mini: scaled down, coords in screen px.
   top/left override prevents clipping by panel overflow:hidden.
   Per-panel vars: --nc-toggle-micro-top/left/scale (→ --nc-toggle-top/left → --nc-btn-* fallback). */
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-panel .nc-ol-toggle-btn {
  top:       var(--nc-toggle-micro-top,   var(--nc-toggle-top,  var(--nc-btn-top,  2px)));
  left:      var(--nc-toggle-micro-left,  var(--nc-toggle-left, var(--nc-btn-left, 2px)));
  transform: scale(var(--nc-toggle-micro-scale, var(--nc-btn-scale, 0.35)));
  transform-origin: top left;
}

/* Micro button: absolute within widget/panel — descendant selector handles any placement */
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-btn-micro {
  position: absolute !important;
  top:  var(--nc-btn-micro-top,  19px) !important;  /* label (~15px) + gap (4px) */
  left: var(--nc-btn-micro-left, 0px)  !important;
  z-index: 9999 !important;
}
/* Hidden in expanded state (btn-maxi inside slot handles that) */
.nc-or-widget[data-collapse="micro"]:not(.is-mini) .nc-or-btn-micro {
  display: none !important;
}

/* ── Micro size modifier classes ──────────────────────────────── */
/* Add one of these classes to the widget root alongside data-collapse="micro"
   to change the slot dimensions.  Omit the class for the 106×49 default.

   Usage:  <div class="nc-hud-02 nc-or-widget nc-micro-l" data-collapse="micro" …>

   Sizes (w × h of the image slot; total widget height = h + 19px):
     nc-micro-s    —  87 × 130 px
     (default)     —  87 × 130 px
     nc-micro-l    — 200 × 308 px                                 */

.nc-micro-s { --nc-micro-w: 87px; --nc-micro-h: 130px; }
.nc-micro-l { --nc-micro-w: 200px; --nc-micro-h: 308px; }

/* .nc-or-widget.is-mini .nc-or-panel constraints (transform, transformOrigin,
   width, height, margin, overflow) are applied as inline styles by hud-mini.js.
   No CSS rule needed — removing avoids duplication and specificity fights. */

/* Toggle button — scoped to nc-or-panel to avoid conflict with nc-ol-panel.
   Per-mode CSS vars (all optional, set on widget root):
     --nc-toggle-maxi-top/left      expanded state only
     --nc-toggle-mini-top/left/scale  mini state only (coords in UNSCALED panel space, ÷ 0.352)
     --nc-toggle-micro-top/left/scale micro state only (coords in screen px)
     --nc-toggle-top/left           shared fallback across all modes
   Old vars --nc-btn-top/left/scale remain as ultimate fallback (backward compat). */
.nc-or-panel .nc-ol-toggle-btn {
  position: absolute;
  top:  var(--nc-toggle-maxi-top,  var(--nc-toggle-top,  var(--nc-btn-top,  -8px)));
  left: var(--nc-toggle-maxi-left, var(--nc-toggle-left, var(--nc-btn-left, -5px)));
  right: auto;
  z-index: 36;
  font-family: "Share Tech Mono", monospace;
  font-size: 38px;
  line-height: 1;
  color: rgba(80, 220, 255, .98);
  background: transparent;
  border: 1px solid transparent;
  padding: 12px 22px;
  cursor: pointer;
  clip-path: polygon(
    5px 0%, calc(100% - 5px) 0%,
    100% 5px, 100% calc(100% - 5px),
    calc(100% - 5px) 100%, 5px 100%,
    0% calc(100% - 5px), 0% 5px
  );
  box-shadow: none;
  transition: border-color .18s ease, box-shadow .18s ease,
              color .18s ease, background .18s ease;
  pointer-events: all;
}

.nc-or-panel .nc-ol-toggle-btn:hover {
  color: rgba(150, 245, 255, .98);
  background: transparent;
  border-color: rgba(80, 220, 255, .75);
  box-shadow: none;
}

/* Mini mode: button scales with the 0.352× panel.
   Coords in UNSCALED panel space (÷ 0.352 to convert to screen px).
   Per-panel vars: --nc-toggle-mini-top/left/scale (→ --nc-toggle-* → --nc-btn-* fallback). */
.nc-or-widget.is-mini .nc-or-panel .nc-ol-toggle-btn {
  top:       var(--nc-toggle-mini-top,   var(--nc-toggle-top,  var(--nc-btn-top,  -8px)));
  left:      var(--nc-toggle-mini-left,  var(--nc-toggle-left, var(--nc-btn-left, -5px)));
  transform: scale(var(--nc-toggle-mini-scale, var(--nc-btn-scale, 1)));
  transform-origin: top left;
}

/* Micro mode: duplicate of the rule in the micro layout section above — kept here
   for cascade legibility. Specificity wins over the base rule and mini rule. */
.nc-or-widget[data-collapse="micro"].is-mini .nc-or-panel .nc-ol-toggle-btn {
  top:       var(--nc-toggle-micro-top,   var(--nc-toggle-top,  var(--nc-btn-top,  2px)));
  left:      var(--nc-toggle-micro-left,  var(--nc-toggle-left, var(--nc-btn-left, 2px)));
  transform: scale(var(--nc-toggle-micro-scale, var(--nc-btn-scale, 0.35)));
  transform-origin: top left;
}

/* Tooltip */
.nc-or-panel .nc-ol-toggle-btn::after {
  content: attr(data-tooltip);
  position: absolute;
  top: calc(100% + 7px);
  left: 0;
  display: none;
  white-space: nowrap;
  font-family: "Share Tech Mono", monospace;
  font-size: 9px;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: rgba(80, 220, 255, .88);
  background: rgba(0, 6, 16, .96);
  border: 1px solid rgba(80, 220, 255, .32);
  padding: 4px 10px;
  pointer-events: none;
  z-index: 50;
  box-shadow: 0 0 10px rgba(80, 220, 255, .18);
}

.nc-or-panel .nc-ol-toggle-btn:hover::after { display: block; }

/* ── Contour sweep runner (HUD-02 SVG path) ───────────────────── */

.nc-hud-02 .nc-or-sweep {
  display: none;
}

.nc-or-runner-svg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 40;
  pointer-events: none;
  overflow: visible;
}

.nc-or-runner-tail {
  fill: none;
  stroke: rgba(60, 190, 255, .13);
  stroke-width: 72;
  vector-effect: non-scaling-stroke;
  stroke-linecap: round;
  stroke-dasharray: 120 2751;
  filter: blur(32px);
  animation: ncOrSweepBar 22s linear infinite;
}

.nc-or-runner-body {
  fill: none;
  stroke: rgba(90, 210, 255, .26);
  stroke-width: 32;
  vector-effect: non-scaling-stroke;
  stroke-linecap: round;
  stroke-dasharray: 120 2751;
  filter: blur(16px);
  animation: ncOrSweepBar 22s linear infinite;
}

.nc-or-runner-head {
  fill: none;
  stroke: rgba(150, 232, 255, .55);
  stroke-width: 10;
  vector-effect: non-scaling-stroke;
  stroke-linecap: round;
  stroke-dasharray: 120 2751;
  filter: blur(3px);
  animation: ncOrSweepBar 22s linear infinite;
}

.nc-or-runner-spark {
  fill: none;
  stroke: rgba(200, 240, 255, .77);
  stroke-width: 2;
  vector-effect: non-scaling-stroke;
  stroke-linecap: round;
  stroke-dasharray: 120 2751;
  filter:
    drop-shadow(0 0 3px  rgba(180, 240, 255, .80))
    drop-shadow(0 0 10px rgba(80,  200, 255, .64));
  animation: ncOrSweepBar 22s linear infinite;
}

/* .nc-hud-row + widget sizing → blogger-hud-shared.css */

/* ── Mobile ───────────────────────────────────────────────────── */

@media (max-width: 900px) {
  .nc-or-widget { padding: 12px; }

  .nc-or-panel {
    height: auto;
    grid-template-columns: 1fr;
  }

  .nc-or-sweep,
  .nc-or-frame-svg { display: none; }

  .nc-or-left {
    min-width: 0;
    width: 100%;
    height: 320px;
  }

  .nc-or-image {
    left: 0; top: 0;
    width: 100%; height: 320px;
  }

  .nc-or-reticle {
    left: 0; top: 0;
    width: 100%; height: 320px;
  }

  .nc-or-right {
    min-width: 0;
    height: auto;
    padding: 28px 22px 30px;
  }

  .nc-or-title { font-size: 22px; }

  .nc-or-grid {
    grid-template-columns: 100px minmax(0, 1fr);
    column-gap: 12px;
  }
}

/* ── Keyframes — HUD-02 local ─────────────────────────────────── */

@keyframes ncOrLoader1 {
  0%           { opacity: 0; }
  13%          { opacity: 1; }
  82%          { opacity: 1; }
  82.01%, 100% { opacity: 0; }
}
@keyframes ncOrLoader2 {
  0%, 6.5%     { opacity: 0; }
  19.5%        { opacity: 1; }
  82%          { opacity: 1; }
  82.01%, 100% { opacity: 0; }
}
@keyframes ncOrLoader3 {
  0%, 13%      { opacity: 0; }
  26%          { opacity: 1; }
  82%          { opacity: 1; }
  82.01%, 100% { opacity: 0; }
}
@keyframes ncOrLoader4 {
  0%, 19.5%    { opacity: 0; }
  32.5%        { opacity: 1; }
  82%          { opacity: 1; }
  82.01%, 100% { opacity: 0; }
}
@keyframes ncOrLoader5 {
  0%, 26%      { opacity: 0; }
  39%          { opacity: 1; }
  82%          { opacity: 1; }
  82.01%, 100% { opacity: 0; }
}
@keyframes ncOrLoader6 {
  0%, 32.5%    { opacity: 0; }
  45.5%        { opacity: 1; }
  82%          { opacity: 1; }
  82.01%, 100% { opacity: 0; }
}
@keyframes ncOrLoader7 {
  0%, 39%      { opacity: 0; }
  52%          { opacity: 1; }
  82%          { opacity: 1; }
  82.01%, 100% { opacity: 0; }
}
@keyframes ncOrLoader8 {
  0%, 45.5%    { opacity: 0; }
  58.5%        { opacity: 1; }
  82%          { opacity: 1; }
  82.01%, 100% { opacity: 0; }
}

@keyframes hudSpinner02 {
  to { transform: rotate(360deg); }
}

@keyframes ncOrOpenHud {
  from { width: 120px; opacity: .65; }
  to   { width: min(1200px, 100%); opacity: 1; }
}

@keyframes ncOrSweepBar {
  from { stroke-dashoffset: 0; }
  to   { stroke-dashoffset: calc(-1 * var(--nc-or-runner-p, 2871px)); }
}
/*]]>*/
</style>

<!-- NebulaCast HUD: Panel CSS — HUD-03 (nc-hp-*) -->
<style>
/*<![CDATA[*/
/* ================================================================
   NebulaCast HUD Panel — Blogger Template CSS
   Version : 1.0  (2026-05-24)
   Covers  : HUD-03 (text + image)  and  HUD-04 (text + video)
   Scope   : All nc-hp-* and nc-hud-* classes — safe for Blogger,
             no global resets, no :root overrides.

   WHERE TO PUT THIS:
   Blogger → Theme → Edit HTML
   Paste the <style> block from optional/blogger-template-snippet.txt
   just before the closing </head> tag.
   ================================================================ */

@import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap');

/* ── Box-sizing (scoped — does not touch Blogger theme) ────────── */

.nc-hp-widget,
.nc-hp-widget * {
  box-sizing: border-box;
}

/* ================================================================
   KEYFRAMES
   ================================================================ */

/* Panel open: stretches from center on load */
@keyframes ncHpOpenHud {
  from { transform: scaleX(.1); opacity: .65; }
  to   { transform: scaleX(1);  opacity: 1;   }
}

/* Ambient glow breathing */
@keyframes hudBreath {
  0%, 100% {
    box-shadow:
      0 0 18px rgba(0,220,255,.18),
      0 0 42px rgba(0,140,255,.10),
      inset 0 0 28px rgba(0,255,255,.08);
  }
  50% {
    box-shadow:
      0 0 28px rgba(0,240,255,.28),
      0 0 64px rgba(0,180,255,.16),
      inset 0 0 42px rgba(0,255,255,.12);
  }
}

/* Diagonal light-beam sweep */
@keyframes hudPanelSweep {
  0%   { transform: rotate(8deg) translateX(-320%); }
  100% { transform: rotate(8deg) translateX(900%); }
}

/* Scrolling ticker: travels left, snaps back */
@keyframes hudTickerScroll {
  0%   { transform: translateX(0);                   opacity: 1; }
  97%  { transform: translateX(calc(-100% - 100vw)); opacity: 1; }
  98%  { opacity: 0; }
  100% { transform: translateX(0);                   opacity: 0; }
}

/* 8-segment sequential loader */
@keyframes ncHpLoader1 {
  0%           { opacity: 0; } 13%          { opacity: 1; }
  82%          { opacity: 1; } 82.01%, 100% { opacity: 0; }
}
@keyframes ncHpLoader2 {
  0%, 6.5%     { opacity: 0; } 19.5%        { opacity: 1; }
  82%          { opacity: 1; } 82.01%, 100% { opacity: 0; }
}
@keyframes ncHpLoader3 {
  0%, 13%      { opacity: 0; } 26%          { opacity: 1; }
  82%          { opacity: 1; } 82.01%, 100% { opacity: 0; }
}
@keyframes ncHpLoader4 {
  0%, 19.5%    { opacity: 0; } 32.5%        { opacity: 1; }
  82%          { opacity: 1; } 82.01%, 100% { opacity: 0; }
}
@keyframes ncHpLoader5 {
  0%, 26%      { opacity: 0; } 39%          { opacity: 1; }
  82%          { opacity: 1; } 82.01%, 100% { opacity: 0; }
}
@keyframes ncHpLoader6 {
  0%, 32.5%    { opacity: 0; } 45.5%        { opacity: 1; }
  82%          { opacity: 1; } 82.01%, 100% { opacity: 0; }
}
@keyframes ncHpLoader7 {
  0%, 39%      { opacity: 0; } 52%          { opacity: 1; }
  82%          { opacity: 1; } 82.01%, 100% { opacity: 0; }
}
@keyframes ncHpLoader8 {
  0%, 45.5%    { opacity: 0; } 58.5%        { opacity: 1; }
  82%          { opacity: 1; } 82.01%, 100% { opacity: 0; }
}

/* ================================================================
   SWEEP BEAM (base — shared by all HUD panels)
   Future: replace with SVG contour runner per HUD03 spec §10
   ================================================================ */

.nc-panel-sweep {
  position: absolute;
  top: -38%;
  left: -32%;
  width: 58%;
  height: 180%;
  pointer-events: none;
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(80,220,255,0) 8%,
    rgba(80,220,255,.06) 25%,
    rgba(180,255,255,.22) 50%,
    rgba(80,220,255,.08) 75%,
    transparent 100%
  );
  transform: rotate(8deg) translateX(-320%);
  mix-blend-mode: screen;
  animation: hudPanelSweep 7s linear infinite;
}

/* ================================================================
   MINI / EXPAND TOGGLE
   NcHudMini.init() (blogger-hud03-template.js) adds
   .nc-mini-widget and .nc-mini-panel to the DOM.
   ================================================================ */

.nc-mini-widget {
  overflow: hidden;
  transition: max-height 700ms cubic-bezier(.2, .9, .2, 1);
}
.nc-mini-widget.is-mini { cursor: pointer; }

.nc-mini-panel {
  transform-origin: top center;
  transition: transform 700ms cubic-bezier(.2, .9, .2, 1);
}

/* Toggle button — anchored to top-left of .nc-hp-panel */
.nc-hp-widget .nc-ol-toggle-btn {
  display: inline-flex;
  align-items: center;
  vertical-align: middle;
  position: absolute;    /* fixed to panel regardless of content height */
  top:    43px;
  left:   40px;
  right:  auto;
  bottom: auto;
  z-index: 36;
  font-family: "Share Tech Mono", monospace;
  font-size: 24px;
  line-height: 1;
  color: rgba(80, 220, 255, .98);
  background: transparent;
  border: 1px solid transparent;
  padding: 2px 10px 2px 10px;
  cursor: pointer;
  clip-path: none;
  box-shadow: none;
  transition: border-color .18s ease, color .18s ease;
  pointer-events: all;
}
.nc-hp-widget .nc-ol-toggle-btn:hover {
  color: rgba(150, 245, 255, .98);
  border-color: rgba(80, 220, 255, .75);
}
/* HUD tooltip on hover */
.nc-hp-widget .nc-ol-toggle-btn::after {
  content: attr(data-tooltip);
  position: absolute;
  top: calc(100% + 7px);
  right: 0;
  display: none;
  white-space: nowrap;
  font-family: "Share Tech Mono", monospace;
  font-size: 9px;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: rgba(80, 220, 255, .88);
  background: rgba(0, 6, 16, .96);
  border: 1px solid rgba(80, 220, 255, .32);
  padding: 4px 10px;
  pointer-events: none;
  z-index: 50;
  box-shadow: 0 0 10px rgba(80, 220, 255, .18);
}
.nc-hp-widget .nc-ol-toggle-btn:hover::after { display: block; }

/* ================================================================
   WIDGET ROOT
   ================================================================ */

.nc-hp-widget {
  width: 100%;
  margin: 0 auto;
  background: #000;
  overflow: clip;
  position: relative;
}

/* ================================================================
   MAIN PANEL
   ================================================================ */

.nc-hp-panel {
  /* Geometry tokens — adjust here to tune proportions */
  --loader-left:        28%;
  --loader-width:       230px;
  --frame-edge:         26px;
  --top-line-y:         18px;
  --top-line-drop:      26px;
  --top-line-lower-y:   calc(var(--top-line-y) + var(--top-line-drop));

  position: relative;
  width: min(1200px, 100%);
  min-height: 520px;
  /* !important: bare <div>, Blogger's ".post div { margin: 0 0 .75em }"
     (0,1,1) otherwise beats this rule's (0,1,0) and replaces
     "0 auto" — losing the auto-centering on any post column wider than
     1200px, plus adding a stray bottom margin. */
  margin: 0 auto !important;
  padding: 86px 92px;
  background: #000;
  overflow: visible;
  isolation: isolate;

  opacity: .65;
  animation:
    ncHpOpenHud 1.2s cubic-bezier(.2,.9,.2,1) forwards,
    hudBreath 5s ease-in-out infinite;
}

/* ================================================================
   FRAME DECORATIONS
   Hide on mobile — content remains readable (see @media below)
   ================================================================ */

/* Outer border with angled clip.
   margin !important: uses inset (top AND bottom both set), so a stray
   ".post div" margin-bottom doesn't just offset this div — it shrinks
   its computed height, pulling the frame short of the bottom edge it's
   meant to reach. Same story for .nc-hp-frame-inner and
   .nc-hp-frame-topline below. */
.nc-hp-frame-shell {
  position: absolute;
  inset: 26px;
  margin: 0 !important;
  z-index: 1;
  pointer-events: none;
  border: 3px solid rgba(80,220,255,.92);
  clip-path: polygon(
    0 22%, 0 100%, 96% 100%,
    100% 72%, 100% 6%,
    21% 6%, 18% 5%, 12% 5%
  );
  filter:
    drop-shadow(0 0 6px rgba(80,220,255,.9))
    drop-shadow(0 0 18px rgba(80,220,255,.5));
}

/* Black masks that cover the frame corners to create the notch effect */
.nc-hp-frame-mask {
  position: absolute;
  z-index: 3;
  pointer-events: none;
  background: #000;
}
.nc-hp-frame-mask-left {
  top: 24px; left: 118px;
  width: 170px; height: 34px;
  transform: skewX(-32deg);
}
.nc-hp-frame-mask-right {
  top: 24px; right: 18px;
  width: 46px; height: 48px;
}

/* Inner inset border */
.nc-hp-frame-inner {
  position: absolute;
  inset: 72px 48px 72px 48px;
  margin: 0 !important;
  z-index: 1;
  pointer-events: none;
  border: 1px solid rgba(145,245,255,.62);
  clip-path: polygon(0 0, 100% 0, 100% 72%, 96% 100%, 0 100%);
  filter: drop-shadow(0 0 4px rgba(80,220,255,.7));
}

/* Top decorative line + angled slope + right drop */
.nc-hp-frame-topline {
  position: absolute;
  inset: 0;
  margin: 0 !important;
  z-index: 4;
  pointer-events: none;
  overflow: visible;
}
.nc-hp-frame-topline span {
  position: absolute;
  display: block;
  background: rgba(80,220,255,.95);
  box-shadow:
    0 0 8px rgba(80,220,255,.95),
    0 0 18px rgba(80,220,255,.45);
}
.nc-hp-topline-left {
  top: var(--top-line-y);
  left: var(--frame-edge);
  width: calc(var(--loader-left) + var(--loader-width) + 4px - var(--frame-edge));
  height: 3px;
}
.nc-hp-topline-slope {
  top: var(--top-line-y);
  left: calc(var(--loader-left) + var(--loader-width) + 4px);
  width: 34px; height: 3px;
  transform-origin: left center;
  transform: rotate(58deg);
}
.nc-hp-topline-right {
  top: var(--top-line-lower-y);
  left: calc(var(--loader-left) + var(--loader-width) + 22px);
  width: calc(100% - (var(--loader-left) + var(--loader-width) + 22px) - var(--frame-edge));
  height: 3px;
}
.nc-hp-topline-side {
  top: var(--top-line-lower-y);
  right: var(--frame-edge);
  width: 3px; height: 62px;
}

/* 8-segment skewed loader bar */
.nc-hp-frame-loader {
  position: absolute;
  top: 42px;
  left: var(--loader-left);
  width: var(--loader-width);
  height: 28px;
  z-index: 5;
  pointer-events: none;
  display: flex;
  gap: 10px;
  align-items: stretch;
  transform: skewX(32deg);
  padding: 3px;
  border: 1px solid rgba(80,220,255,.45);
  background: linear-gradient(90deg, rgba(80,220,255,.08), rgba(80,220,255,.02));
  filter: drop-shadow(0 0 8px rgba(80,220,255,.55));
}
.nc-hp-frame-loader span {
  flex: 1;
  opacity: 0;
  background: rgba(80,220,255,.95);
  box-shadow: 0 0 12px rgba(80,220,255,.85);
  animation-duration: 6.2s;
  animation-timing-function: linear;
  animation-iteration-count: infinite;
}
.nc-hp-frame-loader span:nth-child(1) { animation-name: ncHpLoader1; }
.nc-hp-frame-loader span:nth-child(2) { animation-name: ncHpLoader2; }
.nc-hp-frame-loader span:nth-child(3) { animation-name: ncHpLoader3; }
.nc-hp-frame-loader span:nth-child(4) { animation-name: ncHpLoader4; }
.nc-hp-frame-loader span:nth-child(5) { animation-name: ncHpLoader5; }
.nc-hp-frame-loader span:nth-child(6) { animation-name: ncHpLoader6; }
.nc-hp-frame-loader span:nth-child(7) { animation-name: ncHpLoader7; }
.nc-hp-frame-loader span:nth-child(8) { animation-name: ncHpLoader8; }

/* Scrolling ticker strip */
.nc-hp-frame-ticker {
  position: absolute;
  top: 50px;
  right: 54px;
  width: calc(100% - (var(--loader-left) + var(--loader-width) + 74px));
  height: 32px;
  overflow: hidden;
  z-index: 5;
  pointer-events: none;
}
.nc-hp-frame-ticker span {
  position: absolute;
  white-space: nowrap;
  left: 100%; top: 0;
  font-family: "Share Tech Mono", monospace;
  font-size: 14px;
  letter-spacing: .18em;
  text-transform: uppercase;
  color: rgba(120,245,255,.82);
  text-shadow: 0 0 8px rgba(80,220,255,.55);
  animation: hudTickerScroll 30s linear infinite;
}

/* Sweep overlay — ties into .nc-panel-sweep base */
.nc-hp-sweep {
  z-index: 2;
  opacity: .5;
}

/* ================================================================
   CONTENT
   ================================================================ */

/* margin !important for the same reason as .nc-hp-system/.nc-hp-text
   below — bare <div>, otherwise picks up Blogger's ".post div"
   margin-bottom (.75em) uncontested since this rule never declared one. */
.nc-hp-content {
  position: relative;
  z-index: 6;
  color: #cfffff;
  margin: 0 !important;
}

/* Row wrapper: button + system label, floats above the inner frame line */
.nc-hp-system-row {
  position: absolute;
  top: -36px; left: -44px;
  display: flex;
  align-items: center;
  gap: 0;
}
/* .nc-ol-toggle-btn (above) is position:absolute so it can anchor to
   .nc-hp-panel's top-left when it's a bare child of the panel. Inside
   .nc-hp-system-row it isn't — the row is itself position:absolute, so
   the button's own absolute positioning resolves against the ROW
   instead, landing top:43px/left:40px deep in the panel body rather
   than at the frame corner. Let it flow as a normal flex item instead,
   then nudge it up so it — and .nc-hp-system next to it — sit just
   above .nc-hp-frame-inner (inset 72px from the panel edge) instead of
   overlapping down into the panel body. Values arrived at empirically
   against the actual rendered rects (button and label bottom-aligned,
   button 3-4px right of .nc-hp-frame-inner's left edge). */
.nc-hp-system-row .nc-ol-toggle-btn {
  position: relative;
  top: -30px;
  left: auto;
  margin-left: 4px;
}

/* System label — inline inside .nc-hp-system-row. This is the widget's
   real "top header" (the toggle belongs next to THIS, not .nc-hp-title
   below, which is a separate description heading further down in the
   panel body).
   !important on margin/line-height: this is a bare <div>, and Blogger's
   own base theme has ".post div { margin: 0 0 .75em; line-height: 1.3em; }"
   — (0,1,1) specificity, beats this rule's (0,1,0) — which was silently
   inflating the label's line-height from 24px to 31.2px (1.3em) and
   adding an unwanted 18px bottom margin (.75em) any time this widget is
   actually inside a Blogger post's .post/.post-body. staging.html never
   reproduced this because its preview harness doesn't wrap content in
   .post, so the bug was invisible there — the two only matched once this
   was pinned down. */
.nc-hp-system {
  display: inline;
  vertical-align: middle;
  position: relative;
  /* -27px, not -21px: with line-height correctly forced to 1 (24px) the
     label is shorter than the 30px button, so align-items:center on
     .nc-hp-system-row adds a +3px offset before this shift is even
     applied. Recalibrated empirically after fixing the line-height bug
     above — the old -21px was tuned against the buggy 31.2px height and
     went stale the moment that height became correct. */
  top: -27px;
  left: 2px;
  margin: 0 !important;
  font-family: "Share Tech Mono", monospace;
  font-size: 24px;
  line-height: 1 !important;
  font-weight: bold;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: rgba(80,220,255,.72);
  text-shadow: 0 0 8px rgba(80,220,255,.45);
}

/* Character / post title */
.nc-hp-title {
  margin: 10px 0 34px 0;
  display: block;
  width: 100%;
  max-width: 100%;
  text-align: left;
  font-family: "Share Tech Mono", monospace;
  font-size: 30px;
  line-height: 1;
  letter-spacing: .18em;
  text-transform: uppercase;
  color: #8ff6ff;
  text-shadow:
    0 0 8px  rgba(80,245,255,.95),
    0 0 20px rgba(80,245,255,.45);
}

/* Body text — supports standard HTML inside.
   margin/line-height !important for the same reason as .nc-hp-system
   above: bare <div>, and Blogger's ".post div" (0,1,1) otherwise beats
   this (0,1,0), silently swapping our line-height:1.72 for 1.3em and
   adding an unwanted .75em bottom margin this rule never asked for. */
.nc-hp-text {
  max-width: 100%;
  margin: 0 !important;
  font-family: "Share Tech Mono", monospace;
  font-size: 12px;
  line-height: 1.72 !important;
  letter-spacing: .02em;
  color: rgba(190,250,255,.84);
  text-shadow: 0 0 10px rgba(80,220,255,.18);
  text-align: justify;
  text-align-last: left;
  hyphens: auto;
  overflow-wrap: anywhere;
  display: flow-root;      /* clears floated media children */
}
/* text-align-last:left above is meant for the last line of justified
   paragraph text. Blogger's image/embed wrapper (.separator) sets its own
   text-align:center inline — but since it's a single-line block, that one
   line IS its "last line", so the inherited text-align-last:left silently
   wins and the image sits flush left instead of centered. Reset it here so
   .separator's own centering takes effect. */
.nc-hp-text .separator {
  text-align-last: auto;
}
/* Blogger inserts images with a fixed width= attribute (usually 640px,
   whatever size was picked in the image-insert dialog). The panel itself
   is up to 1200px wide, so a plain 640px graphic ends up small and
   centered with a lot of dead frame space on both sides. Let it fill the
   available content width instead — height:auto keeps the aspect ratio.
   For a crisp result at full width, pick "X-Large" or "Original" size
   when inserting the image in Blogger rather than the 640px default. */
.nc-hp-text .separator img {
  width: 100%;
  height: auto;
}
.nc-hp-text::after {
  content: "";
  display: block;
  clear: both;
}

/* ================================================================
   MEDIA SLOTS  (public API — use these class names in your posts)

   nc-hud-float-image        → static image, floated left
   nc-hud-media              → wrapper for any embedded media
     + nc-hud-media-vertical   → portrait aspect (HeyGen / TikTok / Shorts)
     + nc-hud-media-horizontal → landscape aspect (YouTube / Vimeo)
   ================================================================ */

/* --- Static image -------------------------------------------- */
.nc-hud-float-image {
  float: left;
  width: 28%;
  min-width: 220px;
  max-width: 360px;
  height: auto;
  margin: 0 34px 22px 0;
  object-fit: contain;
  filter: brightness(.76) contrast(1.14) saturate(.9);
  border: 1px solid rgba(80,220,255,.36);
  box-shadow:
    0 0 16px rgba(80,220,255,.18),
    inset 0 0 18px rgba(80,220,255,.08);
}

/* --- Embedded media container ------------------------------- */
.nc-hud-media {
  float: left;
  position: relative;
  top: 0;
  left: -14px;
  overflow: hidden;
  border: 1px solid rgba(80,220,255,.36);
  background: rgba(0,20,34,.55);
  box-shadow:
    0 0 16px rgba(80,220,255,.18),
    inset 0 0 18px rgba(80,220,255,.08);
}
.nc-hud-media-vertical {
  width: 28%;
  min-width: 220px;
  max-width: 360px;
  aspect-ratio: 9 / 16;
}
.nc-hud-media-horizontal {
  width: 42%;
  min-width: 320px;
  max-width: 620px;
  aspect-ratio: 16 / 9;
}
.nc-hud-media iframe,
.nc-hud-media video,
.nc-hud-media img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  border: 0;
  object-fit: cover;
}
/* Smooth reveal after src swap */
.nc-hud-media iframe {
  position: relative;
  z-index: 1;
  transition: opacity 0.4s ease;
}
.nc-hud-media iframe[src="about:blank"] { opacity: 0; }

/* "RECONNECTING…" placeholder — shown while iframe loads */
.nc-reconnect {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: "Share Tech Mono", monospace;
  font-size: 11px;
  letter-spacing: .22em;
  text-transform: uppercase;
  color: rgba(80,220,255,.45);
  text-shadow: 0 0 8px rgba(80,220,255,.3);
  pointer-events: none;
  z-index: 0;
}

/* ================================================================
   MINI CARD  (shown in collapsed state)
   ================================================================ */

.nc-hp-mini-card {
  position: absolute;
  top: 0; left: 0;
  transform: none;
  width: fit-content;
  min-width: 240px;
  max-width: min(420px, 90%);
  height: 148px;
  background: #000;
  overflow: hidden;
  z-index: 10;
  opacity: 0;
  pointer-events: none;
  transition: opacity 300ms ease;
}
/* HUD frame border on mini card */
.nc-hp-mini-card::before {
  content: '';
  position: absolute;
  inset: 8px;
  border: 2.5px solid rgba(80,220,255,.92);
  box-shadow:
    0 0 8px  rgba(80,220,255,.9),
    0 0 20px rgba(80,220,255,.4),
    inset 0 0 12px rgba(80,220,255,.06);
  pointer-events: none;
  clip-path: polygon(
    12% 0, 100% 0, 100% 74%, 96% 100%, 0 100%, 0 22%
  );
}
/* Expand arrow hint */
.nc-hp-mini-card::after {
  content: '▼';
  position: absolute;
  top: 17px; left: 12px; right: auto;
  font-family: 'Share Tech Mono', monospace;
  font-size: 13px; line-height: 1;
  color: rgba(80,220,255,.75);
  text-shadow: 0 0 8px rgba(80,220,255,.6);
  z-index: 5;
}
.nc-mini-widget.is-mini .nc-hp-mini-card {
  opacity: 1;
  pointer-events: auto;
  cursor: pointer;
}
.nc-mini-widget.is-mini .nc-hp-panel { visibility: hidden; }

/* System label inside mini card */
.nc-hp-mini-system {
  position: absolute;
  top: 18px; left: 24px;
  margin: 0;
  font-family: 'Share Tech Mono', monospace;
  font-size: 10px;
  letter-spacing: .22em;
  text-transform: uppercase;
  color: rgba(80,220,255,.65);
  text-shadow: 0 0 8px rgba(80,220,255,.4);
  z-index: 4;
}
/* Row: thumbnail + name.
   margin !important: another bare <div> vulnerable to Blogger's own
   ".post div { margin: 0 0 .75em }" — see .nc-hp-system above. Clipped
   by .nc-hp-mini-card's overflow:hidden either way, but the stray
   margin still pushed the flex row (and everything in it) up off-center
   inside the fixed 148px card height. */
.nc-hp-mini-body {
  display: flex;
  align-items: center;
  gap: 20px;
  margin: 0 !important;
  padding: 38px 50px 16px 20px;
  height: 100%;
  box-sizing: border-box;
  z-index: 3;
}
/* Static image thumbnail.
   padding/border !important: Blogger's ".post img { padding: 4px;
   border: 1px solid $borderColor }" otherwise beats this (same
   specificity fight as the divs above), silently adding padding this
   thumbnail never wanted and swapping the intended cyan border for
   Blogger's generic one. */
.nc-hp-mini-thumb {
  height: 80px; width: auto;
  max-width: none; min-width: 48px;
  object-fit: contain;
  padding: 0 !important;
  border: 1px solid rgba(80,220,255,.38) !important;
  flex-shrink: 0;
  filter: brightness(.82) contrast(1.12) saturate(.9);
}
/* Live video thumbnail (scaled-down iframe) */
.nc-hp-mini-video-wrap {
  width: 45px;
  height: 80px;
  overflow: hidden;
  flex-shrink: 0;
  position: relative;
  border: 1px solid rgba(80,220,255,.38);
  box-shadow: 0 0 8px rgba(80,220,255,.18);
}
.nc-hp-mini-video-wrap iframe {
  position: absolute;
  top: 0; left: 0;
  width: 270px; height: 480px;
  transform: scale(0.1667);
  transform-origin: top left;
  pointer-events: none;
  border: 0;
}
/* Character name in mini card */
.nc-hp-mini-name {
  margin: 0;
  white-space: normal;
  flex-shrink: 1;
  font-family: 'Share Tech Mono', monospace;
  font-size: 16px;
  line-height: 1.25;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: #8ff6ff;
  text-shadow:
    0 0 8px  rgba(80,245,255,.95),
    0 0 22px rgba(80,245,255,.42);
}

/* ================================================================
   MOBILE  (≤ 900px)
   Frame decorations hidden; content stays readable; media goes full-width
   ================================================================ */

@media (max-width: 900px) {
  .nc-hp-panel {
    min-height: 0;
    padding: 48px 24px;
  }

  /* Hide all decorative frame elements */
  .nc-hp-frame-shell,
  .nc-hp-frame-inner,
  .nc-hp-frame-loader,
  .nc-hp-frame-topline,
  .nc-hp-frame-mask,
  .nc-hp-sweep { display: none; }

  .nc-hp-title {
    font-size: 30px;
    margin-bottom: 24px;
  }
  .nc-hp-text {
    font-size: 15px;
    line-height: 1.72;
    letter-spacing: .02em;
  }

  /* Float → block, full width */
  .nc-hud-float-image {
    float: none;
    display: block;
    width: 100%;
    min-width: 0;
    max-width: none;
    margin: 0 0 24px 0;
  }
  .nc-hud-media {
    float: none;
    left: 0;
    width: 100%;
    min-width: 0;
    max-width: none;
    margin: 0 0 24px 0;
  }
}

/* ================================================================
   ROW CONTAINER — HUD-03 / HUD-04 mini-card row
   ================================================================ */

/* Collapsed slot: fixed width so mini-cards sit side by side */
.nc-hud-row .nc-hp-widget.is-mini {
  flex: 0 0 auto;
  width: 280px;
}

/* Expanded panel: full width, pushed below the mini-card row */
.nc-hud-row .nc-hp-widget:not(.is-mini) {
  flex: 0 0 100%;
  order: 99;
}
/*]]>*/
</style>

<!-- NebulaCast HUD: Shared JS — NcHudMini (mini/expand toggle) -->
<script>
/*<![CDATA[*/
/* hud-mini.js — generic mini/expand toggle for HUD panels
   Provides: window.NcHudMini.init(options)

   options = {
     widget       : HTMLElement   required  widget root
     panel        : HTMLElement   required  main panel inside widget
     miniH        : Number        optional  mini height px (default 148)
     panelW       : Number        optional  panel width in mini state (default 901)
     panelH       : Number        optional  panel height in mini state; set when panel base
                                            CSS does not fix height (e.g. html-mode HUD-01)
     scale        : Number|null   optional  scale factor; null = crop (no transform)
     expandedMaxH : Number        optional  max-height in expanded state (default 2000)
     onCollapse   : Function      optional  called immediately on collapse
     onExpand     : Function      optional  called after expand transition completes
   }

   Mini-panel constraints (width, height, transformOrigin, margin, overflow) are ALL
   applied as inline styles by this module — no per-panel CSS rules needed.
   Button transform in mini state is left entirely to per-panel CSS.
*/
'use strict';

(function (global) {

  /* requestAnimationFrame is paused by the browser for backgrounded/hidden
     tabs (and can be throttled in other cases). The FLIP expand/collapse
     below schedules its *entire* completion path — the transitionend
     listener and its own timeout fallback — inside a single rAF callback,
     so if that rAF never fires the panel gets stuck mid-animation forever
     (visually: click does nothing, or the widget stays pinned at its mini
     size). Race rAF against a plain setTimeout so the callback always runs
     promptly either way. */
  function rafOrTimeout(cb) {
    var done = false;
    function run() {
      if (done) return;
      done = true;
      cb();
    }
    requestAnimationFrame(run);
    setTimeout(run, 100);
  }

  function init(opts) {
    var widget       = opts.widget;
    var panel        = opts.panel;
    var miniH        = opts.miniH        || 148;
    var panelW       = opts.panelW       || 901;
    var panelH       = opts.panelH       || null;
    var scale        = opts.scale        || null;
    var expandedMaxH = opts.expandedMaxH || 2000;
    var collapseMode = widget && widget.getAttribute ? widget.getAttribute('data-collapse') : null;

    if (!widget || !panel) {
      console.warn('[NcHudMini] widget or panel not found');
      return;
    }

    /* Release templates may always pass mini opts; force correct micro geometry by attribute. */
    if (collapseMode === 'micro') {
      var cs = (typeof getComputedStyle === 'function') ? getComputedStyle(widget) : null;
      var cssMicroW = cs ? parseFloat(cs.getPropertyValue('--nc-micro-w')) : NaN;
      var cssMicroH = cs ? parseFloat(cs.getPropertyValue('--nc-micro-h')) : NaN;
      panelW = Number.isFinite(cssMicroW) ? cssMicroW : 87;
      panelH = Number.isFinite(cssMicroH) ? cssMicroH : 130;
      miniH  = panelH + 19;
      scale  = null;
    }

    /* Add transition base classes */
    widget.classList.add('nc-mini-widget');
    panel.classList.add('nc-mini-panel');

    /* Resolve micro image geometry after the real widget DOM has been
       inserted. This is intentionally inline + !important: Blogger themes,
       row layout and embed wrappers can all outrank stylesheet selectors.
       The variables stay on the widget root; 0/0 is the panel's top-left. */
    var microImageTarget = null;
    var microImageSlot = null;
    var microTitle = null;
    function applyMicroTitleState(isMini) {
      if (collapseMode !== 'micro') return;
      if (!microTitle) {
        for (var t = 0; t < widget.children.length; t++) {
          if (widget.children[t].classList &&
              (widget.children[t].classList.contains('nc-ol-title-micro') ||
               widget.children[t].classList.contains('nc-or-title-micro'))) {
            microTitle = widget.children[t];
            break;
          }
        }
      }
      if (!microTitle) return;
      if (!isMini) {
        microTitle.style.setProperty('display', 'none', 'important');
        return;
      }
      var titleCss = getComputedStyle(widget);
      var titleTop = titleCss.getPropertyValue('--nc-title-micro-top').trim() || '0px';
      var titleLeft = titleCss.getPropertyValue('--nc-title-micro-left').trim() || '0px';
      microTitle.style.setProperty('display', 'block', 'important');
      microTitle.style.setProperty('position', 'relative', 'important');
      microTitle.style.setProperty('top', '0', 'important');
      microTitle.style.setProperty('left', '0', 'important');
      microTitle.style.setProperty('transform', 'translate(' + titleLeft + ',' + titleTop + ')', 'important');
    }
    function applyMicroImageGeometry() {
      if (collapseMode !== 'micro') return;
      microImageSlot = panel.querySelector('.nc-hud-01-html-slot, .nc-hud-02-html-slot');
      if (!microImageSlot) return;

      var images = microImageSlot.querySelectorAll('img');
      for (var i = 0; i < images.length; i++) {
        if (!images[i].closest('.nc-hud-media')) { microImageTarget = images[i]; break; }
      }
      if (!microImageTarget) {
        microImageTarget = microImageSlot.querySelector('.nc-hud-media, iframe, object, embed');
      }
      if (!microImageTarget) return;

      var rootCss = getComputedStyle(widget);
      function rootVar(primary, fallback, defaultValue) {
        return rootCss.getPropertyValue(primary).trim() ||
          rootCss.getPropertyValue(fallback).trim() || defaultValue;
      }
      microImageSlot.style.setProperty('position', 'absolute', 'important');
      microImageSlot.style.setProperty('inset', '0', 'important');
      microImageSlot.style.setProperty('width', '100%', 'important');
      microImageSlot.style.setProperty('height', panelH + 'px', 'important');
      microImageSlot.style.setProperty('overflow', 'visible', 'important');

      microImageTarget.style.setProperty('position', 'absolute', 'important');
      microImageTarget.style.setProperty('top', rootVar('--nc-image-micro-top', '--nc-image-top', '0px'), 'important');
      microImageTarget.style.setProperty('left', rootVar('--nc-image-micro-left', '--nc-image-left', '0px'), 'important');
      microImageTarget.style.setProperty('right', 'auto', 'important');
      microImageTarget.style.setProperty('bottom', 'auto', 'important');
      microImageTarget.style.setProperty('width', rootVar('--nc-image-micro-width', '--nc-image-width', '100%'), 'important');
      microImageTarget.style.setProperty('height', rootVar('--nc-image-micro-height', '--nc-image-height', '100%'), 'important');
      microImageTarget.style.setProperty('min-width', '0', 'important');
      microImageTarget.style.setProperty('max-width', 'none', 'important');
      microImageTarget.style.setProperty('min-height', '0', 'important');
      microImageTarget.style.setProperty('max-height', 'none', 'important');
      microImageTarget.style.setProperty('transform', 'none', 'important');
      microImageTarget.style.setProperty('z-index', '0', 'important');
    }

    /* ── Toggle button ───────────────────────────────────────────── */
    var btn = document.createElement('button');
    btn.className = 'nc-ol-toggle-btn';
    btn.type      = 'button';
    /* HUD-03 compatibility: wrap button + system label in nc-hp-system-row.
       For all other panels (no .nc-hp-system) falls back to panel.appendChild. */
    var sysEl = panel.querySelector('.nc-hp-system');
    if (sysEl && sysEl.parentNode) {
      var sysRow = document.createElement('div');
      sysRow.className = 'nc-hp-system-row';
      sysEl.parentNode.insertBefore(sysRow, sysEl);
      sysRow.appendChild(btn);
      sysRow.appendChild(sysEl);
    } else {
      panel.appendChild(btn);
    }

    function updateBtn() {
      var mini = widget.classList.contains('is-mini');
      btn.textContent = mini ? '▼' : '▲';
      btn.setAttribute('aria-label',    mini ? 'Expand HUD panel'   : 'Collapse HUD panel');
      btn.setAttribute('aria-expanded', mini ? 'false'              : 'true');
      btn.setAttribute('data-tooltip',  mini ? 'Развернуть панель'  : 'Свернуть панель');
    }

    /* ── Mini-panel constraints ──────────────────────────────────── */
    /* Applied as inline styles — works for any panel type without
       per-type CSS rules.  Button transform is left to CSS.
       IMPORTANT: width/maxWidth/height use setProperty('…','important')
       because CSS animations (e.g. the HUD-02 ncOrOpenHud width animation)
       have higher cascade priority than normal inline styles but are
       overridden by !important inline styles. */
    function applyMiniConstraints() {
      if (scale) panel.style.transform = 'scale(' + scale + ')';
      panel.style.transformOrigin = 'top left';
      panel.style.setProperty('width',     panelW + 'px', 'important');
      panel.style.setProperty('max-width', 'none',        'important');
      if (panelH) panel.style.setProperty('height', panelH + 'px', 'important');
      panel.style.margin   = '0';
      /* micro (scale=null): keep overflow visible so the SVG frame drop-shadow
         can paint beyond the 130px panel edge; mini uses hidden to crop the thumbnail */
      panel.style.overflow = scale ? 'hidden' : 'visible';
      /* The WIDGET (not just the panel) inherits margin:0 auto from its base
         (maxi, width:100%) rule — a no-op there, but once .is-mini gives it a
         fixed flex-basis width, auto absorbs the row's free space and
         centers it instead of leaving it flush left at flex-start. Force it
         off explicitly rather than relying on per-panel-type CSS to do it. */
      widget.style.margin = '0';
      applyMicroTitleState(true);
      applyMicroImageGeometry();
    }

    function clearMiniConstraints() {
      panel.style.transform       = '';
      panel.style.transformOrigin = '';
      panel.style.removeProperty('width');
      panel.style.removeProperty('max-width');
      if (panelH) panel.style.removeProperty('height');
      panel.style.margin   = '';
      panel.style.overflow = '';
      widget.style.margin  = '';
      applyMicroTitleState(false);
      if (collapseMode === 'micro' && microImageTarget) {
        ['position','top','left','right','bottom','width','height','min-width',
         'max-width','min-height','max-height','transform','z-index'].forEach(function (prop) {
          microImageTarget.style.removeProperty(prop);
        });
      }
      /* applyMicroImageGeometry() also pins microImageSlot itself (the
         .nc-hud-0X-html-slot) to position:absolute + a fixed micro height
         — inline !important, so it outranks any expanded/html-mode CSS
         rule regardless of specificity. Only the *target* image/media's
         styles were being cleared above; the slot's own pin never was,
         so the slot (and anything that needs it back in normal flow —
         e.g. a HeyGen video + text row) stayed frozen at the micro height
         after expanding, even though .is-mini was gone. */
      if (collapseMode === 'micro' && microImageSlot) {
        ['position','inset','width','height','overflow'].forEach(function (prop) {
          microImageSlot.style.removeProperty(prop);
        });
      }
    }

    /* ── Expand (FLIP) ───────────────────────────────────────────── */
    /* No opacity trick — the panel stays visible throughout.

       mini (scale≠null) — translate + scale FLIP:
         FIRST  = mini thumbnail rect.
         Switch to expanded layout (keep width constraint so LAST has same
         width as FIRST — only the position delta matters).
         LAST   = expanded position with same width.
         INVERT = translate(dx,dy) scale(miniScale) → panel appears at mini spot.
         PLAY   = animate transform → '' (natural expanded state).

       micro (scale=null) — scale-spread FLIP:
         FIRST  = micro thumbnail rect.
         Clear constraints BEFORE measuring LAST so the panel can reach its
         full natural size.  Scale factor = micro / expanded.
         INVERT = translate(dx,dy) scale(scaleX,scaleY) with origin top-left
                  → panel appears shrunken at micro position.
         PLAY   = animate transform → '' → panel "spreads" outward, no slide. */
    function expand() {
      /* Hide the external micro label before the FIRST FLIP measurement.
         Waiting for class removal/constraint cleanup lets it flash for one
         frame alongside the maxi title during the opening transition. */
      applyMicroTitleState(false);

      /* FIRST — current mini/micro visual bounding box */
      var first = panel.getBoundingClientRect();

      /* Allow panel to extend outside widget slot during animation */
      widget.style.overflow = 'visible';

      /* Switch to expanded layout */
      widget.classList.remove('is-mini');
      widget.style.maxHeight = expandedMaxH + 'px';
      widget.style.cursor    = '';
      btn.style.transform       = '';
      btn.style.transformOrigin = '';
      updateBtn();

      panel.style.transition = 'none';

      if (scale) {
        /* ── mini: translate + scale FLIP ───────────────────────── */
        panel.style.setProperty('width', panelW + 'px', 'important');
        panel.style.margin          = '0';
        panel.style.transformOrigin = 'top left';
        /* Clear the mini scale() BEFORE measuring "last" — otherwise
           getBoundingClientRect() still reports the scaled-down (mini)
           visual size even though width was just set to the full panelW,
           making last ≈ first. dx/dy then come out ≈0 and the FLIP
           invert step re-applies the old mini transform, so the panel
           never visually grows — it gets stuck at the mini scale
           (observed as: click does nothing / layout stays collapsed). */
        panel.style.transform = '';

        var last = panel.getBoundingClientRect();
        var dx   = first.left - last.left;
        var dy   = first.top  - last.top;

        panel.style.transform = 'translate(' + dx + 'px,' + dy + 'px) scale(' + scale + ')';
        panel.offsetHeight;   /* force reflow — commits inverted state */

        rafOrTimeout(function () {
          panel.style.transition = 'transform 600ms cubic-bezier(.2,.9,.2,1)';
          panel.style.transform  = '';   /* animate to natural expanded state */

          var fired = false;
          function onExpanded() {
            if (fired) return;
            if (widget.classList.contains('is-mini')) return;
            fired = true;
            panel.style.transition = '';
            clearMiniConstraints();
            widget.style.overflow  = '';
            if (typeof opts.onExpand === 'function') opts.onExpand();
          }

          var handled = false;
          function onTransitionEnd(e) {
            if (e.propertyName !== 'transform') return;
            panel.removeEventListener('transitionend', onTransitionEnd);
            handled = true;
            onExpanded();
          }
          panel.addEventListener('transitionend', onTransitionEnd);
          setTimeout(function () {
            if (!handled) {
              panel.removeEventListener('transitionend', onTransitionEnd);
              onExpanded();
            }
          }, 800);
        });

      } else {
        /* ── micro: scale-spread FLIP ───────────────────────────── */
        /* Clear constraints first so LAST reflects the full expanded size */
        clearMiniConstraints();

        var last = panel.getBoundingClientRect();
        var dx   = first.left - last.left;
        var dy   = first.top  - last.top;

        var scaleX = first.width  / Math.max(last.width,  1);
        var scaleY = first.height / Math.max(last.height, 1);

        /* Origin top-left → panel spreads rightward + downward from the
           micro thumbnail's corner instead of sliding in from off-screen */
        panel.style.transformOrigin = 'top left';
        panel.style.transform =
          'translate(' + dx + 'px,' + dy + 'px) scale(' + scaleX + ',' + scaleY + ')';
        panel.offsetHeight;   /* force reflow */

        rafOrTimeout(function () {
          panel.style.transition = 'transform 600ms cubic-bezier(.2,.9,.2,1)';
          panel.style.transform  = '';   /* animate: spread to natural expanded state */

          var fired = false;
          function onExpanded() {
            if (fired) return;
            if (widget.classList.contains('is-mini')) return;
            fired = true;
            panel.style.transition      = '';
            panel.style.transformOrigin = '';
            widget.style.overflow       = '';
            if (typeof opts.onExpand === 'function') opts.onExpand();
          }

          var handled = false;
          function onTransitionEnd(e) {
            if (e.propertyName !== 'transform') return;
            panel.removeEventListener('transitionend', onTransitionEnd);
            handled = true;
            onExpanded();
          }
          panel.addEventListener('transitionend', onTransitionEnd);
          setTimeout(function () {
            if (!handled) {
              panel.removeEventListener('transitionend', onTransitionEnd);
              onExpanded();
            }
          }, 800);
        });
      }
    }

    /* ── Collapse (forward-collapse) ────────────────────────────── */
    /* _instant = true  → sync, no animation (used at init time).
       _instant = false → forward-collapse:
         1. Estimate the mini/micro slot position from row siblings.
         2. Apply panel size constraints (mini only) hidden by opacity:0 — so
            the scale factor produces the correct thumbnail size throughout.
         3. Animate the panel from its current expanded position TOWARD the
            mini/micro slot (translate + scale) while staying in the expanded
            flex slot.  No layout change during animation, no floating panel,
            no Blogger-container clipping.
         4. At transitionend: brief opacity:0 hides the layout snap
            (widget.classList.add('is-mini') + applyMiniConstraints).
            Then restore opacity — panel is now in the correct mini/micro state. */
    function collapse(_instant) {
      if (!_instant && typeof opts.onCollapse === 'function') opts.onCollapse();

      if (_instant) {
        widget.classList.add('is-mini');
        widget.style.maxHeight = miniH + 'px';
        /* micro (scale=null): visible so SVG frame glow isn't clipped */
        widget.style.overflow  = scale ? 'hidden' : 'visible';
        widget.style.cursor    = 'pointer';
        applyMiniConstraints();
        updateBtn();
        return;
      }

      /* ── 1. Estimate mini slot position from flex-row siblings ──── */
      /* Widget is currently order:99 at the bottom.  Mini position =
         sum of widths of all is-mini siblings that come before it in
         DOM order (the row has gap:0 so there is no inter-item gap). */
      var par     = widget.parentElement;
      var parRect = par ? par.getBoundingClientRect() : { left: 0, top: 0 };
      var slotLeft = parRect.left;
      var slotTop  = parRect.top;

      if (par) {
        var ch = par.children;
        /* slotTop: use the first mini sibling's top for pixel accuracy */
        var firstMini = null;
        for (var i = 0; i < ch.length; i++) {
          if (ch[i] !== widget && ch[i].classList.contains('is-mini')) {
            if (!firstMini) firstMini = ch[i];
            if (ch[i] === widget) break;
          }
          if (ch[i] === widget) break;
          if (ch[i].classList.contains('is-mini')) {
            slotLeft += ch[i].getBoundingClientRect().width;
          }
        }
        if (firstMini) slotTop = firstMini.getBoundingClientRect().top;
        /* If no mini siblings exist yet, slotLeft/slotTop are still at their
           pre-loop values (parRect.left/top) — which is correct: .nc-hud-row
           is flex + align-items:flex-start with no justify-content, so a
           lone mini widget ends up flush left, not centered. An earlier
           version of this code re-centered slotLeft here for that case,
           but that doesn't match the real CSS layout — the panel would fly
           toward the row's center, then snap left the instant .is-mini
           landed and the real flex rules took over. Do nothing; the
           defaults already agree with the CSS. */
      }

      /* ── 2. Capture unconstrained expanded rect ────────────────── */
      var firstRaw = panel.getBoundingClientRect();

      /* ── 3. Apply size constraints (mini mode only) ────────────── */
      /* For mini: constrain panel to panelW so scale(miniScale) produces
         the exact thumbnail size.  Hidden by brief opacity:0. */
      widget.style.transition = 'none';
      panel.style.transition  = 'none';
      widget.style.opacity    = '0';
      widget.offsetHeight;                   /* reflow — commits opacity:0 */

      if (scale) {
        panel.style.setProperty('width',     panelW + 'px', 'important');
        panel.style.setProperty('max-width', 'none',        'important');
        if (panelH) panel.style.setProperty('height', panelH + 'px', 'important');
        panel.style.margin = '0';
      }
      panel.style.transformOrigin = 'top left';
      panel.style.overflow        = 'visible';  /* prevent clip during animation */
      panel.offsetHeight;                    /* reflow — commits constraints */

      var first = panel.getBoundingClientRect();  /* post-constraint expanded position */
      var dx    = slotLeft - first.left;
      var dy    = slotTop  - first.top;

      /* Hide expanded-slot background so panel animation is clean */
      widget.style.background = 'transparent';
      /* Raise widget above sibling row panels so the shrinking panel
         is never occluded by the mini-row background during animation */
      widget.style.zIndex     = '9999';

      /* ── 4. Restore opacity — user sees animation start ─────────── */
      widget.style.opacity    = '';
      widget.style.transition = '';
      /* Allow panel to leave widget's bounds as it flies to the mini slot.
         Without this the widget's CSS overflow:hidden clips the panel the
         moment it crosses the widget edge. */
      widget.style.overflow   = 'visible';
      panel.style.transition  = 'transform 600ms cubic-bezier(.42,0,.58,1)';

      rafOrTimeout(function () {
        if (scale) {
          /* mini: translate to slot + scale to mini thumbnail */
          panel.style.transform =
            'translate(' + dx + 'px,' + dy + 'px) scale(' + scale + ')';
        } else {
          /* micro: translate to slot + scale from expanded size to micro crop */
          var targetW = panelW || firstRaw.width;
          var targetH = panelH || firstRaw.height;
          var sX = targetW / Math.max(firstRaw.width,  1);
          var sY = targetH / Math.max(firstRaw.height, 1);
          panel.style.transform =
            'translate(' + dx + 'px,' + dy + 'px) scale(' + sX + ',' + sY + ')';
        }

        var colHandled = false;

        function doSnap() {
          /* Brief opacity:0 hides the layout snap at animation end */
          widget.style.opacity    = '0';
          widget.style.transition = 'none';
          panel.style.transition  = 'none';
          widget.offsetHeight;

          widget.classList.add('is-mini');
          widget.style.maxHeight  = miniH + 'px';
          widget.style.cursor     = 'pointer';
          /* micro (scale=null): visible so SVG frame glow isn't clipped */
          widget.style.overflow   = scale ? 'hidden' : 'visible';
          widget.style.background = '';         /* restore widget background */
          widget.style.zIndex     = '';         /* restore stacking order */
          applyMiniConstraints();               /* sets correct mini transform */
          updateBtn();
          if (!scale) panel.style.transform = '';  /* micro: clear anim transform */
          panel.offsetHeight;

          widget.style.opacity    = '';
          widget.style.transition = '';
          panel.style.transition  = '';
        }

        function onColEnd(e) {
          if (e.propertyName !== 'transform') return;
          panel.removeEventListener('transitionend', onColEnd);
          colHandled = true;
          doSnap();
        }
        panel.addEventListener('transitionend', onColEnd);
        setTimeout(function () {
          if (!colHandled) {
            panel.removeEventListener('transitionend', onColEnd);
            doSnap();
          }
        }, 800);
      });
    }

    /* ── Event listeners ─────────────────────────────────────────── */
    btn.addEventListener('click', function (e) {
      e.stopPropagation();
      if (widget.classList.contains('is-mini')) expand(); else collapse();
    });

    widget.addEventListener('click', function () {
      if (widget.classList.contains('is-mini')) expand();
    });

    /* Start in mini state — instant, no transition.
       Panels like hud-03/05/06/07 have a scaleX/scaleY opening animation
       running at DOMContentLoaded time. If we let the CSS transition fire
       on the initial collapse(), it interpolates from the animation's
       mid-run scaleX value → produces a visible squish glitch.
       Disabling transitions for the initial snap fixes that. */
    widget.style.transition = 'none';
    panel.style.transition  = 'none';
    collapse(true);                /* instant — no opacity trick, no rAF */
    panel.offsetHeight;            /* force reflow — commits the instant mini state */
    widget.style.transition = '';
    panel.style.transition  = '';
  }

  global.NcHudMini = { init: init };

})(window);
/*]]>*/
</script>

<!-- NebulaCast HUD: Shared JS — HudPapers (publications module) -->
<script>
/*<![CDATA[*/
/**
 * hud_papers.js — Shared publications module for NebulaCast HUD widgets
 *
 * Queries SIMBAD TAP directly from the browser (CORS-open, no API key).
 * Single ADQL query via has_ref join:
 *   ref ← has_ref → ident  (object name → papers with titles)
 *
 * Public API:
 *   HudPapers.load(objectName, opts)
 *     opts.onStatus(text, state)  — 'loading' | 'error'
 *     opts.onData(papers)         — array of { title, year, bibcode, journal }
 *     opts.limit                  — max papers returned (default 10)
 *
 *   HudPapers.render(container, papers)
 *     Inserts .nc-hud-paper-row elements; handles empty case.
 *
 *   HudPapers.invalidate([objectName])
 *     Clear cache for one object (or all).
 *
 * Used by HUD-01, HUD-02, HUD-03, HUD-05.
 */
(function (global) {
  'use strict';

  var SIMBAD_TAP   = 'https://simbad.cds.unistra.fr/simbad/sim-tap/sync';
  var TIMEOUT_MS   = 15000;
  var DEFAULT_LIMIT = 10;

  var _cache     = {};   /* key: lower-cased object name → papers[] */
  var _inFlight  = {};   /* key: lower-cased object name → AbortController */

  /* ── ADQL builder ────────────────────────────────────────────────────── */

  function buildQuery(objectName, limit) {
    /* Escape single quotes for ADQL string literals */
    var safe = objectName.replace(/'/g, "''");
    return (
      'SELECT TOP ' + limit + ' r.title, r."year", r.bibcode, r.journal ' +
      'FROM ref r ' +
      'JOIN has_ref hr ON hr.oidbibref = r.oidbib ' +
      'JOIN ident i ON i.oidref = hr.oidref ' +
      "WHERE i.id = '" + safe + "' " +
      'ORDER BY "year" DESC'
    );
  }

  /* ── fetch helper ────────────────────────────────────────────────────── */

  function tapFetch(adql, signal) {
    var body = 'REQUEST=doQuery&LANG=ADQL&FORMAT=json&QUERY=' +
               encodeURIComponent(adql);
    return fetch(SIMBAD_TAP, {
      method:  'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body:    body,
      signal:  signal || undefined
    });
  }

  /* ── load ────────────────────────────────────────────────────────────── */

  function load(objectName, opts) {
    if (!objectName || typeof objectName !== 'string') { return; }
    opts = opts || {};

    var onStatus = typeof opts.onStatus === 'function' ? opts.onStatus : function () {};
    var onData   = typeof opts.onData   === 'function' ? opts.onData   : function () {};
    var limit    = (typeof opts.limit === 'number' && opts.limit > 0)
                   ? Math.min(opts.limit, 20) : DEFAULT_LIMIT;

    var key = objectName.toLowerCase();

    /* Serve from cache */
    if (_cache[key]) {
      onData(_cache[key]);
      return;
    }

    /* Abort any previous in-flight request for this object */
    if (_inFlight[key]) {
      try { _inFlight[key].abort(); } catch (e) { /* ignore */ }
    }

    var ctrl   = (typeof AbortController !== 'undefined') ? new AbortController() : null;
    var signal = ctrl ? ctrl.signal : null;
    _inFlight[key] = ctrl;

    var timer = ctrl
      ? setTimeout(function () { ctrl.abort(); }, TIMEOUT_MS)
      : null;

    onStatus('QUERYING SCIENTIFIC ARCHIVE...', 'loading');

    tapFetch(buildQuery(objectName, limit), signal)
      .then(function (resp) {
        if (timer) { clearTimeout(timer); }
        delete _inFlight[key];
        if (!resp.ok) { throw new Error('HTTP ' + resp.status); }
        return resp.json();
      })
      .then(function (json) {
        var meta = json.metadata || [];
        /* Build column-name → index map for robustness */
        var idx = {};
        meta.forEach(function (col, i) { idx[col.name] = i; });

        var papers = (json.data || []).map(function (row) {
          return {
            title:   (row[idx.title]   || '').replace(/\{\\em ([^}]+)\}/g, '$1').trim(),
            year:    row[idx.year]   || null,
            bibcode: row[idx.bibcode] || '',
            journal: row[idx.journal] || ''
          };
        });

        _cache[key] = papers;
        onData(papers);
      })
      .catch(function (err) {
        if (timer) { clearTimeout(timer); }
        delete _inFlight[key];

        var isTimeout = err && (err.name === 'AbortError');
        onStatus(isTimeout ? 'REQUEST TIMED OUT' : 'ARCHIVE UNAVAILABLE', 'error');
      });
  }

  /* ── render ──────────────────────────────────────────────────────────── */

  /**
   * render(container, papers, opts)
   *   opts.header  — optional header text shown above the list
   */
  function render(container, papers, opts) {
    if (!container) { return; }
    opts = opts || {};
    container.innerHTML = '';

    /* Optional section header */
    if (opts.header) {
      var hdr = document.createElement('div');
      hdr.className   = 'nc-hud-panel-header';
      hdr.textContent = opts.header;
      container.appendChild(hdr);
    }

    if (!papers || papers.length === 0) {
      var wrap = document.createElement('div');
      wrap.className = 'nc-ol-data-status nc-ol-data-status--error';
      var span = document.createElement('span');
      span.textContent = 'NO PUBLICATIONS FOUND';
      wrap.appendChild(span);
      container.appendChild(wrap);
      return;
    }

    papers.forEach(function (paper) {
      var row = document.createElement('div');
      row.className = 'nc-hud-paper-row';

      /* [YEAR] */
      if (paper.year) {
        var yr = document.createElement('span');
        yr.className   = 'nc-hud-paper-year';
        yr.textContent = '[' + paper.year + ']';
        row.appendChild(yr);
      }

      /* Title — <a> when bibcode available, plain span otherwise */
      var titleEl;
      if (paper.bibcode) {
        titleEl          = document.createElement('a');
        titleEl.href     = 'https://ui.adsabs.harvard.edu/abs/' +
                           encodeURIComponent(paper.bibcode) + '/abstract';
        titleEl.target   = '_blank';
        titleEl.rel      = 'noopener noreferrer';
      } else {
        titleEl = document.createElement('span');
      }
      titleEl.className   = 'nc-hud-paper-title';
      titleEl.textContent = paper.title || paper.bibcode || '—';
      row.appendChild(titleEl);

      /* Journal tag */
      if (paper.journal) {
        var jrn = document.createElement('span');
        jrn.className   = 'nc-hud-paper-journal';
        jrn.textContent = paper.journal;
        row.appendChild(jrn);
      }

      container.appendChild(row);
    });
  }

  /* ── invalidate ──────────────────────────────────────────────────────── */

  function invalidate(objectName) {
    if (objectName) {
      delete _cache[objectName.toLowerCase()];
    } else {
      _cache = {};
    }
  }

  /* ── export ──────────────────────────────────────────────────────────── */

  global.HudPapers = { load: load, render: render, invalidate: invalidate };

}(typeof window !== 'undefined' ? window : this));
/*]]>*/
</script>

<!-- NebulaCast HUD: Panel JS — NcHud01 (Object Lock factory) -->
<script>
/*<![CDATA[*/
/* blogger-hud01-template.js — NebulaCast HUD-01 Object Lock Panel
   Blogger release: factory pattern for multi-instance support.

   Usage (once per post, in HTML mode of the Blogger post editor):
   ──────────────────────────────────────────────────────────────
   <script>
   (function () {
     var all    = document.querySelectorAll('.nc-hud-01:not([data-hud-init])');
     var widget = all[all.length - 1];
     if (!widget) return;
     widget.setAttribute('data-hud-init', '1');
     NcHud01.init(widget, {
       objectMode : true,       // false → html mode (blog post text)
       target     : 'NGC 1300', // initial target for Aladin / SIMBAD
       vizierLimit: 10          // max catalog entries (1-20)
     });
   })();
   <\/script>

   Dependencies (loaded ONCE in the Blogger template <head>):
     blogger-hud01-template.css
     hud-mini.js      → window.NcHudMini
     hud_papers.js    → window.HudPapers

   Config object (cfg):
     objectMode  : boolean  true = object mode (Aladin + SIMBAD); false = html mode
     target      : string   initial astronomical target (default "NGC 1300")
     vizierLimit : number   max VizieR / Papers rows returned (1-20, default 10)
     onExpand    : function optional callback fired after panel expands

   Limitations resolved vs. original hud-01.js (C-07):
     • Factory pattern — NcHud01.init() creates isolated state per widget
     • Shared Aladin CDN load — only one <script> tag injected per page
       (cooperative with NcHud02 via global._ncAladinLoader)
     • Unique Aladin viewer IDs per instance (nc-hud01-aladin-1, -2, …)
     • NcHudMini.init() used for expand/collapse (replaces custom code)
     • All DOM queries scoped to the passed widget element
*/
'use strict';

(function (global) {

  /* ── Shared Aladin CDN loader state (cooperative with NcHud02) ────
     Both NcHud01 and NcHud02 share global._ncAladinLoader so only ONE
     Aladin <script> tag is injected per page regardless of load order. ── */
  var _loader = global._ncAladinLoader;
  if (!_loader) {
    _loader = global._ncAladinLoader = {
      loaded   : false,
      loading  : false,
      callbacks: []
    };
  }

  var ALADIN_JS_URL    = 'https://aladin.cds.unistra.fr/AladinLite/api/v3/latest/aladin.js';
  var ALADIN_CSS_URL   = 'https://aladin.cds.unistra.fr/AladinLite/api/v3/latest/aladin.min.css';
  var ALADIN_LOAD_TIMEOUT_MS = 15000;

  var _instanceCount = 0;

  /* ── Static data ──────────────────────────────────────────────────── */

  var OTYPE_LABELS = {
    'G'   : 'GALAXY',
    'GBar': 'BARRED SPIRAL GALAXY',
    'GbS' : 'BARRED SPIRAL GALAXY',
    'Sy1' : 'SEYFERT 1 GALAXY',
    'Sy2' : 'SEYFERT 2 GALAXY',
    'AGN' : 'ACTIVE GALACTIC NUCLEUS',
    'QSO' : 'QUASAR',
    '*'   : 'STAR',
    '**'  : 'DOUBLE STAR',
    'Cl*' : 'STAR CLUSTER',
    'GlC' : 'GLOBULAR CLUSTER',
    'OpC' : 'OPEN CLUSTER',
    'Neb' : 'NEBULA',
    'SNR' : 'SUPERNOVA REMNANT',
    'PN'  : 'PLANETARY NEBULA',
    'SFR' : 'STAR-FORMING REGION'
  };

  var NGC1300_FALLBACK = [
    ['OBJECT',     'NGC 1300'],
    ['TYPE',       'BARRED SPIRAL GALAXY'],
    ['RA',         '03H 19M 41.1S'],
    ['DEC',        '-19° 24\' 25"'],
    ['REDSHIFT',   '0.005258'],
    ['RADIAL VEL', '1576 KM/S'],
    ['SIZE',       "6.2' \xd7 4.1'"],
    ['MORPH',      'SB(RS)BC'],
    ['MAGNITUDE',  'V ≈ 11.4']
  ];

  var VIZIER_NGC1300_FALLBACK = [
    {
      catid: 'VII/155',
      title: 'Revised New General Catalogue and Index Catalogue',
      desc:  'Morphological, positional, and bibliographic data for 13 226 NGC and 5 386 IC objects',
      year: '2022', author: 'Steinicke W.'
    },
    {
      catid: 'VII/237',
      title: 'HYPERLEDA — Extragalactic Database',
      desc:  'Redshifts, B-magnitudes, diameters, morphology for ~3 million galaxies',
      year: '2014', author: 'Makarov D. et al.'
    },
    {
      catid: 'J/AJ/146/86',
      title: 'S4G — Spitzer Survey of Stellar Structure in Galaxies',
      desc:  '3.6 and 4.5 μm mosaics and photometry for 2352 nearby galaxies; NGC 1300 included',
      year: '2013', author: 'Sheth K. et al.'
    },
    {
      catid: 'J/ApJS/197/21',
      title: 'NIRS0S — Near-IR Atlas of S0–Sa Galaxies',
      desc:  'K-band photometry, ellipse fits, and bar/bulge decompositions for early-type spirals',
      year: '2011', author: 'Laurikainen E. et al.'
    },
    {
      catid: 'J/AJ/143/138',
      title: 'Galaxy Zoo 2 — Detailed Morphological Classifications',
      desc:  'Bar presence, spiral structure, clumpiness from 300 000+ volunteer classifiers',
      year: '2013', author: 'Willett K.W. et al.'
    },
    {
      catid: 'J/ApJS/190/147',
      title: 'SINGG — Survey for Ionization in Neutral Gas Galaxies',
      desc:  'Hα and R-band photometry for HI-selected nearby star-forming galaxies',
      year: '2010', author: 'Meurer G.R. et al.'
    },
    {
      catid: 'J/MNRAS/444/527',
      title: 'Photometric decomposition of barred galaxies',
      desc:  'Bulge, disk, and bar structural parameters from 2D photometric fits',
      year: '2014', author: 'Salo H. et al.'
    },
    {
      catid: 'J/A+A/532/A74',
      title: 'CALIFA — Calar Alto Legacy Integral Field Survey',
      desc:  'Resolved spectroscopy (3745–7300 Å) for 600+ nearby galaxies',
      year: '2011', author: 'Sánchez S.F. et al.'
    }
  ];

  /* ══════════════════════════════════════════════════════════════════
     Shared Aladin CDN loader (cooperative with NcHud02 via _ncAladinLoader)
     Ensures only ONE Aladin <script> tag is injected per page even when
     both NcHud01 and NcHud02 panels appear together.
  ══════════════════════════════════════════════════════════════════ */
  function loadAladinScript(onLoad, onError) {
    if (_loader.loaded) { onLoad(); return; }

    /* Queue callback; first caller starts the actual load */
    _loader.callbacks.push({ onLoad: onLoad, onError: onError });

    if (_loader.loading) return;   /* already in flight — just queued */
    _loader.loading = true;

    /* Aladin CSS — idempotent */
    if (!document.querySelector('link[href*="aladin.min.css"]')) {
      var link = document.createElement('link');
      link.rel  = 'stylesheet';
      link.href = ALADIN_CSS_URL;
      document.head.appendChild(link);
    }

    var settled = false;
    function settle(ok) {
      if (settled) return;
      settled = true;
      clearTimeout(timer);
      _loader.loading = false;
      if (ok) {
        _loader.loaded = true;
        _loader.callbacks.forEach(function (cb) { try { cb.onLoad(); } catch (e) {} });
      } else {
        _loader.callbacks.forEach(function (cb) { try { cb.onError(); } catch (e) {} });
      }
      _loader.callbacks = [];
    }

    var timer = setTimeout(function () {
      warn('[NcHud01] Aladin CDN script load timed out after ' + ALADIN_LOAD_TIMEOUT_MS + 'ms');
      settle(false);
    }, ALADIN_LOAD_TIMEOUT_MS);

    var script     = document.createElement('script');
    script.src     = ALADIN_JS_URL;
    script.charset = 'utf-8';
    script.onload  = function () { settle(true); };
    script.onerror = function () {
      warn('[NcHud01] Aladin CDN script failed to load');
      settle(false);
    };
    document.head.appendChild(script);
  }

  /* ══════════════════════════════════════════════════════════════════
     Instance factory
  ══════════════════════════════════════════════════════════════════ */
  function createInstance(widgetEl, cfg) {
    cfg = cfg || {};

    var instanceN = ++_instanceCount;
    var widget    = widgetEl;
    var buttons   = {};   /* keyed by data-action */

    /* ── Instance-level Aladin state ─────────────────────────────── */
    var aladinInstance        = null;
    var aladinReady           = false;
    var aladinBusy            = false;
    var aladinInstancePending = false;
    var aladinInitRetries     = 0;
    var pendingTarget         = null;
    var savedAladinState      = null;

    var NGC1300_RA     = 49.9213;
    var NGC1300_DEC    = -19.4069;
    var DEFAULT_FOV    = 0.5;
    var DEFAULT_SURVEY = 'P/DSS2/color';

    /* Unique DOM id for Aladin viewer div — avoids collision on multi-post pages */
    var aladinViewerId = 'nc-hud01-aladin-' + instanceN;

    /* ── Instance-level SIMBAD state ─────────────────────────────── */
    var SIMBAD_TAP_URL    = 'https://simbad.cds.unistra.fr/simbad/sim-tap/sync';
    var SIMBAD_TIMEOUT_MS = 12000;
    var simbadLoaded  = false;
    var simbadBusy    = false;
    var currentTarget = (typeof cfg.target === 'string' && cfg.target.trim())
                        ? cfg.target.trim() : 'NGC 1300';

    /* ── Instance-level Papers / VizieR state ─────────────────────── */
    var papersLoaded = false;
    var papersBusy   = false;

    var VIZIER_URL        = 'https://vizier.cds.unistra.fr/viz-bin/votable';
    var VIZIER_TIMEOUT_MS = 10000;
    var vizierCache          = {};
    var vizierBusy           = false;
    var vizierAbortCtrl      = null;
    var vizierTargetInFlight = null;
    var vizierLimit = (typeof cfg.vizierLimit === 'number' && cfg.vizierLimit > 0)
                      ? Math.min(cfg.vizierLimit, 20) : 10;

    /* ── Mode ────────────────────────────────────────────────────── */
    var objectMode = (cfg.objectMode !== false);

    /* Group B tabs */
    var INFO_TABS   = ['data', 'papers', 'catalog'];
    var TOGGLE_ATTRS = { reticle: 'data-reticle' };

    /* ── DOM helpers ─────────────────────────────────────────────── */

    function q(sel)  { return widget.querySelector(sel); }
    function qa(sel) { return widget.querySelectorAll(sel); }

    /* ── Init ────────────────────────────────────────────────────── */

    function init() {
      widget.setAttribute('data-mode', objectMode ? 'object' : 'html');
      var slot = q('.nc-hud-01-html-slot');
      if (slot) slot.setAttribute('aria-hidden', objectMode ? 'true' : 'false');

      /* Assign unique id to the Aladin viewer container inside this widget */
      var aladinViewer = q('.nc-hud-01-aladdin > div');
      if (aladinViewer && !aladinViewer.id) aladinViewer.id = aladinViewerId;

      if (objectMode) {
        initObjectMode();
      } else {
        initHtmlMode();
      }
    }

    /* ── Object mode ─────────────────────────────────────────────── */

    function initObjectMode() {
      setDefault('data-target-lock', 'on');
      setDefault('data-reticle',     'on');
      widget.setAttribute('data-aladdin', 'on');
      setDefault('data-info-mode',   'data');

      qa('.nc-ol-tb-btn').forEach(function (btn) {
        var action = btn.getAttribute('data-action');
        if (!action) return;
        buttons[action] = btn;
        syncButton(btn, action);
        btn.addEventListener('click', function () { handleToggle(action, btn); });
      });

      /* Clear width set by opening animation so panel stays fluid.
         Guard: skip if widget is mini — applyMiniConstraints() set the
         width intentionally; clearing it would break the 317px thumbnail. */
      var panel = q('.nc-ol-panel');
      if (panel) {
        panel.addEventListener('animationend', function (e) {
          if (e.animationName === 'ncOlOpenHud' &&
              !widget.classList.contains('is-mini')) {
            panel.style.width = '';
          }
        }, { once: true });
      }

      loadSimbadData(currentTarget);
      initAladin();

      /* Expand/collapse via shared NcHudMini */
      NcHudMini.init({
        widget      : widget,
        panel       : q('.nc-ol-panel'),
        miniH       : 148,
        scale       : 0.352,
        expandedMaxH: 2000,
        onCollapse  : function () {
          /* Pause Aladin while mini so the photo fallback is visible */
          savedAladinState = widget.getAttribute('data-aladdin') || 'off';
          widget.setAttribute('data-aladdin', 'off');
        },
        onExpand    : function () {
          /* Restore Aladin toggle state */
          if (savedAladinState !== null) {
            widget.setAttribute('data-aladdin', savedAladinState);
            savedAladinState = null;
          }
          if (aladinInstancePending) {
            /* CDN loaded while panel was mini — create instance now at scale(1) */
            createAladinInstance();
          } else if (aladinReady && aladinInstance) {
            /* Re-expand after collapse.
               FLIP transition runs ~600 ms; dispatching resize immediately gives
               Aladin wrong canvas dimensions (panel still transformed).
               Wait for the transition to finish, then send resize nudges. */
            var nudge = function() {
              try { global.dispatchEvent(new Event('resize')); } catch (e) {}
            };
            setTimeout(nudge, 650);   /* after transition ends  */
            setTimeout(nudge, 850);   /* second pass — stubborn cases */
          }
          if (typeof cfg.onExpand === 'function') cfg.onExpand();
        }
      });

      /* If starting already in mini mode:
         1. NcHudMini's instant collapse() skips onCollapse → data-aladdin stays
            "on" → Aladin div (dark background, opacity:1) covers the galaxy photo.
            Fix: apply the onCollapse side-effect manually.
         2. Make the ncOlOpenHud opening animation instant (0s) so the panel
            jumps to opacity:1 immediately instead of fading from 0 over 1.2s. */
      if (widget.classList.contains('is-mini')) {
        savedAladinState = widget.getAttribute('data-aladdin') || 'off';
        widget.setAttribute('data-aladdin', 'off');
        var startPanel = q('.nc-ol-panel');
        if (startPanel) startPanel.style.animationDuration = '0s, 5s';
      }
    }

    /* ── HTML mode ───────────────────────────────────────────────── */

    function initHtmlMode() {
      widget.setAttribute('data-reticle', 'off');
      widget.setAttribute('data-aladdin',  'off');

      var panel = q('.nc-ol-panel');
      if (panel) {
        panel.addEventListener('animationend', function (e) {
          if (e.animationName === 'ncOlOpenHud' &&
              !widget.classList.contains('is-mini')) {
            panel.style.width = '';
          }
        }, { once: true });
      }

      /* Expand/collapse via shared NcHudMini (same chrome, just content differs) */
      NcHudMini.init({
        widget      : widget,
        panel       : q('.nc-ol-panel'),
        miniH       : 148,
        panelH      : 420,
        scale       : 0.352,
        expandedMaxH: 2000,
        onExpand    : function () {
          if (typeof cfg.onExpand === 'function') cfg.onExpand();
        }
      });

      /* HTML mode: no Aladin to hide, just skip opening animation */
      if (widget.classList.contains('is-mini')) {
        var startPanel = q('.nc-ol-panel');
        if (startPanel) startPanel.style.animationDuration = '0s, 5s';
      }
    }

    /* ── Toolbar helpers ─────────────────────────────────────────── */

    function setDefault(attr, value) {
      if (!widget.hasAttribute(attr)) widget.setAttribute(attr, value);
    }

    function dataVal(attr) {
      return widget.getAttribute(attr) === 'on';
    }

    function syncButton(btn, action) {
      if (TOGGLE_ATTRS[action]) {
        btn.setAttribute('aria-pressed', String(dataVal(TOGGLE_ATTRS[action])));
      } else if (INFO_TABS.indexOf(action) !== -1) {
        btn.setAttribute('aria-pressed',
          String(widget.getAttribute('data-info-mode') === action));
      }
    }

    function handleToggle(action, btn) {
      if (action === 'reticle') {
        setReticle(btn.getAttribute('aria-pressed') !== 'true');
      } else if (INFO_TABS.indexOf(action) !== -1) {
        setInfoMode(action);
      }
    }

    /* ── Aladin Lite ─────────────────────────────────────────────── */

    function initAladin() {
      if (aladinReady || aladinBusy || aladinInstancePending) return;
      aladinBusy = true;

      loadAladinScript(
        function () {                              /* onLoad */
          if (typeof A === 'undefined' || typeof A.init === 'undefined') {
            warn('window.A not found after script load');
            showAladinFallback('ALADIN LITE UNAVAILABLE');
            aladinBusy = false;
            return;
          }

          /* Panel is mini while CDN was loading — defer instance creation */
          if (widget.classList.contains('is-mini')) {
            aladinInstancePending = true;
            aladinBusy = false;
            return;
          }

          createAladinInstance();
        },
        function () {                              /* onError */
          showAladinFallback('ALADIN LITE UNAVAILABLE');
          aladinBusy = false;
        }
      );
    }

    function createAladinInstance() {
      aladinInstancePending = false;

      /* Size guard — A.aladin() reads getBoundingClientRect() for canvas dimensions.
         A CSS transform(scale) on an ancestor produces a wrong measurement.
         Retry until the panel is at full size (scale=1). */
      var container = document.getElementById(aladinViewerId);
      if (container) {
        var rect      = container.getBoundingClientRect();
        var expectedW = 445, expectedH = 352;
        if (Math.abs(rect.width  - expectedW) > 8 ||
            Math.abs(rect.height - expectedH) > 8) {
          aladinInitRetries++;
          if (aladinInitRetries <= 10) {
            warn('[NcHud01-' + instanceN + '] Aladin container ' +
                 Math.round(rect.width) + '\xd7' + Math.round(rect.height) +
                 ' (expected ' + expectedW + '\xd7' + expectedH +
                 ') — panel still scaled, retry #' + aladinInitRetries);
            aladinInstancePending = true;
            setTimeout(createAladinInstance, 300);
            return;
          }
          warn('[NcHud01-' + instanceN + '] Aladin size check gave up — proceeding');
        }
      }
      aladinInitRetries = 0;

      requestAnimationFrame(function () {
        A.init.then(function () {
          try {
            aladinInstance = A.aladin('#' + aladinViewerId, {
              survey               : DEFAULT_SURVEY,
              fov                  : DEFAULT_FOV,
              target               : currentTarget,
              mode                 : 'dark',
              showReticle          : false,
              showZoomControl      : false,
              showFullscreenControl: false,
              showLayersControl    : false,
              showGotoControl      : false,
              showStatusBar        : false,
              showFrame            : false,
              showCooGrid          : false,
              showProjectionControl: false,
              backgroundColor      : '#000814'
            });

            aladinReady = true;
            aladinBusy  = false;
            markAladinReady();

            /* Force Aladin to recalculate canvas size after DOM settle */
            setTimeout(function () {
              try { global.dispatchEvent(new Event('resize')); } catch (e) {}
            }, 120);

            if (pendingTarget) {
              var pt = pendingTarget;
              pendingTarget = null;
              applyTarget(pt.target, pt.fov);
            }

          } catch (err) {
            warn('[NcHud01-' + instanceN + '] A.aladin() threw: ' + err);
            showAladinFallback('ALADIN LITE ERROR');
            aladinBusy = false;
          }
        }).catch(function (err) {
          warn('[NcHud01-' + instanceN + '] A.init rejected: ' + err);
          showAladinFallback('ALADIN LITE UNAVAILABLE');
          aladinBusy = false;
        });
      });
    }

    function markAladinReady() {
      var layer = q('.nc-hud-01-aladdin');
      if (layer) layer.classList.add('nc-hud-01-aladdin--ready');
    }

    function showAladinFallback(msg) {
      widget.setAttribute('data-aladdin', 'off');
      var label = q('.nc-hud-01-aladdin__label');
      if (label) label.textContent = msg;
      var sub = q('.nc-hud-01-aladdin__sub');
      if (sub) sub.textContent = '';
    }

    function applyTarget(target, fov) {
      if (!aladinInstance) return;
      try {
        aladinInstance.gotoObject(target);
      } catch (err) {
        warn('gotoObject("' + target + '") failed — falling back to RA/Dec: ' + err);
        try {
          aladinInstance.gotoRaDec(NGC1300_RA, NGC1300_DEC);
        } catch (e2) {
          warn('gotoRaDec fallback also failed: ' + e2);
        }
      }
      if (fov !== undefined && fov !== null) {
        try { aladinInstance.setFoV(fov); } catch (e) { warn('setFoV failed: ' + e); }
      }
    }

    /* ── SIMBAD data fetch ───────────────────────────────────────── */

    function loadSimbadData(target) {
      if (simbadBusy) return;
      simbadBusy   = true;
      simbadLoaded = false;
      currentTarget = target;

      setDataStatus('QUERYING SIMBAD…', 'loading');

      var query = [
        'SELECT b.main_id, b.otype, b.ra, b.dec,',
        '       b.rvz_redshift, b.rvz_radvel, b.nbref,',
        '       b.galdim_majaxis, b.galdim_minaxis, b.morph_type',
        'FROM basic b',
        'JOIN ident i ON i.oidref = b.oid',
        "WHERE i.id = '" + target.replace(/'/g, "''") + "'"
      ].join(' ');

      var url = SIMBAD_TAP_URL +
        '?REQUEST=doQuery&LANG=ADQL&FORMAT=json&QUERY=' +
        encodeURIComponent(query);

      var aborted = false;
      var ctrl  = (typeof AbortController !== 'undefined') ? new AbortController() : null;
      var timer = setTimeout(function () {
        aborted = true;
        if (ctrl) ctrl.abort();
        simbadBusy = false;
        warn('[NcHud01-' + instanceN + '] SIMBAD fetch timed out');
        renderFallback(target);
      }, SIMBAD_TIMEOUT_MS);

      fetch(url, ctrl ? { signal: ctrl.signal } : {})
        .then(function (r) {
          if (!r.ok) throw new Error('HTTP ' + r.status);
          return r.json();
        })
        .then(function (json) {
          if (aborted) return;
          clearTimeout(timer);
          simbadBusy = false;

          if (!json.data || json.data.length === 0) {
            warn('[NcHud01-' + instanceN + '] SIMBAD: no results for "' + target + '"');
            renderFallback(target);
            return;
          }

          var row = {};
          json.metadata.forEach(function (col, i) { row[col.name] = json.data[0][i]; });
          renderSimbadData(row, target);
          simbadLoaded = true;
        })
        .catch(function (err) {
          if (aborted) return;
          clearTimeout(timer);
          simbadBusy = false;
          warn('[NcHud01-' + instanceN + '] SIMBAD fetch failed: ' + err);
          renderFallback(target);
        });
    }

    function renderSimbadData(row, target) {
      var rows = [];
      rows.push(['OBJECT', row.main_id ? String(row.main_id).trim() : target]);
      if (row.otype) {
        var label = OTYPE_LABELS[String(row.otype).trim()] || String(row.otype).trim();
        rows.push(['TYPE', label]);
      }
      if (row.ra  != null) rows.push(['RA',  formatRA(row.ra)]);
      if (row.dec != null) rows.push(['DEC', formatDec(row.dec)]);
      if (row.rvz_redshift != null && row.rvz_redshift !== '')
        rows.push(['REDSHIFT', Number(row.rvz_redshift).toFixed(5)]);
      if (row.rvz_radvel != null && row.rvz_radvel !== '')
        rows.push(['RADIAL VEL', Math.round(Number(row.rvz_radvel)) + ' KM/S']);
      if (row.galdim_majaxis != null && row.galdim_majaxis !== '') {
        var maj = Number(row.galdim_majaxis).toFixed(1);
        var min = (row.galdim_minaxis != null && row.galdim_minaxis !== '')
                  ? Number(row.galdim_minaxis).toFixed(1) : '—';
        rows.push(['SIZE', maj + "' \xd7 " + min + "'"]);
      }
      if (row.morph_type != null && row.morph_type !== '')
        rows.push(['MORPH', String(row.morph_type).trim().toUpperCase()]);
      if (row.nbref != null && row.nbref !== '')
        rows.push(['PAPERS', Number(row.nbref).toLocaleString() + ' REFS']);
      injectDataRows(rows, false);
    }

    function renderFallback(target) {
      if (target === 'NGC 1300' || target === currentTarget) {
        injectDataRows(NGC1300_FALLBACK, true);
      } else {
        setDataStatus('SIMBAD DATA UNAVAILABLE', 'error');
      }
    }

    function injectDataRows(pairs, isFallback) {
      var panel = q('#nc-hud01-data-panel');
      if (!panel) return;
      panel.innerHTML = '';
      pairs.forEach(function (pair) {
        var lbl = document.createElement('div');
        lbl.className   = 'nc-ol-label';
        lbl.textContent = pair[0] + ':';
        var val = document.createElement('div');
        val.className   = 'nc-ol-value';
        val.textContent = pair[1];
        panel.appendChild(lbl);
        panel.appendChild(val);
      });
      if (isFallback) {
        var note = document.createElement('div');
        note.className   = 'nc-ol-data-note';
        note.textContent = '— SIMBAD OFFLINE / CACHED DATA —';
        panel.appendChild(note);
      }
    }

    function setDataStatus(text, state) {
      var panel = q('#nc-hud01-data-panel');
      if (!panel) return;
      panel.innerHTML = '';
      var wrap = document.createElement('div');
      wrap.className = 'nc-ol-data-status nc-ol-data-status--' + (state || 'loading');
      if (state === 'loading') {
        var ring = document.createElement('div');
        ring.className = 'nc-ol-spinner';
        wrap.appendChild(ring);
      }
      var txt = document.createElement('span');
      txt.textContent = text;
      wrap.appendChild(txt);
      panel.appendChild(wrap);
    }

    /* ── Coordinate formatters ───────────────────────────────────── */

    function pad2(n) { return n < 10 ? '0' + n : String(n); }

    function formatRA(deg) {
      if (deg == null) return '—';
      var h  = Math.floor(deg / 15);
      var rm = (deg / 15 - h) * 60;
      var m  = Math.floor(rm);
      var s  = (rm - m) * 60;
      return pad2(h) + 'H ' + pad2(m) + 'M ' + s.toFixed(1) + 'S';
    }

    function formatDec(deg) {
      if (deg == null) return '—';
      var sign = deg < 0 ? '-' : '+';
      var abs  = Math.abs(deg);
      var d    = Math.floor(abs);
      var dm   = (abs - d) * 60;
      var m    = Math.floor(dm);
      var s    = (dm - m) * 60;
      return sign + pad2(d) + '\xb0 ' + pad2(m) + "' " + s.toFixed(1) + '"';
    }

    /* ── VizieR catalog references ───────────────────────────────── */

    function loadVizierReferences(target) {
      if (vizierCache[target]) {
        renderVizierRefs(vizierCache[target].refs, vizierCache[target].fallback);
        return;
      }

      if (vizierBusy && vizierTargetInFlight !== target) {
        if (vizierAbortCtrl) vizierAbortCtrl.abort();
        vizierBusy = false;
      }
      if (vizierBusy) return;

      vizierBusy           = true;
      vizierTargetInFlight = target;

      setPapersStatus('LOADING VIZIER REFERENCES…', 'loading');

      var params = [
        '-c=' + encodeURIComponent(target),
        '-c.rs=2',
        '-out.max=1',
        '-source=',
        '-out.form=mini'
      ].join('&');

      var aborted = false;
      vizierAbortCtrl = (typeof AbortController !== 'undefined') ? new AbortController() : null;

      var timer = setTimeout(function () {
        aborted = true;
        if (vizierAbortCtrl) vizierAbortCtrl.abort();
        vizierBusy = vizierTargetInFlight = vizierAbortCtrl = null;
        warn('[NcHud01-' + instanceN + '] VizieR fetch timed out');
        useVizierFallback(target);
      }, VIZIER_TIMEOUT_MS);

      fetch(VIZIER_URL + '?' + params, {
        signal: vizierAbortCtrl ? vizierAbortCtrl.signal : undefined
      })
        .then(function (r) {
          if (!r.ok) throw new Error('HTTP ' + r.status);
          return r.text();
        })
        .then(function (xml) {
          if (aborted) return;
          clearTimeout(timer);
          vizierBusy = vizierTargetInFlight = vizierAbortCtrl = null;

          var refs = parseVizierVOTable(xml);
          if (!refs || refs.length === 0) { useVizierFallback(target); return; }

          vizierCache[target] = { refs: refs, fallback: false };
          renderVizierRefs(refs, false);
        })
        .catch(function (err) {
          if (aborted) return;
          clearTimeout(timer);
          vizierBusy = vizierTargetInFlight = vizierAbortCtrl = null;
          warn('[NcHud01-' + instanceN + '] VizieR fetch failed: ' + err);
          useVizierFallback(target);
        });
    }

    function useVizierFallback(target) {
      if (target === 'NGC 1300' || target === currentTarget) {
        var refs = VIZIER_NGC1300_FALLBACK.slice(0, vizierLimit);
        vizierCache[target] = { refs: refs, fallback: true };
        renderVizierRefs(refs, true);
      } else {
        setPapersStatus('VIZIER REFERENCES UNAVAILABLE', 'error');
      }
    }

    function parseVizierVOTable(xml) {
      var refs = [];
      try {
        var doc = (new DOMParser()).parseFromString(xml, 'application/xml');
        if (doc.querySelector('parsererror')) { warn('VizieR VOTable XML parse error'); return refs; }
        var resources = doc.getElementsByTagName('RESOURCE');
        for (var i = 0; i < resources.length && refs.length < vizierLimit; i++) {
          var res  = resources[i];
          var name = res.getAttribute('name') || '';
          if (!name || name === 'votable' || name === 'results') continue;
          var parts = name.split('/');
          var catid = parts.length > 3 ? parts.slice(0, parts.length - 1).join('/') : name;
          var desc  = '';
          var infoEls = res.getElementsByTagName('INFO');
          for (var j = 0; j < infoEls.length; j++) {
            var infoName = infoEls[j].getAttribute('name') || '';
            if (infoName === 'Description' || infoName === 'description') {
              desc = (infoEls[j].getAttribute('value') || infoEls[j].textContent || '').trim();
              break;
            }
          }
          refs.push({ catid: catid, title: name, desc: desc, year: '', author: '' });
        }
      } catch (e) { warn('VizieR VOTable parse exception: ' + e); }
      return refs;
    }

    /* ── Papers tab (HudPapers shared module) ────────────────────── */

    function loadPapers(target) {
      if (!target) return;
      papersBusy   = true;
      papersLoaded = false;

      HudPapers.load(target, {
        limit   : vizierLimit,
        onStatus: function (text, state) {
          papersBusy = (state === 'loading');
          setPapersInfoStatus(text, state);
        },
        onData  : function (papers) {
          papersBusy   = false;
          papersLoaded = true;
          var panel = q('.nc-ol-info-panel--papers');
          if (!panel) return;
          panel.classList.add('nc-hud-papers-list');
          HudPapers.render(panel, papers, {
            header: 'TOP ' + papers.length + ' PAPERS LINKED TO THE OBJECT'
          });
        }
      });
    }

    function setPapersInfoStatus(text, state) {
      var panel = q('.nc-ol-info-panel--papers');
      if (!panel) return;
      panel.innerHTML = '';
      panel.classList.remove('nc-hud-papers-list');
      appendStatus(panel, text, state);
    }

    function setPapersStatus(text, state) {
      var panel = q('.nc-ol-info-panel--catalog');
      if (!panel) return;
      panel.innerHTML = '';
      panel.classList.remove('nc-ol-papers-list');
      appendStatus(panel, text, state);
    }

    function appendStatus(container, text, state) {
      var wrap = document.createElement('div');
      wrap.className = 'nc-ol-data-status nc-ol-data-status--' + (state || 'loading');
      if (state === 'loading') {
        var ring = document.createElement('div');
        ring.className = 'nc-ol-spinner';
        wrap.appendChild(ring);
      }
      var txt = document.createElement('span');
      txt.textContent = text;
      wrap.appendChild(txt);
      container.appendChild(wrap);
    }

    function renderVizierRefs(refs, isFallback) {
      var panel = q('.nc-ol-info-panel--catalog');
      if (!panel) return;
      panel.innerHTML = '';
      panel.classList.add('nc-ol-papers-list');

      var hdr = document.createElement('div');
      hdr.className   = 'nc-hud-panel-header';
      hdr.textContent = 'TOP ' + refs.length + ' CATALOGS LINKED TO THE OBJECT';
      panel.appendChild(hdr);

      if (isFallback) {
        var note = document.createElement('div');
        note.className   = 'nc-ol-data-note';
        note.textContent = '— VIZIER OFFLINE / DEMO DATA —';
        panel.appendChild(note);
      }

      refs.forEach(function (ref) {
        var card = document.createElement('div');
        card.className = 'nc-ol-paper-card';

        var vizUrl  = 'https://vizier.cds.unistra.fr/viz-bin/VizieR?-source=' +
                      encodeURIComponent(ref.catid);
        var titleEl = document.createElement('a');
        titleEl.className   = 'nc-ol-paper-title';
        titleEl.textContent = ref.title || ref.catid;
        titleEl.href   = ref.url || vizUrl;
        titleEl.target = '_blank';
        titleEl.rel    = 'noopener noreferrer';
        card.appendChild(titleEl);

        var metaParts = [ref.catid];
        if (ref.year)   metaParts.push(ref.year);
        if (ref.author) metaParts.push(ref.author);
        if (metaParts.length) {
          var meta = document.createElement('div');
          meta.className   = 'nc-ol-paper-meta';
          meta.textContent = metaParts.join(' \xb7 ');
          card.appendChild(meta);
        }

        if (ref.desc) {
          var desc = document.createElement('div');
          desc.className   = 'nc-ol-paper-abstract';
          desc.textContent = ref.desc;
          card.appendChild(desc);
        }

        panel.appendChild(card);
      });
    }

    /* ── Public API ──────────────────────────────────────────────── */

    function setTargetLock(enabled) {
      widget.setAttribute('data-target-lock', enabled ? 'on' : 'off');
    }

    function setReticle(enabled) {
      widget.setAttribute('data-reticle', enabled ? 'on' : 'off');
      if (buttons.reticle) buttons.reticle.setAttribute('aria-pressed', String(!!enabled));
    }

    function setAladdin(enabled) {
      widget.setAttribute('data-aladdin', enabled ? 'on' : 'off');
      if (enabled && !aladinReady && !aladinBusy) initAladin();
    }

    function setInfoMode(mode) {
      if (INFO_TABS.indexOf(mode) === -1) { warn('unknown info mode: ' + mode); return; }
      widget.setAttribute('data-info-mode', mode);
      INFO_TABS.forEach(function (m) {
        if (buttons[m]) buttons[m].setAttribute('aria-pressed', String(m === mode));
      });
      if (mode === 'data' && !simbadLoaded && !simbadBusy) {
        loadSimbadData(currentTarget);
      } else if (mode === 'catalog') {
        loadVizierReferences(currentTarget);
      } else if (mode === 'papers' && !papersLoaded && !papersBusy) {
        loadPapers(currentTarget);
      }
    }

    function setAladinTarget(target, fov) {
      if (aladinReady) { applyTarget(target, fov); return; }
      pendingTarget = { target: target, fov: fov };
      if (!aladinBusy) initAladin();
    }

    function setTarget(target) {
      if (!target || typeof target !== 'string') { warn('setTarget: invalid target'); return; }
      currentTarget = target;
      simbadLoaded  = false;
      papersLoaded  = false;
      HudPapers.invalidate(target);

      var mode = widget.getAttribute('data-info-mode') || 'data';
      if (mode === 'data') loadSimbadData(currentTarget);
      else if (mode === 'catalog') loadVizierReferences(currentTarget);
      else if (mode === 'papers') loadPapers(currentTarget);

      if (aladinReady) applyTarget(target);
      else if (!aladinBusy) pendingTarget = { target: target, fov: DEFAULT_FOV };
    }

    function setMode(mode) {
      if (mode !== 'object' && mode !== 'html') {
        warn('setMode: unknown mode "' + mode + '"'); return;
      }
      widget.setAttribute('data-mode', mode);
      var slot = q('.nc-hud-01-html-slot');
      if (mode === 'html') {
        widget.setAttribute('data-reticle', 'off');
        widget.setAttribute('data-aladdin',  'off');
        if (buttons.reticle) buttons.reticle.setAttribute('aria-pressed', 'false');
        if (slot) slot.setAttribute('aria-hidden', 'false');
      } else {
        widget.setAttribute('data-reticle',   'on');
        widget.setAttribute('data-aladdin',   'on');
        widget.setAttribute('data-info-mode', 'data');
        if (buttons.reticle) buttons.reticle.setAttribute('aria-pressed', 'true');
        INFO_TABS.forEach(function (m) {
          if (buttons[m]) buttons[m].setAttribute('aria-pressed', String(m === 'data'));
        });
        if (slot) slot.setAttribute('aria-hidden', 'true');
        if (!simbadLoaded && !simbadBusy) loadSimbadData(currentTarget);
        if (!aladinReady && !aladinBusy) initAladin();
      }
      objectMode = (mode === 'object');
    }

    function getMode() {
      return widget.getAttribute('data-mode') || 'object';
    }

    /* ── Start ───────────────────────────────────────────────────── */
    init();

    /* Return public API for this instance */
    return {
      setTargetLock       : setTargetLock,
      setReticle          : setReticle,
      setAladdin          : setAladdin,
      initAladin          : initAladin,
      setAladinTarget     : setAladinTarget,
      setInfoMode         : setInfoMode,
      loadSimbadData      : loadSimbadData,
      loadVizierReferences: loadVizierReferences,
      loadPapers          : loadPapers,
      setTarget           : setTarget,
      setMode             : setMode,
      getMode             : getMode
    };
  }

  /* ── Shared helper ───────────────────────────────────────────── */
  function warn(msg) {
    if (typeof console !== 'undefined') console.warn(msg);
  }

  /* ══════════════════════════════════════════════════════════════════
     Public namespace
  ══════════════════════════════════════════════════════════════════ */
  global.NcHud01 = {
    /**
     * NcHud01.init(widgetElement, cfg) → instance API
     * Call once per .nc-hud-01 element.
     *
     * cfg = {
     *   objectMode  : boolean  default true
     *   target      : string   default "NGC 1300"
     *   vizierLimit : number   default 10, max 20
     *   onExpand    : function optional, called after expand transition
     * }
     */
    init: function (el, cfg) {
      if (!el) { warn('[NcHud01] init: element not found'); return null; }
      return createInstance(el, cfg);
    }
  };

})(window);
/*]]>*/
</script>
